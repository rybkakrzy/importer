// Test bootstrap for the Angular unit-test builder (Vitest runner).
// The app is zone-based (see src/main.ts), so the test environment needs
// Zone.js and its testing patch loaded before any spec runs.
import 'zone.js';
import 'zone.js/testing';
