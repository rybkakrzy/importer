# Current State

> Plik krótki i aktualny. Czytaj na początku każdej sesji.

## Ostatnia aktualizacja

2026-07-13 — **Fix: „Inna pierwsza strona" (w:titlePg) ignorowana — nagłówek first nie renderował się na str. 1 i ginął przy zapisie (ADR-0037, +31 testów)**. Zgłoszenie (DEV, dokument ING): pierwsza strona pokazywała ten sam nagłówek co kolejne. Root cause WIELOWARSTWOWY: (1) **GUI (główny)** — `wysiwyg-editor` trzymał JEDEN wspólny sygnał `_differentFirstPage`/`_differentOddEven` dla obu pasm, a settery `[headerContent]` i `[footerContent]` oba go nadpisywały → binding stopki (w szablonie PO nagłówku) zerował flagę nagłówka, gdy dokument miał nagłówek `first` bez stopki `first` (typowe) → str. 1 renderowała default, a po pierwszej edycji/zapisu skorumpowana flaga szła do `SaveDocumentRequest` i writer NIE emitował `w:titlePg` ani partu first (trwała utrata). Fix: flagi PER PASMO + wspólny resolver `_resolveHfVariant(pageIndex, kind)` dla renderu/edycji/routingu zapisu: first na PIERWSZEJ stronie KAŻDEJ sekcji (`pageSectionIndexes`, nie `pageIndex===0`), warianty first/even wpisów sekcyjnych honorowane, null = dziedziczenie z wcześniejszej sekcji/bazy (jak „Połącz z poprzednim"), `''` = celowo puste pasmo; `_updateSectionEntry` per wariant; toggle/dialog ustawiają oba pasma (titlePg = właściwość sekcji). (2) **Reader** — `titlePg` z pustym/BRAKUJĄCYM partem first dawał `DifferentFirstPage=false` → default wyciekał na str. 1 (Word pokazuje puste pasmo); teraz opt-in wyklucza fallback: flaga zawsze true przy titlePg, `FirstPageHtml=""` dla pustego/brakującego (semantyka ST_OnOff bez zmian — brak val/true/1/on = włączone); symetrycznie `evenAndOddHeaders`→`EvenHtml=""`; `Extract*OwnedBySection` emituje wpis też dla sekcji first-only/titlePg-only (null=dziedzicz, ""=jawnie puste). (3) **Writer** — warianty pisane NIEZALEŻNIE od default (first-only bez default przestał znikać przy 1. autosave), `titlePg` emitowany zawsze przy fladze (pusty first = part z pustym akapitem, CT_HdrFtr); PRZY OKAZJI pre-existing błąd schematu: `EnsureTitlePage` appendował `w:titlePg` PRZED dopisaniem pgSz/pgMar (AddHeaderAndFooter biegnie przed AddPageSettings) → każdy eksport z titlePg łamał sekwencję CT_SectPr; fix `AppendBeforeTitlePage` + wstawianie titlePg przed textDirection/docGrid (ważne dla ConvertPreservingPackage). Weryfikacja: nowe `TitlePageHeaderFidelityTests` **22/22** (ST_OnOff: bez val/true/1/on/false/0/off; pusty i brakujący first; first-only; titlePg per sekcja ≥ 1; walidator OOXML 0 błędów; round-tripy), Infrastructure **449/449**, Application 306/306, build sln 0 błędów; GUI nowy `wysiwyg-editor.first-page-header.spec.ts` **9/9**, pełne **464/464**, `ng build` OK. Ograniczenia (ADR-0037): titlePg sekcji dziedziczącej (bez własnych refs ani wpisu) przybliżane flagą najbliższego wcześniejszego wpisu; parzystość even/odd globalna (bez `pgNumType/start`); dokument zgłoszenia niedostępny lokalnie — potwierdzone na syntetycznych DOCX 1:1 ze strukturą OOXML zgłoszenia. Wcześniej:

2026-07-13 — **Binarny `.doc`: odzyskiwanie formatowania znaków (bold/italic/underline/strike/kolor) — warstwa CHPX (+2 testy)**. Zgłoszenie: `.doc` po imporcie do DOC2 tracił całe formatowanie (w MS Word pogrubienie/kursywa/podkreślenie/przekreślenie/kolory; w DOC2 zwykły tekst). Root cause: `LegacyDocBinaryConverter` świadomie odzyskiwał TYLKO tekst + podział akapitów (FIB + piece table PlcPcd) i NIGDY nie czytał warstwy formatowania znaków. Fix: pełny parser CHPX — `PlcfBteChpx` (FIB @0x00FA/@0x00FE, strumień tablicy) → `PnFkpChpx` (numery stron FKP w WordDocument) → CHPX FKP (512 B: rgfc/rgb/dane, crun@511) → grpprl SPRM-ów; wspierane `sprmCFBold`/`sprmCFItalic`/`sprmCFStrike`/`sprmCKul`/`sprmCIco` (paleta 16)/`sprmCCv` (COLORREF 24-bit), generyczny dekoder rozmiaru operandu z pola `spra`. Każdy znak → offset bajtowy (FC) z piece (compressed fc/2+k; unicode fc+2k) → format z indeksu CHPX; sąsiednie znaki o tym samym formacie łączą się w run; writer buduje `w:rPr` w kolejności CT_RPr (b,i,strike,color,u), reszta płynie dojrzałym `DocxToHtmlConverter` do podglądu. Odporność: niespójna warstwa CHPX → degradacja do samego tekstu (nigdy wyjątek); niespójny FIB/CLX → `null` (kontrolowane odrzucenie); ToggleOperand 0x80 „dziedzicz ze stylu" → brak stylu w płaskim DOCX → wył. Weryfikacja: nowe testy pokrywają każdą właściwość osobno + kombinację 4 + plain-bez-rPr (syntetyczny `.doc` z REALNĄ warstwą CHPX budowaną 1:1 wg MS-DOC — ta sama ścieżka co produkcja) + degradację przy uszkodzonym PnFkpChpx; `LegacyDocBinaryConverterTests` 7/7, Infrastructure **448/449** (jedyny fail `TitlePageHeaderFidelityTests…BlankFirstPage` PRE-EXISTING — failuje na czystym `git stash`, z równoległego WIP writera nagłówków). Ścieżka DOCX nietknięta (normalizer = pass-through dla ZIP). Ograniczenie świadome: bez stylów/tabel/obrazów; rozmiar/font znaku pominięte. Wcześniej:

2026-07-13 — **Fix GUI: po otwarciu dokumentu 9pt toolbar pokazywał domyślne 11 aż do 1. kliknięcia (+12 testów)**. Zgłoszenie: MS Word od razu pokazuje 9 w polu rozmiaru, DOC2 pokazuje 11 i dopiero klik w tekst aktualizuje na 9. Root cause (`wysiwyg-editor.ts`): `updateFormattingState()` liczyło formatowanie WYŁĄCZNIE z `window.getSelection()`; po załadowaniu dokumentu nie ma jeszcze selekcji w edytorze → metoda pomijała odczyt → `editorState.currentStyle` zostawał `{}` → toolbar (`selectedFontSize = signal(11)`) trzymał sztywny default. Dopiero `selectionchange` (klik/klawiatura) wyliczał rozmiar z computed style runu. Kaskada stylu POPRAWNA (klik dawał 9), więc backend nietknięty. Fix (Wariant B — synchronizacja BEZ widocznego kursora, zgodna z Word, bez kradzieży fokusu): gdy brak selekcji w edytorze, `updateFormattingState` rozwiązuje kontekst z PIERWSZEGO edytowalnego runu (`_firstEditableContextElement` — pierwszy blok p/h/li/td, w nim pierwszy tekstowy liść; pusty akapit → sam blok; pusty dokument → null=default) i liczy z niego rozmiar/krój/kolor/wyrównanie/blok/listy oraz bold/italic/… (`_readInlineFormattingFromElement`, bo `queryCommandState` nie ma wtedy selekcji). Ścieżka z żywą selekcją bez zmian (`fromSelection`). Nowy `_syncInitialFormatting()` z post-render `setTimeout(0)` w setterze `content` i `setContent()` — bez fokusa/selekcji/dirty/undo/scrolla, no-op po kliknięciu, guard `_isDestroyed`; **świadomie bez `afterNextRender`** (wersja z nim przeciekała między testami = Angular err 205, wywracała `font-family.spec`). Naprawia CAŁY toolbar wspólnym mechanizmem, nie tylko rozmiar. Weryfikacja: nowy `wysiwyg-editor.initial-formatting.spec.ts` 12/12 (9pt→9 bez kliknięcia, integracja EditorToolbar→9, przełączenie 9↔12, pusty/tabela/obraz, brak dirty), pełna suita GUI 445/445 na baseline, `ng build` OK. **UWAGA — kolizja współbieżna:** równolegle inny agent przepisywał ten sam plik (refaktor header/footer `differentFirstPage`, nowy `pastePlainTextAt`+`paste-plain.spec`); jego stan pośredni chwilowo psuł build (dangling `_differentFirstPage()`), a `paste-plain.spec` ma 1 failujący test — OBA niezwiązane z tą zmianą. Wcześniej:

2026-07-12 — **Zmiana czcionki dla nowo wpisywanego tekstu: zgłoszony bug NIE reprodukuje się (już naprawiony) + parytet `setFontFamily`↔`setFontSize`**. Zgłoszenie: po wybraniu innego kroju niż firmowy „ING Me" nowy tekst dalej dostaje domyślny. Weryfikacja empiryczna (headless Chrome + CDP, realne klawisze) we WSZYSTKICH scenariuszach (pusty akapit, karetka po tekście, pełny przepływ przez realny `<input list>` combobox, dwukrotna zmiana kroju bez wpisywania, pick→utrata-fokusu→pick→wpisz) pokazuje POPRAWNE działanie — „to jest" ląduje w `<span style="font-family:…">`, `caretFont`/`stateFont` = wybrany krój. Root cause zgłoszenia: screenshot z czasu STAREGO natywnego `<select>`; obecny build ma refactor na `<input list>`+`<datalist>` z odtwarzaniem selekcji PRZED `focus()` i kotwicą ZWS-spana. Firmowy `--corporate-font-family` (`wysiwyg-editor.scss`) aplikowany BEZ `!important` → inline span wygrywa. Jedyna realna usterka w kodzie: gałąź świeżego ZWS-spana w `setFontFamily` (zwinięta karetka) nie aktualizowała `savedSelection` ani nie odświeżała stanu toolbara jak `setFontSize` — dołożone (5 linii, defensywnie). Znane ograniczenie współdzielone z `setFontSize`: `pendingFontFamily`/`pendingFontSize` ustawiane, ale nigdy niekonsumowane (ścieżka „brak karetki i brak savedSelection" po cichu gubi wybór) — poza zakresem zgłoszenia. Weryfikacja: nowy `wysiwyg-editor.font-family.spec.ts` **5/5**, komponenty **281/281**, `ng build` OK. Backend nietknięty. Wcześniej:

2026-07-12 — **Listy DOCX etap 1 wariantu A (ADR-0036): eksport startOverride/lvlOverride + numPicBullet + suffix/isLgl/lvlRestart/wcięcia (+6 testów)**. Przyjęta specyfikacja „kompletnej obsługi list"; decyzja: wariant A — semantyka specyfikacji na istniejącym transporcie `data-*`, bez kanonicznego modelu JSON w kontraktach API (ADR-0036). Domknięte realne utraty danych przy pierwszym autosave: (1) **Writer wreszcie emituje restart numeracji** — `w:abstractNum` współdzielony po `data-abstract-num-id` (`_abstractIdByHtmlAbstract`), „Rozpocznij od nowa" = nowa instancja z `w:lvlOverride/w:startOverride` (wcześniej writer w ogóle nie znał lvlOverride — restart degradował się do niezależnej listy). (2) **Punktatory graficzne round-tripują**: `TryCreatePictureBullet` = ImagePart na CZĘŚCI numeracji + `w:numPicBullet` (wariant VML jak Word) + `w:lvlPicBulletId`, deduplikacja po data URI; marker img już NIE jest bake'owany jako inline run (dublowanie); nieosadzalny src → stary fallback numFmt=none. (3) **Rozszerzony kontrakt data-***: `data-start-override` (ODDZIELNIE od `data-start` = w:start definicji — zmiana semantyki `ListLevelInfo.Start`), `data-suffix` (space/nothing), `data-is-legal`, `data-lvl-restart` (surowa jednobazowa), `data-pic-bullet`, `data-ind-left/hanging/first-line-tw` (koniec niszczenia niestandardowych wcięć drabinką 720×(lvl+1)). (4) **Kolejność CT_Lvl naprawiona** (rPr szedł przed lvlJc — poza sekwencją schematu); numPicBullet→abstractNum→num. (5) Stan writera resetowany per Convert (`_numberingId`, `_numberingPart` — wcześniej przeciek części między konwersjami na reużytej instancji). **Runda 2 (pytanie o kontynuację/wcięcia/rodzaj numeracji) — 3 luki znalezione i domknięte:** (6) **`data-ilvl` honorowane w writerze** (`ResolveListLevel` w `ConvertListElement` + wszystkich skanach) — top-level fragment kontynuacji na poziomie 1 po zwykłym akapicie był spłaszczany do ilvl=0 (złe wcięcie + format poziomu 0). (7) **Pełny `w:lvlOverride/w:lvl`** (instancja nadpisuje cały wygląd poziomu): reader emituje `data-lvl-override="1"` (FindLevelDefinition zwraca źródło definicji), writer buduje `w:lvl` wewnątrz lvlOverride na instancji i NIE zapieka go we wspólnym abstrakcie — dwie instancje wspólnego abstraktu mogą wyglądać różnie. (8) **`UpgradeSharedAbstractLevels`** — fragment tworzący abstrakt definiuje tylko używane poziomy; późniejszy fragment współdzielący abstrakt dosyła definicje brakujących (bez tego poziom 1 drugiej listy zostawał przy drabince domyślnej); poziomy z jawnych data-* nieruszane. Wydzielone `BuildAbstractLevel`/`ResolveLevelOrdered` (wspólne dla abstraktu i lvlOverride). Weryfikacja: `ListNumberingFidelityTests` **21/21** (+9 łącznie: reader start-override osobno, writer lvlOverride na wspólnym abstrakcie, pełny round-trip restartu, suffix/isLgl/lvlRestart/wcięcia, pic-bullet pełny cykl z bajtami 1:1 i brakiem inline Drawing, walidator OOXML 0 błędów, fragment ilvl=1, upgrade poziomów wspólnego abstraktu, pełny lvlOverride na instancji), Infrastructure **421/421** (0 fail, goldeny nietknięte), build sln 0 błędów. **Runda 3 — prezentacja w edytorze Angular (etap 2+3 planu, część):** nowy `core/utils/list-label.util.ts` = silnik etykiet TS lustrzany do liczników readera (formaty decimal/decimalZero/letters-jak-Word-aa/roman, szablony `%1.%2)` z formatem POZIOMU odwołania, isLegal, suffix, `lvlRestart=0/N`, startOverride raz per instancja, licznik per abstrakt, kontynuacja fragmentów przez granice stron); `wysiwyg-editor.refreshListLabels()` liczy `data-list-label` na `li` (hooki: content-input, setContent, persist-debounce, undo/redo, pageEditorRefs.changes), render przez CSS `li[data-list-label]::before` (position:absolute right:100% — etykieta POZA edytowalnym tekstem, nie kopiuje się, wiersze zawijane od pozycji tekstu), `_serializeSingleEditor` zdejmuje atrybuty prezentacyjne (`stripListLabelAttributes`) — zapis niesie wyłącznie kontrakt data-* writera. Edycja listy (dodanie/usunięcie li) przelicza też DALSZE fragmenty tej samej listy — statyczny `<ol start>` tego nie umiał. Weryfikacja GUI: `list-label.util.spec` 12 + `wysiwyg-editor.list-labels.spec` 5, pełne **415/415** (0 fail), `ng build` OK; **wizualnie potwierdzone headless Chrome+CDP** (skill verify): żywy edytor renderuje `1. / 2. / 2.a) / 2.b) / 3.(kontynuacja po akapicie) / 1.(startOverride)`, zapis bez data-list-label, screenshot w scratchpadzie. **Runda 4 — katalog wymagań (schematy/punktatory/markery tekstowe):** fix readera — wieloznakowy `lvlText` punktatora („TODO:", „Pkt", „§ 1") renderuje się w CAŁOŚCI zamiast pierwszego znaku (`MapBulletChar` brał 1 code-point; skróty natywne disc/circle/square tylko dla jednoznakowego lvlText — tekst od „o" nie robi się kółkiem). Testy: schematy wielopoziomowe (1.1.1. / 1.1.1) / A.1.a. / I.A.1. / § 1.1.1) round-trip formatów+szablonów, katalog ~70 punktatorów Unicode (lvlText 1:1), markery tekstowe (pełny tekst w podglądzie + round-trip); TS: te same schematy jako etykiety + numeratory z literałami ((%1)/[%1]/Krok %1). Infrastructure **424/424**, GUI **417/417**. **Runda 5 — ENTER w listach i eksport po edycjach (weryfikacja empiryczna, headless Chrome + realne klawisze):** Enter na końcu/w środku li = nowy element tej samej listy z przeliczonymi etykietami (także dalszych fragmentów); wyjście z listy 2×Enter = Chrome dzieli ol KOPIUJĄC data-* (kontynuacja przeżywa), akapit po wyjściu = `<div>` → writer robi z niego zwykły akapit; test `Writer_EditorHtmlAfterEnterAndListExit_KeepsOneLogicalList` na dosłownym HTML z edytora (jedna instancja, kolejność, walidator, reimport start=3/4). Fix: `ensureBulletMarkers` — li utworzony Enterem w liście z marker spanem (symbol/„TODO:"/obraz) dostaje znacznik klonem z rodzeństwa (add-only); potwierdzone na żywo. Infrastructure **425/425**, GUI **419/419**. **Runda 6 — naprawy z audytu (R-30 częściowo):** (1) listy w KOMÓRKACH TABEL przestały ginąć — `AppendTableCellHtml` grupuje akapity listowe w ol/ul (`ConvertConsecutiveListItems`, jak body/SdtBlock), li w komórce z inline spacingiem ADR-0031, kontynuacja numeracji przez granicę komórek; (2) nieznane `w:numFmt` (ordinal/cardinalText/ordinalText/chicago/językowe) round-tripują SUROWYM tokenem zamiast degradacji do decimal (pkt 22.10; `Val.InnerText` → data-num-fmt → `new NumberFormatValues(token)`); (3) przy okazji pre-existing bug writera: kolejność dzieci `w:tblBorders` top→left→bottom→right wg CT_TblBorders (było top→bottom→… = błąd walidacji każdej tabeli bez data-tbl-style — wykryte, bo nowy test pierwszy walidował eksport tabeli). Infrastructure **427/427**, build sln 0 błędów. OTWARTE z audytu (R-30): paste z Worda (mso-list → literalny numer), remap data-num-id przy paste między dokumentami, atomowa paginacja długich list, rPr/lvlJc znacznika. Poza zakresem (następne etapy): komendy edytora (Tab/poziomy/kontynuuj/restart/ustaw wartość — nowe listy z edytora nadal bez data-*), wcięcia znacznika z data-ind-*-tw w podglądzie (dziś padding kontenera), `lvlRestart=N` w licznikach READERA (silnik TS już honoruje), łańcuch basedOn/styleLink, kody diagnostyczne LIST_*. Wcześniej:

2026-07-11 — **Fix GUI: przyciski wyrównania/list nie pokazywały stanu aktywnego — toolbar główny, mini-toolbar i menu (+7 testów)**. Zgłoszenie: klik „wyśrodkuj" nie zostawia przycisku zaznaczonego jak w MS Word; dotyczyło też list i menu kontekstowego. Root cause dwuwarstwowy: (1) `TextFormatting` niósł wyłącznie bold/italic/underline/strike/sub/sup (queryCommandState) — wyrównanie i stan list w ogóle nie istniały w kontrakcie `EditorState`; (2) przyciski wyrównania w `editor-toolbar.html` oraz cały mini-toolbar (wyrównanie+listy) nie miały bindingu `[class.active]`. Fix: model +`alignment?`/`bulletList?`/`numberedList?`; `wysiwyg-editor.updateFormattingState` liczy wyrównanie z computed `text-align` bloku pod karetką (pokrywa inline z `execCommand justify*` i wartości z importu DOCX; `start`/brak → `left` jak w Wordzie — lewa aktywna domyślnie), listy z przodka `li` (UL/OL); stan odświeża się na selectionchange (istniejąca ścieżka). Toolbar: `isAlignActive()` + `[class.active]` na 4 przyciskach wyrównania i 2 list, `isActive()` utwardzone na `=== true`. Document-editor: `alignmentActive()`, mini-toolbar `[class.mt-btn-active]` (wyrównanie+listy), menu „Formatuj→Wyrównanie" i kontekstowe podmenu z klasą `dropdown-item-checked`/`context-menu-item-checked` (nowe style, paleta jak `.toolbar-btn.active`). Weryfikacja: nowy `wysiwyg-editor.formatting-state.spec.ts` **7/7** (jsdom bez queryCommandState — stub w spec), pełne GUI **396/396** (0 fail), `ng build` OK. Backend nietknięty. Wcześniej:

2026-07-11 — **Fix (UAT): każda komórka tabeli zaczynała się od twardej spacji (&nbsp;) — przesunięcie tekstu względem MS Word (+3 testy)**. Zgłoszenie UAT ze screenów: po „Wstaw tabelę" i wpisaniu tekstu w komórce DOM zawierał `&nbsp;test` — placeholder pustej komórki (`td.innerHTML='&nbsp;'`) zostawał przed tekstem i trafiał jako U+00A0 do zapisanego DOCX. Fix dwuczęściowy (tylko front): (1) placeholder nowych komórek zmieniony na `<br>` w `wysiwyg-editor.insertTable` i we wszystkich operacjach tabelowych `document-editor` (wstaw wiersz/kolumnę, split/merge; `<br>` w `td` eksportuje się do pustego akapitu — `ConvertHtmlNode` case "br"); (2) nowy `onEditorBeforeInput` (binding `beforeinput` na stronach + nagłówku/stopce): blok (p/h1-6/li/td/th) zawierający wyłącznie U+00A0 dostaje zaznaczenie placeholdera tuż przed wstawieniem tekstu → pisanie go ZASTĘPUJE; obejmuje komórki/akapity z importu DOCX (`<td><p>&nbsp;</p></td>` — reader emituje &nbsp; dla pustych akapitów) i dokumenty zapisane przed poprawką. Separatory-akapity po tabeli celowo zostają z `&nbsp;` (`<p><br></p>` = akapit z `w:br` w eksporcie — dodatkowa linia). Weryfikacja: nowy `wysiwyg-editor.table-cell-placeholder.spec.ts` 3/3, pełne GUI **389/389** (42 pliki). Uwaga techniczna: `wysiwyg-editor.ts` zawiera literalny bajt NUL (klucz cache przypisów) — ripgrep widzi plik jako binarny, grep przez Select-String. Wcześniej:

2026-07-11 — **Fix: dokument Word „tylko do odczytu" (Ogranicz edycję / hasło zapisu) był w DOC2 Editor w pełni edytowalny (ADR-0034, +11 testów)**. Zgłoszenie ze screenów: `test_read_only_docx.docx` z wymuszonym `w:documentProtection w:edit="readOnly"` w Wordzie pokazuje „Możesz tylko wyświetlić ten dokument", a w edytorze dało się swobodnie pisać. Root cause: konwerter NIE czytał ochrony z settings.xml (jedyne użycie settings = `EvenAndOddHeaders`), a GUI decydowało o trybie wyłącznie z obecności `versionId` w URL. Fix: (1) **Reader** — `HasEnforcedEditProtection` (wymuszone `w:documentProtection` edit≠none, w tym tryby częściowe comments/forms/trackedChanges; `w:writeProtection` recommended/hash/hashValue) → nowe pole `DocumentContent.IsReadOnlyProtected` (Domain + interfejs TS). (2) **GUI `document-editor`** — sygnał `documentEditProtected` z flagi w `_applyLoadedContent` (samoresetujący na kolejnym dokumencie), `editingDisabled` rozszerzone, badge „Tylko do odczytu — dokument chroniony przed edycją" + toast przy otwarciu, guard `saveDocument()` z `readOnly()`→`editingDisabled()`, guard w ticku auto-save, przełącznik Autozapisu ukryty dla chronionych. (3) Przy okazji domknięta luka: `d2-wysiwyg-editor [readOnly]` dostawał surowe `readOnly()` zamiast `editingDisabled()` — treść była edytowalna w trybie `lockedByOther` mimo ukrytego toolbara. (4) Pre-existing fail `layout-shell.spec` naprawiony (stub `ResourceAccessService`/`MsalService` — NG0201 po tym, jak Dashboard zaczął wstrzykiwać gating zasobów). Weryfikacja: nowe `DocumentProtectionImportTests` **8/8**; GUI +3 testy w `document-editor.spec`, pełna suita **386/386** (pierwszy raz od dawna 0 faili). UWAGA: egzekwowanie tylko w GUI — endpoint zapisu nadal przyjmie PUT od klienta ignorującego flagę (risk R-29). Wcześniej:

2026-07-11 — **Fix readera: znaki specjalne z fontów symbolicznych (strzałka →, checkboxy) renderowane jako kwadrat/tofu (+10 testów)**. Zgłoszenie: „Utwórz KYC → Know Your Customer" — w Wordzie strzałka, w DOC2 kwadrat. Root cause w `DocxToHtmlConverter`: (1) `w:sym` (znak z fontu symbolicznego, kod w Private Use Area U+F000..U+F0FF) emitowany jako GOŁA encja `&#xF0xx;` — bez mapowania na Unicode i bez font-family przeglądarka renderuje tofu; (2) symbole w zwykłym `w:t` — kod PUA albo znak bajtowy w runie z fontem Wingdings (autokorekta Worda „-->" wstawia literalne `è`) — przechodziły bez tłumaczenia. Fix: `ConvertSymbolCharToHtml` + `MapSymbolicTextRun` + `TryMapSymbolicChar` z tabelami `SymbolFontMap` (greka/operatory/strzałki, kodowanie Adobe) i `WingdingsFontMap` (strzałki/checkboxy/kształty; 0xE8=➔ strzałka autokorekty) — kod po zdjęciu przesunięcia PUA mapowany na odpowiednik Unicode (renderuje się wszędzie, round-trip jako zwykły tekst przez writer); kod SPOZA tabel → encja w `<span style="font-family:'{font}'">` (fonty symboliczne są na Windows; writer odtwarza rFonts z CSS) zamiast tofu. `NormalizeSymbolFontName` dopasowuje nazwę DOKŁADNIE (symbol/wingdings/wingdings 2/3/webdings) — „Segoe UI Symbol" to normalny font Unicode, jego znaki nietykane; PUA bez fontu symbolicznego bez zgadywania. `MapBulletChar` (punktatory) celowo nieruszany (własny fallback •). Weryfikacja: nowe `SymbolCharFidelityTests` **10/10**, Infrastructure **412/412**, build sln 0 błędów, goldeny bez zmian. Wcześniej:

2026-07-11 — **Fix edytora: kursor przeskakiwał na ułamek sekundy na początek dokumentu podczas pisania, znaki lądowały w złym miejscu (+4 testy)**. Root cause (`wysiwyg-editor.ts`): `_repaginateNow` porównywał nowy rozkład stron z sygnałem `pageContents`, który między repaginacjami jest CELOWO przestarzały (`onPageInput` go nie aktualizuje) → przy pisaniu check `identical` zawsze false → co ~600 ms (max-wait debounce'a paginacji) rebind `[innerHTML]` wszystkich stron: podmiana DOM kasowała selekcję (karetka spadała na początek contenteditable przy zachowanym focusie), a restore był odroczony `setTimeout(0)` — znaki wpisane w oknie rebind→restore lądowały na początku dokumentu, znaki wpisane między `set()` a renderem ginęły ze starym DOM. Fix: (1) decyzja o rebindzie porównuje nowy rozkład z **żywym DOM** (serializacja per strona tym samym mechanizmem co `newPageContents`; celowo bez fallbacku `<p></p>`, żeby opróżniona strona wymusiła rebind) + wymagana zgodność liczby stron z sygnałem (sygnał steruje `@for`) — zwykłe pisanie bez przelania strony = ZERO rebindów; pominięcie `set()` bezpieczne, bo `getContent()` czyta żywy DOM; (2) przy realnym rebindzie restore karetki przez `afterNextRender(..., {injector})` zamiast `setTimeout(0)` (wąskie okno). Weryfikacja: `pagination-overflow.spec` 18/18 (+4: brak rebindu przy pisaniu, rebind przy przelaniu, pusta strona → syntetyczny `<p></p>`, rozjazd liczby stron sygnał↔DOM → rebind/scalanie), pełne GUI **382/383** (fail = pre-existing `spec-layout-shell`). Backend nietknięty. Wcześniej:

2026-07-10 — **Fix: tabela traci obramowania po 1. zapisie („rozpada się") — writer nie parsował rozbitych `border-*` ani `rgb()` (+3 testy)**. Zgłoszenie ze zrzutu (tabela „kredyty"/Qutalo): po edycji i zapisie w edytorze tabela traciła WSZYSTKIE linie komórek. Root cause potwierdzony harnessem round-trip: `HtmlToDocxConverter.ApplyCellBorders` rozpoznawał obramowanie tylko w formie skróconej `border-top: 0.7px solid #hex`, a przeglądarka przy `getContent`/`outerHTML` SERIALIZUJE jednolite obramowanie `<td>` jako OSOBNE właściwości `border-width` + `border-style` + `border-color` i normalizuje kolor `#000`→`rgb(0,0,0)`. Efekt: po 1. autosave `w:tcBorders` nie powstawało → wszystkie krawędzie `none`, tabela dodatkowo znaczona `data-no-borders`. Fix: parser akceptuje formę rozbitą (jednolita ramka na 4 strony; `border-style:none`→brak `tcBorders`, styl tabeli decyduje) ORAZ `rgb()/rgba()` w formie per-strona i `border:` (nowe helpery `TryParseBorderShorthand`/`GetCssDeclarationValue`/`NormalizeCssColorToken`). Ścieżka table-level i marker `data-no-borders` NIE ruszane (osobna kosmetyka ADR-0031, nieszkodliwa — komórki niosą własne linie). Weryfikacja: harness przed=14 komórek `border-*:none` / po=14× `border-top:0.7px solid #000000`; `TableStyleFidelityTests` 29/29 (+3), `Infrastructure.UnitTests` **391/391** 0 fail. Wcześniej:

2026-07-10 — **Kompletna obsługa przypisów dolnych DOCX (ADR-0032, +27 testów)**. Aplikacja nie obsługiwała przypisów Worda (reader je dropował, writer nie tworzył `footnotes.xml`). Pełny pionowy przepływ: import → model/API → render+edycja Angular → eksport → ponowny import. (1) **Model**: `Footnote { Id, Html }`; `DocumentContent.Footnotes` + `SaveDocumentRequest.Footnotes` (backend+TS). Tożsamość `fn-<ooxmlId>` STABILNA, oddzielona od numeru widocznego (kolejność pierwszych odwołań) i numerycznego `w:id` OOXML (deterministycznie 1..N na eksporcie; separatory techniczne id -1/0). Treść = jedno źródło prawdy, odwołania niosą tylko `data-footnote-id`. (2) **Reader** (`DocxToHtmlConverter`): `w:footnoteReference`→`<sup class="footnote-ref" data-footnote-id aria-label>N</sup>` (numer wg pierwszego wystąpienia, wspólny dla powtórzonych), `FootnoteReferenceMark` pomijany; `ExtractFootnotes` czyta `FootnotesPart` z pominięciem separator/continuationSeparator/continuationNotice, treść przez ISTNIEJĄCE konwertery akapitu/tabeli (formatowanie/wieloakapitowość/Unicode); jeden wadliwy przypis nie przerywa importu (try/catch + `ILogger`), odwołanie bez treści zachowane z pustą treścią + diagnostyka; brak odwołań→`Footnotes=null`. (3) **Writer** (`HtmlToDocxConverter`): `AssignFootnoteOoxmlIds`+`CreateFootnoteReferenceRun` (odwołanie do nieistniejącego przypisu POMIJANE), `AddFootnotes` tworzy `word/footnotes.xml` (relacja+content type przez `AddNewPart<FootnotesPart>`) z separatorami technicznymi i treścią (pierwszy akapit `w:footnoteRef`; treść przez `ConvertHtmlToBody`, relacje obrazów do części przypisów); brak przypisów→brak części; `Convert`/`ConvertPreservingPackage`+interfejs+param `footnotes` przewleczony przez `SaveDocumentCommand`/`DownloadEditedDocumentCommand`+kontrolery. (4) **GUI** (`wysiwyg-editor`): input `footnotes`+output `footnotesChange`; panel treści POZA contenteditable (`.footnotes-panel`, numer wg kolejności, edytowalny, usuń, aria-label); odwołania jako `<sup class="footnote-ref">` (SCSS superscript); `commitFootnoteContent`/`addFootnoteAtCursor`/`removeFootnote`/`syncFootnotesWithBody` (renumeracja wg DOM+przycięcie osieroconych, w debounce persist); `document-editor` sygnał `footnotes` z importu→`[footnotes]`/`(footnotesChange)`+`buildSaveRequest`. Weryfikacja: `FootnoteFidelityTests` **18/18** (import, eksport z inspekcją ZIP/XML, walidacja OOXML Office2013=0 błędów, round-trip, add/edit/delete/reorder, regresja bez przypisów; fixture `FootnoteTestDocuments` na czystym OpenXML SDK — niezależny od eksportera), solucja Domain 79/Application 306/Api 130/Api.Integration 9/Infrastructure **388** 0 fail; GUI `wysiwyg-editor.footnotes.spec` 7/7 + `document-editor.footnotes.spec` 2/2, pełne **378/379** (fail = pre-existing `spec-layout-shell`), `ng build`+`dotnet build` OK. Ograniczenia: brak infra E2E (tylko Vitest) → headless pokryty TestBed + backend round-trip zamiast Playwright; panel przypisów dla całego dokumentu (bez przypisania do strony); ID OOXML nie zachowywane 1:1 (deterministyczne przemapowanie). Wcześniej:

2026-07-09 — **Edytor: ENTER na dole strony przestaje ROZCIĄGAĆ kartkę i nie duplikuje treści (ADR-0038, +2 testy)**. Zgłoszenie: „ENTER przekraczający kraniec strony rozciąga kartkę w nieskończoność zamiast przejść na kolejną stronę". Diagnoza w REALNEJ przeglądarce (headless Chrome + CDP — testy jednostkowe stubują wysokości i tego NIE łapały): dwie przyczyny u źródła. (1) **Desync `[innerHTML]`↔contenteditable**: `_repaginateNow` czyta bloki z DOM, redystrybuuje i woła `pageContents.set(...)`, ale Angular POMIJA nadpisanie `[innerHTML]` strony, której wartość SafeHtml się nie zmieniła (cache w `getPageContentSafe`, celowy pod perf/kursor). Strona, w którą użytkownik pisze, ma w DOM ŻYWE dopisane akapity nieznane sygnałowi → jej DOM nie jest resetowany do paginacji: rośnie w pion, a nadmiar jest DODATKOWO duplikowany na kolejne strony i wchodzi do ZAPISU (`getContent` czyta DOM → korupcja/duplikacja treści; empirycznie +8 fantomowych pustych akapitów na wywołanie). (2) **CSS**: `.page` miała `min-height` + `.editor-content{flex:1}` — flex-kontener o wysokości auto rośnie do max-content, więc `overflow:hidden` NIE przycinał, tylko kartka się rozciągała (zmierzone: 45×ENTER → 1. strona 2300 px, „Strona 2 z 28"). Fix: (A) `_syncPageEditorDom(newPageContents)` po repaginacji WYMUSZA zgodność DOM edytorów z rozkładem (nadpisuje tylko rozjechane strony, re-opakowuje obrazy), (B) `.page` ma DEFINITYWNĄ `height` (`pageHeightPx`) — flex ogranicza `.editor-content`, nadmiar przycięty, kartka nigdy > format. Zoom sprawdzony jako CZYSTO wizualny (`transform:scale`, `origin:top center`) — rozmiar strony (cm→px @96 DPI, `CSS_PX_PER_CM`) niezależny od zoomu jak w Wordzie (część zgłoszenia o skalowaniu była OK). Weryfikacja: CDP PO — 1. strona 1122 px (A4) przez cały burst, po ustabilizowaniu 3 strony A4, `getContent` 34 unikalne akapity, ZERO duplikatów/utraty; pełna strona 50 akapitów bez przycięcia (`scrollHeight==clientHeight`); `wysiwyg-editor.pagination-overflow.spec` 16/16 (+2), pełne `wysiwyg-editor` 89/89, `ng build` 0 błędów. Ograniczenie: pojedynczy blok wyższy niż strona jest teraz przycinany (wcześniej rozciągał) — akceptowalne. Wcześniej:

2026-07-08 — **Znaki z `w:sym` (strzałki/checkboxy z fontów Symbol/Wingdings) widoczne w edytorze i w finalnym dokumencie (+6 testów)**. Objaw zgłoszony: „znaki specjalne — strzałki i inne — nie wyświetlają się w edytorze i, co gorsza, nie przenoszą się do finalnego dokumentu". Root cause w readerze (`DocxToHtmlConverter`, gałąź `SymbolChar` w `ConvertRunChildToHtml`): znaki wstawione przez „Wstaw → Symbol" (`w:sym w:font=... w:char=...`) emitowane były jako SUROWY punkt Private Use Area `&#xF0E0;` — bez zainstalowanego fontu symbolicznego renderuje się jako „tofu"/pusto, a po round-tripie writer robił z niego literalny `w:t` bez fontu (nadal niewidoczny). Reader IGNOROWAŁ też `sym.Font` (font symbolu bywa inny niż `w:rFonts` runa). Fix: nowy `ConvertSymbolCharToHtml` — (1) znane glify → prawdziwy Unicode (`MapSymbolCharToUnicode`: Symbol ← ↑ → ↓ ↔ ⇐ ⇑ ⇒ ⇓ ⇔ ± ≤ ≥ ≠ ≈ × ÷ ∞ ° •; Wingdings ☑ ☐ ✔ ■ •), widoczne wszędzie i round-tripują jako tekst; (2) reszta → znak w ORYGINALNYM foncie glifowym (`<span style="font-family:'Wingdings'">` z niskim bajtem — Word koduje symbol w PUA F000–F0FF = ten sam glif co 0xXX; writer odtwarza `w:rFonts` z `font-family`, glif przeżywa zapis, w Wordzie zawsze poprawny). Zakres mapy Unicode celowo wąski/pewny; nieznane kody → fallback z fontem (zero ryzyka błędnego mapowania). Zwykłe strzałki Unicode w `w:t` działały już wcześniej (`WebUtility.HtmlEncode` zachowuje znaki ≥ U+0100). Weryfikacja: nowy `SymbolCharImportTests` 6/6, Infrastructure **376/376**, golden bez zmian. Ograniczenie: nie odtwarzamy elementu `w:sym` (wynik wizualnie identyczny, bez utraty danych). Wcześniej:

2026-07-08 — **Zapis przez pass-through: definicje stylów tabel/motywu/numeracji przeżywają autosave (opcjonalny `masterId` w `/save`, +7 testów)**. Domknięcie rekomendacji z ADR-0031: `POST /document/save` (ścieżka autosave/Zapisz/Pobierz/Zakończ) używa `ConvertPreservingPackage`, gdy dostanie `masterId`. Dotąd zapis ZAWSZE regenerował pakiet od zera (14 stylów, bez definicji stylów tabel — stąd `data-no-borders` od 2. otwarcia); teraz gdy komenda niesie `masterId` + dokument to DOCX z wersją bazową, handler wczytuje oryginał z GCS i zachowuje `styles.xml`/`theme`/`fontTable`/`numbering` (38 stylów z definicją `Tabela-Siatka`). Zmiany: `SaveDocumentCommand.MasterId` + `SaveDocumentRequest.MasterId` (opcjonalne, wstecznie kompatybilne); `SaveDocumentCommandHandler` dostał opcjonalne `IDocumentRepository`/`IDocumentStorageService`/`ILogger` (nullable → czysta konwersja bez masterId, np. „nowy dokument"/testy, działa bez storage), pass-through z **best-effort fallbackiem** do `Convert` przy każdym błędzie (uszkodzony oryginał/GCS down nie wywala zapisu); `DocumentController.SaveDocument` przekazuje masterId; GUI `buildSaveRequest()` dołącza `masterId ?? undefined`. Weryfikacja: harness pass-through vs regeneracja na realnym qutable (14 stylów/brak def. vs 38 stylów/def. obecna + theme + fontTable); `SaveDocumentCommandHandlerTests` 13/13 (+5), Application **311/311**, Api **130/130**, build sln 0 błędów; GUI `document-editor.spec` +2, **371/372** (fail tylko pre-existing layout-shell), `ng build` OK. Uwaga: zachowuje definicje z ORYGINAŁU (v1); body/nagłówki/obrazy z edytora nadal generowane (jak w Download); Sign nadal regeneruje (poza zakresem). Wcześniej:

2026-07-08 — **Domyślne odstępy/interlinia dokumentu + tabele przestają się psuć po zapisie (ADR-0031, +15 testów)**. Root cause zgłoszenia „tabele po zapisie w edytorze totalnie się psują" (diagnoza na realnym `qutable.docx`): writer regenerował styles.xml z HARDKODAMI (docDefaults 11pt/after=160/line=259, zero definicji stylów tabel) → 1. zapis zmniejszał tekst 12→11pt, zmieniał interlinię 278→259, a akapity w komórkach traciły pPr stylu „Tabela – Siatka" (after=0/line=240) i dostawały pełne odstępy docDefaults → **wiersze tabel puchły ~2× w Wordzie**; do tego GUI rozwijało wrapper `.document-content` przy paginacji i NIE odtwarzało go w getContent → writer w produkcji nie widział nawet fontu dokumentu. Fix: (1) **Reader** — docDefaults/pPrDefault + nadpisanie z domyślnego stylu akapitowego → kontener niesie `data-default-before/after-tw`/`data-default-line`/`data-default-line-rule` + inline `line-height`; **pPr stylu tabeli rozwiązywane INLINE na akapity komórek** (`TableStyleContext.ParagraphDefaultCss`, save/restore dla zagnieżdżeń — częściowe domknięcie luki ADR-0026); `<col>` niesie dokładne `data-w-tw` z tblGrid; finalny CSS akapitu przez `DeduplicateCss` (duplikat styl-vs-direct był brany przez regexy writera z PIERWSZEGO wystąpienia — nadpisania ginęły). (2) **Writer** — `CaptureDocumentDefaults` z kontenera → docDefaults+Normal z realnych wartości (fallback = konfiguracja, zero regresji bez wrappera); tabela z `data-tbl-style` bez CSS border NIE dostaje `tblBorders val=none` (nadpisywało obramowania stylu w Wordzie, a reader utrwalał to jako `data-no-borders`); tblGrid/tcW z `data-w-tw` (koniec dryfu 3020→3015→… per zapis; szerokości % nietykane); **kolejność schematu**: `NormalizeParagraphPropertiesOrder` (spacing/tabs/pStyle przed jc — psuło się dopiero, gdy akapit miał JEDNOCZEŚNIE wyrównanie i spacing, np. wyśrodkowana komórka), `NormalizeTableCellPropertiesOrder` (tcW przed gridSpan, tcBorders przed tcMar/vAlign), tcBorders top→left→bottom→right — walidator OOXML (Office2013) na eksporcie: **0 błędów (było 52)**. (3) **GUI** — `_captureDocumentDefaults` przechwytuje atrybuty wrappera i rozwija go, `getContent()` OWIJA treść z powrotem; sygnały `documentDefaultLineHeight` (binding line-height na `.editor-content`) i `documentDefaultParagraphSpacing` (CSS var `--doc-par-margin`, SCSS fallback 10px) — edytor renderuje odstępy między akapitami i interlinię jak Word; `syncTableColgroup` usuwa `data-w-tw` przy zmianie szerokości kolumny (po resize wraca fallback px). Weryfikacja: nowe `DocumentDefaultsAndTableSpacingFidelityTests` **11/11**, Infrastructure **370/370** (goldeny simple/merged-cells-table zregenerowane — diff tylko data-w-tw), build sln 0 błędów; GUI +4 specy, **369/370** (fail tylko pre-existing layout-shell), `ng build` OK; harness qutable: docDefaults sz=24/line=278 w eksporcie, komórki `w:spacing after=0 line=240`, tblGrid 3020/3021/3021 stabilny przez 2 cykle. Ograniczenia: definicje stylów tabel wracają w pełni tylko przez pass-through (Download) — docelowo Save→`ConvertPreservingPackage` (wymaga masterId w `POST /document/save`, zmiana kontraktu do ustalenia); `data-no-borders` od 2. otwarcia regenerowanego pakietu (nieszkodliwe — bordery zapieczone per komórka). Wcześniej:

2026-07-07 — **Kotwiczenie jak w Wordzie (ADR-0030): pola tekstowe przeżywają zapis, jawny model kotwicy, znacznik kotwicy w edytorze (+36 testów)**. Trzy warstwy naprawione u źródła: (1) **Writer** nie miał ŻADNEJ obsługi `div.docx-textbox` (generyczna gałąź div spłaszczała pole do akapitów — przy 1. autosave ginęła ramka/pozycja/rozmiar/kotwica); nowy `BuildTextBoxDrawing` odtwarza `wps:wsp`+`w:txbxContent`, pływające → `wp:anchor` (X=page, Y=margin — osie ADR-0029, jak obrazy), inline → `wp:inline`, `data-border-*`→`a:ln`; model kotwicy: **textbox kotwiczy do NASTĘPNEGO akapitu** (bufor `AttachPendingTextBoxes`; wpięty też w header/footer, `<li>` i zagnieżdżenia). (2) **Reader**: metadane kotwicy jawnie na divie (`data-pos-mode/x/y/width/height-emu`), **hoisting** divu PRZED akapit-kotwicę (blokowy div w `<p>` był re-parentowany przez przeglądarkę i ROZCINAŁ akapit; w `<li>` zostaje w środku), fix dublowania w `mc:AlternateContent`, koniec zapiekania obramowania edycyjnego `#ccc` w treść (realne `a:ln` kształtu → inline `border`+`data-border-*`), `data-wrap` na obrazach/textboxach → writer emituje `wrapSquare`/`wrapTopBottom` zamiast zawsze `WrapNone` (Word odzyskuje opływanie). Round-trip idempotentny (2 cykle bez wzrostu liczby akapitów — test). (3) **GUI**: nowy `core/utils/floating-anchor.util.ts` (czyste funkcje: `findAnchorParagraph` — obraz=blokowy przodek, textbox=następny brat-akapit; `computeAnchorBadgePosition` — px układu strony z korekcją skali; `viewportDeltaToLayout`; `isPointerOnEdge`); **znacznik kotwicy** (SVG anchor, aria-label, pointer-events:none) jako overlay w `.page` POZA contenteditable — pokazywany przy akapicie-kotwicy zaznaczonego elementu PŁYWAJĄCEGO, znika przy odznaczeniu/usunięciu, jedzie za akapitem po edycji/repaginacji (rAF-koalescencja), zoom/scroll bez przeliczeń (skaluje się ze stroną); **textbox interaktywny**: klik = `tb-selected`, drag pływającego pola z pasa 8px krawędzi (wnętrze = edycja tekstu; kursor move przez `tb-edge`), delty przez skalę zoomu, drag-end → `left/top`+`data-x/y-emu` (kotwica bez zmian — tylko offsety); **fix**: drag pływających OBRAZÓW dzieli delty przez `_pageVisualScale` (wcześniej obraz uciekał kursorowi przy zoomie ≠ 100%); serializacja czyści `tb-*`/badge; SCSS: obramowanie EDYCYJNE = outline na hover/focus-within (dashed) i selected/drag (solid), dokumentowe inline `border` nieruszane. Weryfikacja: nowy `TextBoxAnchorRoundTripTests` **12/12**, Infrastructure **359/359**, build `D2ViewerEditor.sln` 0 błędów; GUI `floating-anchor.util.spec` 15 + `wysiwyg-editor.anchor.spec` 9, pełne **365/366** (jedyny fail = pre-existing `layout-shell`, failuje też w izolacji), `ng build` OK. Ograniczenia: `wps:wsp` bez fallbacku VML (Word <2010), wrapTight/Through ≈ square, wrap square nadal renderowany w edytorze jako pozycja absolutna (przybliżenie). Wcześniej:

2026-07-07 — **Stopka/nagłówek: tekst obok numeru strony przestał znikać — maszyna pól złożonych obsługuje formanty inline (SdtRun)**. Objaw: w stopce „tekst po lewej — numer strony — tekst po prawej" po imporcie zostawał sam numer strony. Diagnoza na harnessie konwertera (8 scenariuszy stopki: taby direct/ze stylu, tabela 3-kolumnowa, SdtBlock galerii, fldSimple, hyperlink — te działały): pierwsza utrata w READERZE, `DocxToHtmlConverter.ConvertComplexFieldParagraphContent` — akapit z JAKIMKOLWIEK `w:fldChar` szedł przez ścieżkę pól złożonych, która obsługiwała tylko `Run`/`Hyperlink`/`SimpleField`, a **`SdtRun` (formant inline — tak Word trzyma tytuł/klauzulę obok numeru i CAŁE galerie „Strona X z Y") był pomijany w całości** (wariant galerii tracił nawet numer strony). Fix reader: maszyna pól przepisana na `ComplexFieldState` + `AppendComplexFieldContent`/`AppendComplexFieldRun`/`AppendComplexFieldSdtRun` — rekurencyjnie schodzi w `SdtContentRun` ze WSPÓLNYM stanem pola (pole może żyć w formancie albo przecinać jego granicę), emituje wrapper `span.sdt-inline` + `data-sdt-*` jak `ConvertSdtRunToHtml`, kolejność dokumentu zachowana, PAGE/NUMPAGES → dynamiczne placeholdery, inne pola → wartość zbuforowana (KR-05 bez zmian). Fix writer: `HtmlToDocxConverter.BuildSdtRunFromHtml` odtwarza `span.field-page/page-number/field-numpages` wewnątrz formantu jako pola `fldSimple` (wcześniej `CreateRunsFromNode` robił z nich literalny tekst „{page}" przy 1. autosave); pusty-content check obejmuje `SimpleField`. Front bez zmian w kodzie (podmiana `{page}/{pages}` jest regexem na całym HTML stopki — działa też w sdt-inline; sanitizer bypassowany; CSS bez ukrywania). Weryfikacja: nowy `HeaderFooterFieldNeighborContentTests` **6/6** (SDT obok pola z kolejnością, galeria PAGE+NUMPAGES w SDT, cached DATE w SDT, plain-runs wokół pól, eksport sdt-inline→fldSimple, pełny round-trip DOCX→HTML→DOCX→HTML), Infrastructure **347/347**, build solucji 0 błędów, GUI `wysiwyg-editor.spec` **32/32** (+1 test treści stopki obok numeru). Wcześniej:

2026-07-07 — **Edytor: ENTER na dole strony nie „rozciąga" już wizualnie kartki — natychmiastowa repaginacja + przewinięcie kursora**. Objaw zgłoszony: ENTER na dolnej krawędzi strony wydłużał kartkę (zamiast przejść na następną), a druga strona pojawiała się dopiero po ręcznym scrollu. Root cause = kombinacja trzech rzeczy (fix z 2026-07-05 naprawił tylko pomiar, więc „wrażenie" zostało): (1) `.page{min-height:1122px;overflow:visible}` + `.editor-content{flex:1}` z domyślnym `min-height:auto` flex-itemu → treść rozciąga kartkę w tej samej klatce co ENTER; (2) ENTER nie był obsługiwany specjalnie — repaginacja szła przez **debounce** `_schedulePaginate` (250 ms), więc rozciągnięcie było widoczne ~250 ms zanim `_repaginateNow` przelał treść na nową stronę; (3) `_restoreGlobalCaret` nie robił `scrollIntoView` → przelana treść lądowała poza widokiem. Fix (tylko front, `wysiwyg-editor.ts`): (A) `handleKeyboard` przy `Enter` bez ctrl/meta/alt woła `_flushPaginateSoon()` (koalescujący `requestAnimationFrame` → `_flushPaginateNow` kasuje debounce i repaginuje NATYCHMIAST po mutacji DOM; bez `preventDefault`; przytrzymany ENTER = 1 repaginacja/klatkę przez guard `_paginateRafHandle`, sprzątany w `ngOnDestroy`); (B) `_restoreGlobalCaret` woła `target.scrollIntoView({block:'nearest',inline:'nearest'})` — nowa strona wchodzi w widok bez ręcznego scrolla, a `nearest` nie skacze gdy kursor już widoczny. Zwykłe pisanie nadal debounce'owane (perf). Writer/kontrakty/model nietknięte. Weryfikacja: `wysiwyg-editor.pagination-overflow.spec` **14/14** (+5), `enter-caret` 3/3, `page-break` 2/2, `wysiwyg-editor.spec` 31/31 — brak regresji. Wcześniej:

2026-07-07 — **Wektorowe EMF (logo w stopce Qutalo) przestały być niewidoczne — mapowanie window/viewport w `MetafileVectorTranslator`**. Przyczyna źródłowa: tłumacz EMF pomijał mapowanie page→device (`SETWINDOW*`/`SETVIEWPORT*` na liście „silent"), a `viewBox` SVG brał z `rclBounds` (jednostki URZĄDZENIA) — współrzędne rysowania są LOGICZNE, więc dla klasycznego EMF ze ścieżek (`BEGINPATH`/`POLYBEZIERTO16`/`LINETO`/`FILLPATH`, brush) z niejednostkowym window/viewport ścieżki lądowały poza viewBox → SVG poprawny, ale pusty → **logo widoczne w Wordzie, niewidoczne w edytorze**. Diagnoza na realnym pliku (wyciąg/pismo Qutalo): `word/media/` = 2× `.emf` (NIE EMF+; osadzone w stopce przez `mc:AlternateContent > mc:Choice Requires="wpg" > wpg:wgp`). Fix: `GdiState.Apply` stosuje mapowanie window→viewport po world-transformie `(p−winOrg)·(vpExt/winExt)+vpOrg`, aktywne TYLKO gdy metafile zdefiniował OBA zakresy (inaczej tożsamość — zero regresji dla metafile bez tych rekordów); rekordy EMR 9/10/11/12 obsłużone; safety-net w `BuildSvg` (gdy treść nie przecina `rclBounds` → `viewBox` z bbox realnej treści). Oryginalny EMF nadal pass-through do DOCX (podgląd-only; Word/eksport nietknięte). Weryfikacja: nowy `Emf_WindowViewportMapping_ScalesLogicalCoordsIntoDeviceBounds` + `Infrastructure.UnitTests` **341/341**. Ograniczenia: pojedynczy `EMR_ALPHABLEND` w logo nierysowany; tryby metryczne (MM_LOMETRIC..HITWIPS) łapie safety-net content-bbox; `wpg:wgp` = pierwszy blip. Wcześniej:

2026-07-07 — **Kształty DrawingML z geometrią (`a:custGeom` / ellipse / roundRect) renderowane — grafika z oryginału przestała znikać w edytorze**. Przyczyna źródłowa: `RenderVectorShapeAsHtml` obsługiwał tylko linię i prostokąt; kształt `wps:wsp` bez obrazu/pola tekstowego z `a:custGeom` (własna ścieżka — np. wordmark „Qutalo", ikona ostrzeżenia „!") zwracał `""` → grafika **nie rysowała się w edytorze**. Fix: `BuildCustomGeometrySvg` tłumaczy ścieżkę na inline `<svg><path>` (moveTo/lnTo/cubicBezTo/quadBezTo/close; `viewBox` z `a:path w/h`; `preserveAspectRatio=none` = dopasowanie do `wp:extent`); presety rozszerzone o elipsę (`border-radius:50%`) i roundRect (`12%`); kolor z `spPr/a:solidFill` przez `GetShapeFillHex` (nie z obrysu `a:ln`/ukrytej linii), obrys tylko gdy `a:ln` ma realny fill. PODGLĄD-only (writer nie odtwarza — jak istniejące linie/prostokąty; oryginał v1 nietykalny). **Audyt czcionki stopki**: na realnym szablonie Qutalo footer renderuje się POPRAWNIE — runy niosą jawne `w:sz=16` (8pt); kontener `header-footer-content` dostaje domyślny `11pt Calibri` z `docDefaults w:sz=22` (minorHAnsi) i dotyczy tylko tekstu BEZ jawnego rozmiaru. Dokument ze zrzutu (z „Konsekwencje"/„Podstawa prawna") nie jest lokalnie dostępny — rozjazd rozmiaru „boxu" Qutalo najpewniej z rPr **stylu tabeli** (nieaplikowany do tekstu komórki — luka ADR-0026), do potwierdzenia na właściwym pliku. Weryfikacja: Infrastructure **340/340** (+`CustomGeometryShape_WithoutImage_RendersAsInlineSvgPath`), round-trip HTML→DOCX bez wyjątku, podgląd SVG „Qutalo" potwierdzony wizualnie (headless Chrome). Wcześniej:

2026-07-07 — **Kotwiczone obiekty (wp:anchor): pozycja jak w Wordzie — `relativeFrom` + `wp:align` (ADR-0029, +8 testów)**. Objaw: logo i pływające pole tekstowe z adresem (dokument Qutalo) renderowały się w edytorze przesunięte względem MS Word — o cały margines za bardzo w lewo/za wysoko albo do lewej zamiast do prawej. Przyczyna w readerze: `ConvertDrawingToHtml`/`BuildTextBoxLayoutCss` brały surowy `wp:posOffset` ignorując `relativeFrom` (offset kotwiczony do marginesu/kolumny startuje od obszaru treści, nie od krawędzi strony) oraz `wp:align` (wyrównanie bez jawnego offsetu — logo do prawego marginesu — zerowało się do rogu). Fix: nowy `ResolveAnchorPosition`/`ResolveAxis` liczy współrzędne układu edytora (X = lewa krawędź strony page-relative; Y = góra obszaru treści = page-relative minus górny margines), z bazą+rozpiętością wg `relativeFrom` (page/margin/column/leftMargin/rightMargin/insideMargin/…) i wyrównaniem `wp:align` (right/center/left/inside/outside). Rozmiar do align z `wp:extent`. Geometria 1. sekcji w polach instancji (`LoadPageGeometry`, brak marginesów → 1440 twips). Wpięte w obie ścieżki: obrazy (`data-x-emu`/`data-y-emu`) i pola tekstowe/kształty (inline `position:absolute`). Writer: pionowa kotwica `RelativeFrom=Margin` zamiast `Page` (spójne z Y-originem edytora, round-trip idempotentny, plik poprawny w Wordzie). `OoxmlUnits.TwipsToEmu`. Front bez zmian. **To domyka WIP z poprzedniego wpisu** (3 faile `ImageFloatingRoundTripTests` naprawione zmianą writer V→Margin). Weryfikacja: nowy `DocxAnchorPositionFidelityTests` 8/8, zaktualizowane `Doc2ImportFidelityTests.AnchoredTextBox…` + `ImageFloatingRoundTripTests`, Infrastructure **339/339**, build solucji 0 błędów. Ograniczenia: pionowe `wp:align` center/bottom przybliżone, inside/outside bez stron parzystych/nieparzystych. Wcześniej:

2026-07-07 — **„Inne na pierwszej stronie" (titlePg): stopka/nagłówek pierwszej strony wyciekały na kolejne strony**. Objaw (Doc2/Qutalo): stopka z adresem Qutalo widoczna w edytorze na str. 2, choć w Wordzie tylko na str. 1 (dokument ma „Różne dla pierwszej strony" = referencja `first` + `titlePg`, BEZ referencji `default`). Przyczyna w `DocxToHtmlConverter.ExtractHeader`/`ExtractFooter`: gdy sekcja nie miała referencji `default`, kod spadał do `mainPart.FooterParts.FirstOrDefault()` = część pierwszej strony → renderowana jako domyślna na WSZYSTKICH stronach (`firstPageHtml` == `html` == adres Qutalo). Fix: fallback do dowolnej części pakietu tylko gdy sekcja nie deklaruje ŻADNEJ referencji (legacy); przy `first`/`even` bez `default` domyślny wariant jest pusty (`Html = string.Empty`, `DifferentFirstPage = true`), a metoda zwraca `HeaderFooterContent` także gdy tylko `FirstPageHtml`/`EvenHtml` istnieje (`null` tylko gdy wszystkie puste). Nowe `SectionDeclaresAnyHeaderReference`/`SectionDeclaresAnyFooterReference`. Front bez zmian (`wysiwyg-editor` już mapuje str. 0 → `firstPageHtml`, reszta → `html`). Weryfikacja: `DocxToHtmlConverterSectionReferenceTests` **10/10** (+2 first-page-only), Infrastructure build 0 błędów. UWAGA: 3 faile `ImageFloatingRoundTripTests` w drzewie roboczym pochodzą z RÓWNOLEGŁEGO WIP kotwic (`ResolveAnchorPosition`), nie z tej zmiany (potwierdzone: przechodzą na czystym HEAD konwertera). Wielosekcyjne `Extract*OwnedBySection` bez tego fallbacku — nietknięte. Wcześniej:

2026-07-07 — **SVG „puste białe logo" w nagłówku (Doc2/Qutalo): sanitizer wycinał wewnętrzne `<use>` + odrzucał BOM/DOCTYPE**. Objaw: element graficzny w edytorze osadzony z poprawnymi wymiarami, ale pusty/biały (w Wordzie logo OK). Trzy przyczyny w `GraphicConversionService`: (1) `SanitizeSvg` usuwał WSZYSTKIE `<use>` — logo z `<defs>`+`<use href="#id">` (typowy eksport korporacyjny) traciło całą widoczną treść (defs niewidoczne); teraz `<use>` z wewnętrznym `#id` zostaje (`HasInternalFragmentHref`), zewnętrzny URL/data:/brak href — usuwany. (2) UTF-8 BOM w bajtach partu wywalał parser XML → poprawny SVG odrzucany, obraz znikał; `TrimStart(U+FEFF/U+200B/białe)` przed parsowaniem. (3) DOCTYPE (Illustrator) → `DtdProcessing.Prohibit` rzucał; zmiana na `Ignore` w `SafeParse` — DTD pomijany, XXE/billion-laughs nadal zablokowane (encje niezdefiniowane → parser rzuca → null; test bez zmian zachowania). Weryfikacja (czysty worktree HEAD+zmiana, bo drzewo robocze miało WIP równoległej sesji): testy SVG **25/25** (+6 nowych: defs+use zachowane E2E w data-URI, use zewnętrzny out, BOM in, DOCTYPE in, XXE null), Infrastructure **325/328** (3 faile = WIP hMerge/tab-in-cell równoległej sesji). Jeżeli logo u użytkownika NADAL puste: w logach szukać „SVG part pominięty…"/„Media part bez rastra web…", w DOM `data-legacy-graphic="blank"` → wtedy to metafile EMF+ poza etapem 1 tłumacza (ADR-0027 etap 2). Ograniczenie bez zmian: writer dropuje SVG przy eksporcie HTML→DOCX (utrata na 1. autosave). Wcześniej:

2026-07-07 — **Tabele Doc2/Qutalo follow-up: legacy `w:hMerge` + taby w komórkach (2 wady z porównania edytor↔Word)**. (1) Reader obsługiwał scalanie poziome tylko przez `w:gridSpan` — komórki scalone legacy `w:hMerge` (restart+continue) renderowały się jako osobne `<td>` (treść w pierwszej wąskiej kolumnie + puste widma; wiersz „Umowa wieloproduktowa…" znowu rozbity mimo fixu „deficytu" z 2026-07-06, bo ten dokument używa hMerge, nie krótkiego wiersza). Nowy `BuildRowRenderPlan` w `ConvertTableToHtml`: restart pochłania gridSpan komórek continue jako colspan, continue nie emitują `<td>` (jak kontynuacje vMerge), pominięty `w:val`=continue (ECMA-376), sierota-continue renderowana normalnie; writer emituje gridSpan z colspan (równoważne dla Worda). (2) Rendering POZYCYJNY tabów (rozszerzony 2026-07-06 na body) obejmował też akapity w komórkach tabel: segment `position:absolute;left:{stop}px` kotwiczony do akapitu przy stopach w geometrii strony wyjeżdżał poza wąską komórkę i malował się po sąsiedniej (nałożone nagłówki „Waluta"/„Termin spłaty kredytu") oraz wyłączał `text-align` komórki (rozjechane centrowanie nagłówków/wartości). `usePositionedTabs` wyklucza akapity z przodkiem `TableCell` — w komórce tab inline/flex jak przed regresją, `data-tab-stops` round-trip bez zmian. Weryfikacja: `Doc2ImportFidelityTests` **24/24** (+4), Infrastructure **321/322** — jedyny fail `SanitizeSvg_KeepsSafeDataHrefAndFragment` to była RÓWNOLEGŁA sesja SVG w tym samym drzewie (produkcja już zachowywała `<use>`, test jeszcze oczekiwał usunięcia; domknięte wpisem „SVG puste białe logo" z 2026-07-07 — test zaktualizowany, komplet zielony), build 0 błędów. Ograniczenie bez zmian: warunkowe formatowanie tekstu ze stylu tabeli (ADR-0026). Wcześniej:

2026-07-06 — **Audyt import/edycja/eksport DOCX (ADR-0028): 5 konkretnych wad edytora/readera + narzędzia diagnostyczne + moduł jednostek (+20 testów)**. (item 8) Podział strony przestał być grafiką — `insertPageBreak` wstawia semantyczny niedrukowalny `div.page-break contenteditable=false` (bez inline-border/etykiety); SCSS domyślnie zero wysokości, podpowiedź tylko w trybie `show-formatting-marks` (sygnał `showFormattingMarks`); eksport bez zmian (writer już mapuje `div.page-break`→`w:br type=page`, NIGDY drawing/picture — potwierdzone). (item 6) Pole fontu nie białło już — natywny `<select>` (zerujący `selectedIndex=-1` na mousedown) → combobox `<input list>`+`<datalist>`: efektywny font zawsze widoczny, wpisywanie+wyszukiwanie, commit dopiero Enter/change/blur, Escape przywraca, stan MIESZANY (puste) dla wielu fontów (`EditorState.fontMixed` + `computeFontMixed`); usunięte 3 debug `console.log`. (item 7) `FontProviderService` (root) = jedno źródło listy fontów dla obu toolbarów, z fontem FIRMOWYM z `--corporate-font-family` + wspólna normalizacja (koniec dwóch list `editor-toolbar.fontFamilies` vs `document-editor.commonFonts`). (item 1) `core/utils/units.util.ts` = jedyne źródło `CSS_PX_PER_CM=37.8`; linijka używała 37.795 a strona 37.8 → dryf usunięty; zoom potwierdzony jako czysto wizualny `transform:scale` (nie zmienia modelu ani eksportu). (item 3 / KR-05 / KR-08) Reader zachowuje wartość cache pól złożonych ≠ PAGE/NUMPAGES i DATE (simple+complex) zachowuje datę dokumentu zamiast `DateTime.Now`. Narzędzia: `tools/docx-diagnostics/` (zero-dependency Node ESM) — `inspect-docx` i `compare-docx` (własny czytnik ZIP na `zlib`; JSON+konsola; exit codes; klasyfikacja diffów), self-test 8/8. Weryfikacja: GUI nowe specy page-break 2 / editor-toolbar.font 6 / font-provider 4 / units 5, pełne GUI **334/335** (jedyny fail pre-existing `spec-layout-shell`), `ng build` OK; backend `FieldValuePreservationTests` 3, Infrastructure **318/318**, `dotnet build` solucji 0 błędów. Ograniczenia: round-trip KODU pola (TOC/REF) poza zakresem (wartość jako tekst = „unlink field"); „mieszany" font w jsdom = false; narzędzia regexowe. Items 4/5/2 były zrobione wcześniej (ADR-0023/0025/0026); item 9 (kształty/kotwice) pozostaje jako KR-06 w audycie. Wcześniej:

2026-07-06 — **Wierność importu Word (dokument „Doc2"/wyciąg Qutalo): 4 potwierdzone wady odwzorowania naprawione u źródła + 16 testów regresyjnych**. (1) **Tab-stopy w TREŚCI** — body renderował taby jako równomierne rozpychacze `flex:1 1 0` (albo sztywny spacer 2em dla samego lewego tabu), ignorując realne pozycje `w:tabs`; renderowanie POZYCYJNE (`span.docx-tab-seg` na pozycji stopu: lewy = start segmentu NA pozycji, prawy = koniec przez `translateX(-100%)`, środek `-50%`) rozszerzone z nagłówka/stopki na **body** (`DocxToHtmlConverter.ConvertParagraphToHtml`: `usePositionedTabs` bez warunku Header/Footer; writer odtwarza `w:tabs`+`w:tab` z `data-tab-stops`/`docx-tab-seg` bez zmian). (2) **Scalone kolumny — wiersze nieregularne** — wiersz, którego suma `gridSpan` < liczba kolumn siatki (np. pojedyncza komórka „Umowa wieloproduktowa…" w tabeli 6-kolumnowej), przy `table-layout:fixed`+colgroup lądował w 1. wąskiej kolumnie, reszta jako puste kolumny-widma; ostatnia komórka wiersza pochłania teraz deficyt kolumn (`colspan`) — `ConvertTableToHtml` liczy `deficit = GridColumnCount − Σ gridSpan`, `AppendTableCellHtml(..., extraColspan)`. Jawny `gridSpan`/`vMerge`/pełne wiersze bez zmian. (3) **Pionowe wyśrodkowanie komórek** — reader emitował `vertical-align:top;` bezwarunkowo, a rzeczywistą wartość dopisywał drugi raz (zduplikowana deklaracja inline); teraz jedna rozwiązana wartość (`top`/`middle`/`bottom`). GUI: `.editor-content table td>p/th>p { margin:0 }` — blankietowy `p { margin:0 0 10px }` psuł wizualne centrowanie i pompował gęste tabele (inline marginesy z backendu nadal wygrywają). (4) **SVG jako data-URI** — reader normalizuje jednoznacznie błędny MIME `img/svg+xml`→`image/svg+xml` (bez tego przeglądarka nie renderuje), **sanityzuje** treść SVG przez istniejący `IGraphicConversionService.SanitizeSvg` (script/foreignObject/on*/zewn. refs usuwane) z limitem 2 MB; SVG niepoprawny/za duży = odrzucony (nie osadzany surowo). Interlinia/odstępy PRZED/PO/`<br/>` już mapowane poprawnie (potwierdzone testem). (5) **Pola tekstowe (KR-06 minimum)** — kształt/textbox bez blipa był cicho dropowany (treść tracona na 1. autosave); `RenderTextBoxContent` renderuje `w:txbxContent` (nowoczesne `wps:txbx` i legacy VML `v:textbox`) jako `div.docx-textbox` — wpięte w `ConvertDrawingToHtml`/`ConvertPictureToHtml`/`ConvertAlternateContentToHtml` (Choice/Fallback = ta sama treść → raz). Weryfikacja: Infrastructure **303/303** (+18 `Doc2ImportFidelityTests`, golden `tab-stops-lcr` zregenerowany na pozycyjny), Application **306/306**, solucja build 0 błędów, GUI **317/318** (jedyny fail pre-existing `spec-layout-shell`), `ng build` OK. Ograniczenia/uwagi: writer nadal DROPUJE obraz SVG na eksporcie HTML→DOCX (goły `a:blip` psuje Word — utrata na 1. autosave, do rozwiązania rasteryzacją/`asvg:svgBlip`); „Formatny" (Issue 4) nie znaleziono w kodzie/`.ai`/diagramach — brak jednoznacznej interpretacji, danych nie usuwamy (patrz raport). Wcześniej:

2026-07-05 — **Własny tłumacz wektorowy EMF/WMF → SVG (etap 1, ADR-0027)**. Czysto wektorowe metafile są wreszcie WIDOCZNE w edytorze: nowy pure-managed `MetafileVectorTranslator` (parser rekordów MS-EMF/MS-WMF) wpięty jako strategia `vector-translate` w łańcuch `ConvertMetafile` (`embedded-raster` → `dib-rasterize` → `vector-translate` → blank). Podzbiór etapu 1: pióra/pędzle (stock objects, dash, COLORREF), MoveTo/LineTo, Rectangle/Ellipse/RoundRect, Poly*/PolyPolygon 16/32-bit (fill-rule), ścieżki Begin/Close/Fill/Stroke, SetWorldTransform/Modify (rotacja→polygon), StretchDIBits→`<image>`; WMF ze slotową tabelą obiektów (fonty zajmują sloty) i odwróconą kolejnością parametrów. SVG = WYŁĄCZNIE podgląd (Lossy, przez SanitizeSvg) — do DOCX zawsze wraca oryginalny metafile (data-original-src/pass-through); metafile bez obsługiwanych rekordów → blank jak dotąd (zero regresji). Limity anty-DoS: 200k rekordów/100k pkt/20k elementów/2 MB; wyjątek → blank. Poza etapem 1: tekst, clipping, ROP, EMF+ (dual ma fallback EMF). Weryfikacja: `MetafileVectorTranslationTests` **10/10**, Infrastructure **285/285**, Application **306/306**, build 0 błędów. Wcześniej:

2026-07-05 — **Import obrazów z DOCX: 6 potwierdzonych bugów naprawionych w ISTNIEJĄCYM mechanizmie (+10 testów regresyjnych, zero nowych zależności)**. Przyczyny źródłowe znalezione w kodzie: (1) `_images` kluczowany samym rId — rId-y są unikalne tylko per część pakietu (main/header/footer każdy od rId1), więc obraz nagłówka o kolidującym rId renderował obraz z body → klucz `{part.Uri}|{rId}` + rozwiązywanie relacji względem `sourcePart ?? MainDocumentPart`; (2) `mc:AlternateContent` w run children dropowany w CAŁOŚCI (default → "") — obrazy zakotwiczone/grupy znikały bez śladu → `ConvertAlternateContentToHtml` (Choice-e w kolejności, potem Fallback VML); (3) EMZ/WMZ (metafile GZIP, `image/x-emz`) nierozpoznawane → broken image → `GraphicConversionService` dekompresuje GZIP przed detekcją (bounded, anty-bomb) + `WebGraphicForLegacy` obejmuje `Unknown` (nigdy nierenderowalny `data:{ct}` w src); (4) zerowy `wp:extent` → `width:0px` (obraz w DOM, niewidoczny) → wymiary intrinsic z nagłówka pliku; (5) writer fałszował content type partu (wszystko spoza listy = „Jpeg" — TIFF/ICO/WEBP psuły się po 1. autosave) → mapowania Tiff/Icon + `AddImagePart(contentType)` dla nieznanych `image/*`; (6) `r:link`-only i błędy relacji — kontrolowane pominięcie z logiem (`ILogger` opcjonalny w readerze: część/relId/typ/rozmiar/status/powód, bez binarek) zamiast `catch {}`. Weryfikacja: nowe `ImageImportRegressionTests` **10/10**, Infrastructure **275/275**, Application **306/306**, build solucji 0 błędów, golden bez zmian. Ograniczenia bez zmian: czysty wektor EMF/WMF → blank+pass-through, `r:link` niepobierane (SSRF), textboxy/wykresy nadal nierenderowane, grupa = pierwszy blip. Linux: pure-managed (GZipStream BCL), do potwierdzenia w CI. Wcześniej:

2026-07-05 — **Pokrycie testami zmian ADR-0025/0026 (+16 testów, zero zmian w kodzie)**. Application.UnitTests **306/306** (+4: Save/DownloadEdited — `SectionHeadersFooters`+`PageSize` przekazywane do konwertera nietknięte, obie ścieżki Convert/ConvertPreservingPackage, null-default), Infrastructure.UnitTests **265/265** (+12: TabStop — leader/`clear` ze stylu/direct-override/bar-tab; TableStyle — lastRow/firstColumn/legacy maska hex tblLook/noHBand off/pasy kolumnowe band1Vert; MultiSection writer — wpis footer-only, indeks poza zakresem ignorowany bez wyjątku, indeks 0 ignorowany). Wcześniej:

2026-07-05 — **Audyt zgodności z MS Word (tylko docs)**. Nowy `.ai/AUDIT_WORD_COMPATIBILITY.md` — pełna lista ~60 problemów wierności (potwierdzone w kodzie vs potencjalne) z priorytetyzacją i 6-etapowym planem napraw. Ustalenie nadrzędne: reader cicho dropuje nieobsługiwane elementy (w:ins/w:del, pola ≠ PAGE/NUMPAGES/DATE — w tym cały TOC, przypisy, komentarze, zakładki, txbx/oMath/wykresy, vanish, keepNext…), a autosave i „Zakończ i wyślij" idą przez pełną regenerację `Convert` (pass-through tylko w Download) → trwała utrata w v2 przy pierwszym autosave; Sign dodatkowo bez `SectionHeadersFooters`. Zero zmian w kodzie. Wcześniej:

2026-07-05 — **Tabele: rozwiązywanie STYLU tabeli + wierność Word (ADR-0026)**. Przyczyna źródłowa rozjazdu „tabela w edytorze ≠ tabela w Wordzie": reader ignorował `w:tblStyle` — obramowania/cieniowanie większości tabel Worda żyją w stylu (styles.xml), nie w tblPr; tabele „Tabela – Siatka" renderowały się BEZ linii, a `data-no-borders` utrwalał stratę na eksporcie. Reader: `ResolveTableStyleContext` (łańcuch basedOn per-side, tblLook, tblStylePr: firstRow/lastRow/kolumny/pasy — banding pomija nagłówek jak Word), pozycyjne krawędzie (outer vs insideH/V wg pozycji w siatce — wcześniej zewnętrzne szły na WSZYSTKIE krawędzie), `themeFill`/`themeColor`+tint/shade przez motyw, wzory `pctNN` blendowane, grubość `sz/6` px (koniec pogrubiania +33%/zapis), `height:` na tr (min-height nie działa) + `data-row-height-tw`/`data-row-hrule`, `data-tbl-header`/`data-cant-split`, `tblCellSpacing`→border-spacing, szerokości % z ułamkiem, cell width auto/nil bez `width:0px`. Writer: `w:tblStyle`+`w:tblLook` z data-*, trHeight z dokładnych twips+hRule, `w:tblHeader`/`w:cantSplit`/`w:tblCellSpacing`/`w:tblInd`, px×6, dziesiętne %; fix duplikacji wierszy tabel ZAGNIEŻDŻONYCH (`.//tr`→`./tr|./thead|tbody|tfoot`). GUI: koniec zapiekania mierzonych wysokości wszystkich wierszy (R-18 zamknięte — eksport tylko jawnych inline), ręczny resize wiersza czyści data-*, `syncTableColgroup` po resize/wstawieniu/usunięciu kolumny (kolumny użytkownika trafiają wreszcie do w:tblGrid). Weryfikacja: nowe `TableStyleFidelityTests` **21/21**, Infrastructure **253/253**, cała solucja 0 fail; GUI `table-grid.util.spec` 5 + pełne **317/318** (fail pre-existing `spec-layout-shell`), `ng build` OK. Golden `simple-table`/`merged-cells-table` zregenerowane (nowe grubości/atrybuty). Ograniczenia: bez warunkowego formatowania TEKSTU ze stylu, regionów narożnych, przekątnych tl2br/tr2bl, fitText/hideMark; tblHeader/cantSplit tylko round-trip (edytor nie powtarza nagłówka przy paginacji). Wcześniej:

2026-07-05 — **Nagłówki/stopki: dynamiczne pasmo + tab-stopy pozycyjne + nagłówki/stopki PER SEKCJA (ADR-0025)**. Trzy kroki wierności z FIDELITY_REPORT: (1) **Dynamiczne pasmo** — `.page-header/.page-footer` zaczynają się `headerDistance`/`footerDistance` od krawędzi strony (`margin-top/bottom`), `min-height` = pasmo (margines − dystans), treść wyższa SPYCHA body jak Word; `PageGeometry` +`headerDistanceCm/footerDistanceCm` (marker sekcji niesie `data-header/footer-distance-cm`); `_repaginateNow` mierzy realne pasma z DOM (`_measureBandHeightsPx`) i odejmuje od dostępnej wysokości. (2) **Tab-stopy per akapit** — reader czyta efektywne `w:tabs` (styl+direct, `clear`) → `data-tab-stops="pos:align[:leader]"`; nagłówek/stopka renderuje segmenty POZYCYJNIE (`span.docx-tab-seg`, center/right przez translateX na pozycji stopu; flex zostaje dla body); writer odtwarza `w:tabs` z atrybutu, `w:tab` z segmentów, a literalny `\t` w tekście emituje jako element `w:tab` (nie `w:t`). (3) **Nagłówki/stopki per sekcja** — `SectionHeaderFooter` w `DocumentContent.SectionHeadersFooters`/`SaveDocumentRequest` (Domain+TS, pole opcjonalne); reader: wpis per sekcja ≥ 1 z WŁASNYMI refs; writer: `Convert/ConvertPreservingPackage` +param, wpis → sectPr swojej sekcji (`_emittedSectionProps`); handlery Save/DownloadEdited; GUI: `pageSectionIndexes`, wybór wpisu per strona z dziedziczeniem, edycja pasma na KLIKNIĘTEJ stronie (`editingHfPageIndex`) z routingiem do właściciela (`_applyEditedHeader/FooterHtml`), output `sectionHeadersFootersChange` → save. Weryfikacja: Infrastructure **232/232** (nowe `TabStopFidelityTests` 7, MultiSection +4), cała solucja 0 fail; GUI **312/313** (+9 nowych; fail pre-existing `spec-layout-shell`), `ng build` OK. Ograniczenia: Sign bez sekcyjnych nagłówków; k-ty tab→k-ty stop; leader nierysowany w edytorze. Wcześniej:

2026-07-05 — **Edytor: fix paginacji — ENTER przy dolnej krawędzi tworzy nową stronę zamiast rozciągać kartkę**. Root cause w pomiarze `_repaginateNow` (`wysiwyg-editor.ts`): bloki mierzone w gołym `<div>` poza kontekstem stylów `.editor-content` (pusty `<p>` = 0 px, bo `p:empty::before {content:'\00a0'}` nie działało; brak scope'owanych marginesów) + `getBoundingClientRect().height` nie zawiera marginesów bloków → suma zaniżona → paginacja nie widziała przepełnienia → `.page` (min-height, overflow:visible) rosła w pion. Fix: `_createBlockMeasurer` (measurer z klasą `editor-content` — style globalne przy `ViewEncapsulation.None`) + `_measureBlockRunHeights` (wysokość konsumowana deltami pozycji z sentinelem: marginesy + realny kolaps, margin-top 1. bloku doliczany; nadal 1 layout-flush na przebieg) + **max-wait 600 ms** w `_schedulePaginate` (przytrzymany Enter resetował czysty debounce 250 ms w nieskończoność). Kursor/nowa strona nie wymagały zmian (kotwica `{block,offset}` + `pageEditorRefs.changes` już działały). Weryfikacja: nowy `wysiwyg-editor.pagination-overflow.spec` **9/9**, wysiwyg 69/69, pełne GUI **312/313** (fail tylko pre-existing `spec-layout-shell`), `ng build` OK; backend nietknięty. Wcześniej:

2026-07-05 — **Listy wielopoziomowe: logiczna tożsamość + liczniki Worda + round-trip formatów**. Przyczyny źródłowe: (1) HTML nie niósł tożsamości listy (numId/ilvl/format przepadały) → writer przy KAŻDYM autosave tworzył nową definicję z hardkodowaną drabinką formatów (decimal/lowerLetter/lowerRoman…), niszcząc oryginalną numerację (upperRoman, "%1)", start=5); (2) reader sklejał niezależne listy o identycznym wyglądzie (warunek „ten sam format ⇒ ta sama lista") i restartował od 1 listę przerwaną akapitem (`start` brał z definicji `w:start`, nie z faktycznej pozycji); (3) po zapisie fragmenty listy dostawały osobne numId → Word tracił kontynuację na stałe. Fix: reader — liczniki per **(abstractNumId, ilvl)** z semantyką Worda (wspólny abstrakt kontynuuje, `startOverride` restartuje przy 1. użyciu instancji, poziomy głębsze restartują po powrocie na płytszy, `lvlRestart=0` honorowane, `numStyleLink` rozwiązywany), grupowanie po numId, `<ol start>` z liczników, kontener niesie `data-num-id/-abstract-num-id/-ilvl/-num-fmt/-start/-lvl-text/-bullet-font`; writer — fragmenty z tym samym `data-num-id` współdzielą jedną `NumberingInstance` (kontynuacja), `CreateAbstractNumbering` odtwarza dokładny `numFmt/lvlText/start/font` z data-*. Weryfikacja: nowy `ListNumberingFidelityTests` **12/12** (w tym pełny round-trip DOCX→HTML→DOCX→HTML); Infrastructure 225/226 (jedyny fail = snapshot `tab-stops-lcr` z równoległej sesji, do regeneracji). Ograniczenia: sufiks lvlText i numeracja multilevel "1.1.2" nierenderowane wizualnie w edytorze (wracają do DOCX bez strat); nowe listy z edytora bez data-* → domyślna drabinka. Wcześniej:

2026-07-05 — **Audyt dokumentacji `.ai` vs kod (tylko docs, zero zmian w kodzie)**. Przejrzane wszystkie 34 pliki `.ai`, fakty zweryfikowane w repo. Najważniejsze poprawki: TECH_STACK (Identity.Web 3.12.0 zamiast pinowanego JwtBearer, SkiaSharp 3.119.2, pełna lista projektów testowych, `D2ExampleExternalApp`), API_CONTRACTS (external `GET /status` = okrojone `{masterId,status}`, `Classification` opcjonalna, +`generate-image`, +`GET /identity/resources`, klasowy `RequireAppOperator`), DATABASE (SQL 008–011, statusy `Queued`/`SendAborted`/`Cancelled`, `last_modified_by`/`corporate_key`), SECURITY (claim `corpKey` case-insensitive — nie `ck`; guardy bez `appAdminGuard`; SSRF `ReturnUrlValidator` jako stan, nie rekomendacja; wysyłka multipart), FEATURES/PRODUCT_GOALS/RISKS (**Krok 2 = Implemented — podgląd ładuje v1 przez `downloadBaseVersion`, R-02 zamknięte**; „Zakończ" = 200 sync, nie 202), DOMAIN/GLOSSARY (pełne enumy, metody inline/cancel, BR-006 opcjonalna klasyfikacja), DOCX_CONVERSION (sekcja 2 + roadmapa R-10 pod ADR-0023), OBSERVABILITY (pola ECS/GCP, obiekt `service{}`, oba hosty), DECISIONS (duplikat ADR-0020 → **ADR-0024** centralne polityki security), RISKS (duplikat R-25 → R-29, A-05 Closed, A-03 001..011, A-06 multipart), TASK_HANDOFF (sekcje statyczne odświeżone), erraty w `confluence-*`/diagramach (auth istnieje, multipart potwierdzony), INDEX (+4 pliki). Wcześniej:

2026-07-05 — **Edytor: rendering stron per sekcja (domknięcie ADR-0023) + realny PageSize + szybsza repaginacja**. `wysiwyg-editor` dostał input `pageSize` i geometrię per strona (`pageGeometries`): sekcja 1 z inputów, kolejne z `data-*` markera `docx-section-break` (marker otwierający stronę zmienia geometrię od tej strony, continuous od następnej). Szablon binduje per strona wymiary/orientację/paddingi/prowadnice; `_repaginateNow` łamie strony wg wysokości i szerokości BIEŻĄCEJ sekcji (koniec hardkodu A4 1122/816 px — dokumenty A5/Letter i poziome aneksy łamią się poprawnie) + pomiar wsadowy bloków (1 layout-flush na przebieg zamiast per blok). Guard: `_flattenTopBlocks` nie wchodzi w markery (strona z samym markerem gubiła go). Weryfikacja: specy wysiwyg **51/51** (+6 geometrii), pełne GUI **294/295** (fail tylko pre-existing `spec-layout-shell`), `ng build` OK; backend nietknięty, solucja **730/730**. Ograniczenie: nagłówki/stopki nadal wspólne dla wszystkich sekcji (model 1-kompletowy). Wcześniej:

2026-07-05 — **DOCX↔HTML: wiele sekcji (R-10 read+write) + tabele + interlinia atLeast (ADR-0023)**. Przyczyny źródłowe znalezione w kodzie: (1) reader brał `Body.Elements<SectionProperties>().FirstOrDefault()` = sectPr **OSTATNIEJ** sekcji → dokument pionowy z poziomym aneksem otwierał się cały poziomo i z nagłówkami aneksu; (2) paragraph-level `pPr/sectPr` ignorowane → autosave **spłaszczał dokument wielosekcyjny do jednej sekcji**; (3) writer tabel: pusty `tblGrid` z liczby komórek (szerokości kolumn ginęły przy każdym zapisie), zawsze `TableLayout Autofit`, brak komórek kontynuacji `vMerge` pod rowspan (komórki przesuwały się w lewo — tabela uszkodzona w Wordzie); (4) interlinia `atLeast` wracała jako `exact` (przycinanie tekstu). Fix: `GetSectionPropertiesInDocumentOrder` — `PageSize`/`Margins`/nagłówek/stopka z PIERWSZEJ sekcji; przerwy sekcji → `div.page-break` + niewidoczny `div.docx-section-break` z geometrią następnej sekcji w `data-*`; writer odtwarza z markerów paragraph-level sectPr (body-level = ostatnia sekcja; refs nagłówka/stopki + titlePg na PIERWSZYM sectPr — dziedziczenie; page-break przed markerem nie staje się w:br), tabele: `colgroup`→`tblGrid` (px→twips), `table-layout:fixed`→`Fixed`, kontynuacje `vMerge` wg kolumny gridu; interlinia: marker `--w-line-rule:atLeast`. GUI: scss ukrywa marker; bez zmian kontraktów API. Weryfikacja: Infrastructure **210/210** (nowe `MultiSectionFidelityTests` 12, `TableWriteFidelityTests` 6, LineSpacing +3; snapshoty Golden bez zmian), cała solucja 0 fail, GUI **288/289** (+1 test markera; fail tylko pre-existing `spec-layout-shell`), `ng build` OK. Ograniczenie: edytor renderuje wszystkie strony w geometrii 1. sekcji (per-page rendering = przyszły krok w `wysiwyg-editor`). Wcześniej:

2026-07-03 — **RBAC obszarów: Operator vs Administrator rozłączne**. Macierz dostępu do obszarów UI: brak roli → nic (nawet dashboard), Operator → dashboard/editor/viewer (NIE admin), Administrator → tylko admin (NIE dashboard/editor/viewer), dwie role → suma. `ResourcesProvider.GetForUser`: Operator→`[dashboard,editor,viewer]`, Admin→`[admin]` (nowy zasób **`dashboard`**; `DocumentResources`→`OperatorResources`). Front: nowy **`homeRedirectGuard`** na root `''` — Operator→dashboard, Administrator→`/admin`, brak roli→`/brak-uprawnien`; `resourceGuard` czyta `data.resource` (uogólniony), `pdf-maintenance`→zasób `viewer`. Polityk endpointów API **świadomie nie ruszano** (`RequireAppOperator` zostaje Operator|Admin z ADR-0022): moduł admina czyta „operatorskie" endpointy (`getDocumentVersions`/`download`), więc dostęp do OBSZARÓW rządzi `ResourcesProvider`, nie polityki. Weryfikacja: `Api.UnitTests` IdentityController 8/8, Integration EditorAuthorization 9/9, GUI `home-redirect.guard` 4 + `resource.guard` 3, dashboard 3/3, tsc czysto. Wcześniej:

2026-07-03 — **SECURITY: rola `Operator` egzekwowana na backendzie edytora (broken access control)**. Znaleziono i naprawiono realną podatność: `DocumentController` i `DocumentStorageController` dziedziczyły tylko `[Authorize]` (dowolny zalogowany), a `RequireAppOperator` była nałożona jedynie na `GET /identity/resources` — więc użytkownik **bez roli aplikacyjnej** mógł przez bezpośrednie API tworzyć/zapisywać/nadpisywać/przywracać/podpisywać i „Zakończyć i wysłać" dokumenty. Fix: klasowy `[Authorize(Policy = RequireAppOperator)]` na obu kontrolerach (endpointy admina zachowują `RequireAppAdmin`, AND → Administrator). Front (defense-in-depth): pulpit bramkuje „Nowy/Otwórz" sygnałem `canUseEditor` (zasób `editor`) + `ensureEditorAccess()`; `resourceGuard` rozróżnia transient (401/0/5xx → retry ×3) od `403` (deny). Powód objawu „user bez roli widział edytor": pulpit (`''`) ma tylko `authGuard` (bez `resourceGuard`) i pokazywał akcje, a backend ich nie blokował. Weryfikacja: nowy `D2ViewerEditor.Api.IntegrationTests` **9/9** (401 bez tokenu, 403 bez roli, 200 Operator/Administrator, 403 Operator na admin); GUI `resource.guard.spec` 3 + `dashboard.spec` 3 + `resource-access.service.spec` 3. Decyzja: **ADR-0022**. Do potwierdzenia w Entra: przypisania App Roles + „Assignment required". Wcześniej:

2026-06-28 — **Fix: okno podwójnej wysyłki w „Zakończ"**. `FinishAndSendDocumentCommandHandler` nie commituje już nowego zadania jako `Pending`/due przed próbą inline — `CreateQueuedDeliveryAsync` tylko `AddAsync` (bez SaveChanges), a pierwszy commit (w `AttemptInlineDeliveryAsync` po `BeginInlineAttempt`) wstawia rekord od razu jako `Sending`. Rekord nigdy nie jest w bazie claimowalnym `Pending`, więc worker tła nie przejmie go równolegle z inline → koniec okna na 2× `SendAsync`. Inline-`Sending` ma `locked_until=NULL` (worker nie reclaimuje w trakcie wysyłki). Test: `Handle_NewDelivery_IsPersistedAsSending_NeverClaimablePending`. Weryfikacja: `Application.UnitTests` 302/302. Uwaga: powtarzalny dubel u usera prawdopodobnie we wdrożonym senderze OGate/Keycloak (poza repo) + at-least-once chroniony `Idempotency-Key`. Wcześniej:

2026-06-28 — **`GcpJsonConsoleFormatter` — bogate logi observability (GCP + ECS) do diagnozy 500 z samego logu**. Formatter rozbity na małe funkcje; dodane pola GCP (`logging.googleapis.com/trace|spanId|trace_sampled|sourceLocation`, `httpRequest`, `labels`) i ECS (`@timestamp`, `log.*`, `service.*`, `trace.*`/`span.*`, `transaction.*`, `event.*` z `event.reason`=klasyfikacja db/dependency/validation/authorization/code, `error.{type,message,stack_trace,inner}`, `http.*`, `url.*`, `user.id`). Korelacja z `HttpContext.Items`/header `X-Correlation-ID`/scope/Activity baggage → `correlation_id`+`labels.correlation_id`. Maskowanie pól wrażliwych ("[REDACTED]", case/separator-insensitive) + maskowanie query. `IHttpContextAccessor` opcjonalny (działa też w workerze bez HTTP). `StructuredLogFormatterOptions` rozbudowane (ProjectId/ServiceVersion/Include*/RedactedPropertyNames); `LoggingExtensions` czyta ProjectId z `GOOGLE_CLOUD_PROJECT`, instance id z `K_REVISION`/`HOSTNAME`. **Migracja kontraktu:** ECS ON domyślnie → `service`/`environment` w obiekcie `service{}` (zmigrowany 1 test). Weryfikacja: `Api.UnitTests` 129/129 (formatter 21). Wcześniej:

2026-06-27 — **Wysyłka do aplikacji zewnętrznej: „w toku" przestaje być błędem + zamykanie okna + anulowanie w trakcie**. (1) Front `document-editor.finishDocument()` rozróżnia trzy stany: `delivered`→koniec, `status==='Sending'`→**okno informacyjne „Trwa wysyłka pliku do aplikacji zewnętrznej" (NIE błąd)**, realny błąd→modal problemu. (2) Modal wysyłki dostał przyciski **„Przerwij wysyłkę"** (`cancelSend()` — anuluje request w locie przez unsubscribe + `abortSend`, stan „Cancelled", neutralny komunikat) i **„Zamknij"** (`closeSendingModal()` — wysyłka leci dalej w tle, UI odpięte guardem `sendDetachedFromUi`, spóźniony błąd nie wyskakuje). (3) Backend: nowa `DocumentDelivery.CancelByUser()` (obejmuje też próbę INLINE w toku = Sending bez lease; worker-Sending z lease i stany końcowe rzucają) — używa jej `AbortSendCommandHandler`. (4) `FinishAndSendDocumentCommandHandler` traktuje `OperationCanceledException` jako **anulowanie** (propaguje, nie `Result.Failure`, nie `DeliveryFailed`). Bez i18n (projekt go nie ma). Weryfikacja: Domain `DocumentDelivery` 27/27, Application `FinishAndSend`+`AbortSend` 14/14, GUI `document-editor.spec` **71/71** (+3). Wcześniej:

2026-06-26 — **FIX: `CorporateKey` zawsze null mimo poprawnego tokenu — dopasowanie claimu case-insensitive**. Diagnoza z debuggera na realnym tokenie Qutalo-AD: użytkownik **uwierzytelniony** (`IsAuthenticated=true`, 30 claimów, `IsAdmin=true`) — to NIE było 401, tylko handler zwracał `Failure`→**400**, bo `_currentUser.CorporateKey==null`. Przyczyna: token niesie claim **`corpkey`** (małe litery), a kod szukał **`corpKey`**; `Identity` to **`CaseSensitiveClaimsIdentity`** (Microsoft.Identity.Web), więc `ClaimsPrincipal.FindFirst("corpKey")` NIE dopasowuje `"corpkey"`. Poprawka w [ClaimsCurrentUserProvider]: zamiast `FindFirst(...)` szukamy po `Claims` z `StringComparison.OrdinalIgnoreCase` — odporne na `corpkey`/`corpKey`/`CorpKey`, bez zmian w pipeline auth i bez zmian konfiguracji per-środowisko. Testy: nowy `Api.UnitTests/Security/ClaimsCurrentUserProviderTests` (8, w tym jawny case-insensitive `corpkey`→found) + „brak corpKey → Failure bez GCS/repo" w `Save`/`Update`. Weryfikacja: `Api.UnitTests` Security 8/8, `Application.UnitTests` (handlery) 109/109. Wcześniej:

2026-06-26 — **corpKey ustalany wyłącznie po stronie API (token), bez transportu z GUI**. Komendy `SaveDocumentVersion`/`UpdateDocumentVersion`/`FinishAndSendDocument` straciły parametr `CorporateKey`; handlery czytają `_currentUser.CorporateKey` (claim z tokenu) i zwracają `Result.Failure`, gdy brak. Controller DTO `SaveDocumentVersionRequest` bez pola `CorporateKey`. GUI `document-editor.ts` nie odczytuje już claimu `corpKey` z `idTokenClaims` i nie wysyła `corporateKey` (zapisy = `{ content }`). Powód: snapshot `getActiveAccount().idTokenClaims` bywał null → `SaveDocumentVersionCommand.CorporateKey` null; token wysyłany do API i tak niesie `corpKey`, więc `_currentUser` jest niezawodnym źródłem. Wyświetlanie `corporateKey` na listach admina (z `DocumentDelivery`) bez zmian. Build backend 0 błędów; `Api.UnitTests` **113/113**, `Application.UnitTests` **295/295**; GUI build OK. Decyzja: **ADR-0021**. Wcześniej:

2026-06-24 — **corpKey z Entra ID przy zapisach z edytora + kolumna „Kto modyfikował" w administracji**. GUI (`document-editor.ts`) odczytuje claim `corpKey` z `idTokenClaims` aktywnego konta MSAL (sygnał `corporateKey`) i przekazuje go w body przy `saveDocumentVersion`/`updateDocumentVersion` (auto-save + nowy dokument) oraz `finishAndSend`; `SaveDocumentVersionRequest` (GUI) +`corporateKey?`. API: controller DTO i komendy `SaveDocumentVersion`/`UpdateDocumentVersion`/`FinishAndSendDocument` +`CorporateKey` (opcjonalne, fallback do claimu `_currentUser.CorporateKey`). Domena: `Document.LastModifiedBy` (+`SetLastModifiedBy`, aktualizuje tylko jedną kolumnę jak `MarkEditing`), kolumna `documents.last_modified_by` (migracja **`infra/sql/011`**), mapowanie EF w `DocumentConfiguration`. Listy admina: `DocumentListItemDto.LastModifiedBy` (z `Document`), `DeliveryListItemDto.CorporateKey` (reużycie istniejącego `DocumentDelivery.CorporateKey`); nowa kolumna „Kto modyfikował" + filtr w `admin-files` (`lastModifiedBy`) i `admin-deliveries` (`corporateKey`). Build backend 0 błędów; `D2Api.Api.UnitTests` **113/113**, `Application.UnitTests` **295/295**; GUI build OK; `ng test` **268/269** (jedyny fail `spec-layout-shell.spec` — pre-existing, niezwiązany). TODO deploy: zastosować SQL `011` na środowiskach. Wcześniej:

2026-06-22 — **Grafiki: usunięcie widocznego placeholdera (EMF/WMF/TIFF) + cache + nowe formaty**. `GraphicConversionService` przepisany na czytelny łańcuch strategii (`Execute`→per-format) z raportowaniem strukturalnym (`AttemptedStrategies`/`FailureReason`/`CacheKey`/`SourcePath`). **Koniec szarego placeholdera „… — podgląd w Word"**: metafile bez osadzonego rastra → **przezroczysty pusty SVG** (`IsBlankFallback`, wymiary z layoutu), oryginał pass-through do DOCX (Word renderuje wektor). Dodano deduplikację po SHA-256 (bounded cache), detekcję/obsługę **TIFF**(→PNG/Skia)**, WEBP/ICO** (web-native), raster-rescue Skia dla Unknown. `WebGraphicRepresentation.IsPlaceholder`→`IsBlankFallback`; atrybut `data-legacy-graphic="blank"`; reader obejmuje TIFF. Decyzja: **ADR-0020**. Testy: GraphicConversion **35/35**, Infrastructure.UnitTests **188/188**, `dotnet build sln` 0 błędów. Ograniczenie: czysty wektor EMF/WMF nadal nie rasteryzowany w web (sidecar = roadmapa). Wcześniej:


2026-06-22 — **Security hardening upload + returnUrl (central policy services)**. Dodano centralne serwisy w Application: `IReturnUrlValidator` (`ReturnUrlValidator` + `ReturnUrlSecurityOptions`) oraz `IFileUploadSecurityService` (`FileUploadSecurityService` + `UploadSecurityOptions`) z kontrolą: spójność extension↔MIME↔magic-bytes, inspekcja archiwum DOCX (zip-slip, limity entry/uncompressed/compression ratio, wymagane part-y OOXML, blokada VBA), sanitizacja MIME dla obrazów i abstrakcja skanera malware (`IFileScanner`, `NoOpFileScanner`, fail-closed policy). Handlery podpięte do centralnych reguł: `UploadDocument`, `UploadImage`, `IngestExternalDocument`, `UpdateCallbackUrl`, `UpdateDeliveryRecipientUrl`, `FinishAndSendDocument`. Hosty (`D2Api` i `D2Services`) bindują sekcje `Security:Upload` i `Security:ReturnUrl`. Dodano testy security: `ReturnUrlValidatorTests`, `FileUploadSecurityServiceTests` + aktualizacje istniejących testów handlerów pod nowe zależności. Wynik walidacji: Application.UnitTests **295/295**, D2Api.Api.UnitTests **110/110**, D2Services.Api.UnitTests **39/39**.

2026-06-22 — **D2Services: domknięcie observability do standardu ELK/GCP**. W `Program.cs` podłączono `UseRequestObservability()` + `UseExceptionHandlingMiddleware()` (pipeline), a `UseSerilogRequestLogging` wzbogacono o `httpMethod/httpPath/statusCode/requestId/correlationId`. Dodano `Middleware/RequestObservabilityMiddleware.cs` (propagacja `X-Correlation-ID`, scope + Serilog `LogContext`). `ExceptionHandlingMiddleware` rozszerzony o `correlationId` w ProblemDetails oraz serializację runtime-type (`ValidationProblemDetails.errors`). `GcpJsonSerilogFormatter` ujednolicony do standardu D2Api: `severity+level+service+environment+traceId/spanId+flatten properties`. Testy D2Services.UnitTests: **28/28 pass** (nowe testy dla RequestObservability, MiddlewareExtensions, formattera i correlation/errors w exception middleware). Wcześniej:

2026-06-21 — **D2Services: dodatkowe testy Health + MiddlewareExtensions**. W nowym projekcie `D2ServicesViewerEditor.Api.UnitTests` dodano: `HealthControllerTests` (payload `/api/health` i `/api/health/detailed`) oraz `MiddlewareExtensionsTests` (`UseExceptionHandlingMiddleware` zwraca ten sam builder). Wynik projektu testowego: **22/22 pass** (wcześniej 19). Wcześniej:

2026-06-21 — **Zwiększenie pokrycia testami jednostkowymi: D2Api + D2Services**. Dodano **nowy projekt** `D2ServicesViewerEditor.Api.UnitTests` (NUnit): testy `DocumentController` (walidacja wejścia, mapowanie statusu), `ExceptionHandlingMiddleware` (mapowania wyjątków), `GcpJsonSerilogFormatter` (severity/category/exception). Wynik: **19/19 pass**. W `D2ApiViewerEditor.Api.UnitTests` dodano 3 testy brzegowe middleware: `RequestObservabilityMiddleware` (odrzucenie whitespace i >128 znaków w `X-Correlation-ID`) + `ExceptionHandlingMiddleware` (propagacja `correlationId` do `ProblemDetails.Extensions`). Wynik pełnego projektu API unit tests: **76/76 pass**. Zmieniono też `D2ServicesViewerEditor.sln` (podpięcie nowego projektu testowego). Wcześniej:

2026-06-21 — **„Zakończ" → synchroniczna wysyłka + statusy + „Pobierz oryginał" + ukrywanie „Ostatnia modyfikacja"**. Cztery zadania:
(1) **Pobierz oryginał dokumentu** — nowa pozycja menu „Plik" widoczna gdy `loadedFromDisk || userDownload`; zwraca ZAWSZE oryginał (v1): plik z dysku trzymany w pamięci (`diskOriginalFile`) albo wersja bazowa z API (`downloadBaseVersion` = `GET {masterId}/download`, już istniał). „Pobierz dokument" (edytowana kopia) bez zmian.
(2/2B) **„Zakończ"** przerobiony z modala-z-timerem na **synchroniczną pierwszą próbę**. `FinishAndSendDocumentCommandHandler`: zapis v2 → snapshot → delivery (Pending) → `document.Queued` → `BeginInlineAttempt` (`document.Sending`) → `IDeliverySender.SendAsync`. Sukces → `delivery.MarkSent` + `document.Sent` (delivered=true). Błąd → `delivery.HoldAfterFailedInlineAttempt` (RetryScheduled „zaparkowane" na DeadlineAt — worker nie przejmie) + `document.DeliveryFailed` (delivered=false). GUI: „Trwa wysyłanie dokumentu ...", potem na błędzie modal „Wystąpiły problemy z dostarczeniem dokumentu" + **„Przerwij"** (`POST /{masterId}/abort-send` → delivery `Cancelled` + `document.SendAborted` + powrót do edytora, bez zamknięcia karty) / **„Kontynuuj wysyłkę w tle"** (`POST /{masterId}/continue-delivery` → `Requeue` + `document.Queued` + zamknięcie karty; worker dokańcza). Nowe `DocumentStatus`: **`Queued`** („Zlecono do wysyłki"), **`SendAborted`** („UzytkownikPrzerwałWysyłkę"); „W trakcie wysyłki"=`Sending`, „Wysłano"=`Sent`, „Błąd wysyłki"=`DeliveryFailed` (istniały). Migracja **`infra/sql/010`** (tylko COMMENT — kolumna bez CHECK). `finish` zwraca teraz **200** `{deliveryId,status,documentStatus,delivered,error?}` (było 202).
(3) **Ostatnia modyfikacja** — w stopce gated `showSaveState()` (wcześniej zawsze widoczna); reguła `showSaveState` rozszerzona o ten element.
(4) **GET `/api/v1/document/{masterId}/status`** — już zwracał wyłącznie `{ masterId, status }` (`DocumentStatusResponse`) — bez zmian, zweryfikowane.
Build: D2Api + D2Services 0 błędów. Testy: Domain **77**, Application **282** (nowe: AbortSend/ContinueDelivery + przepisane FinishAndSend + DocumentDelivery inline), GUI Vitest **267** (document-editor 68, +Pobierz oryginał, przepisany flow „Zakończ"). `ng build` AOT kompiluje się; „failed" wyłącznie z powodu **pre-existing budżetu** bundla (`document-editor.scss` 38 kB — nietknięty). Wcześniej:

2026-06-19 — **`showSaveState` (ingest) — ukrywanie UI zapisu w edytorze**. Nowe opcjonalne pole `ShowSaveState: bool?` w `POST /api/v1/document` (D2Services). Inverse-default: brak/`true` → widoczne (jak dotąd), tylko jawne `false` zapisywane do `documents.metadata.showSaveState` i ukrywa w edytorze switch Autozapis + status w stopce + przycisk „Zapisz". Łańcuch: `CreateDocumentRequest` → metadata JSON → `ExternalDocumentMetadata.ShowSaveState`/`IsSaveStateVisible` (inverse-default `!= false`, pozycyjny param z domyślnym `null` — nie łamie wywołań) → `DocumentMetadataDto.ShowSaveState` → GUI sygnał `showSaveState` (z `meta.showSaveState !== false`). Reguła **prezentacyjna**: autozapis dalej działa w tle, „Zakończ i wyślij" bez zmian. Build D2Api+D2Services 0 błędów; Application metadata 31/31; GUI Vitest **263/263** (+2 `showSaveState`). Docs: API_CONTRACTS (reguła `showSaveState`), CHANGELOG. Wcześniej:

2026-06-13 — **Usunięto autoryzację po Keycloak (dual-auth)**. Zostaje wyłącznie Entra ID. Backend (`D2ApiViewerEditor.Api`): z `Program.cs` wycięta gałąź `if (keycloak.Enabled…)` (policy scheme `EntraOrKeycloak` + drugi `JwtBearer` + `SelectByIssuer`) — zostaje jeden tor `AddAuthentication(JwtBearer).AddMicrosoftIdentityWebApi(...)`. Skasowane pliki: `Security/KeycloakOptions.cs`, `Security/AuthSchemes.cs`, `Api.UnitTests/Security/AuthSchemesTests.cs`. Usunięta sekcja `"Keycloak"` z `appsettings.json` i `appsettings.DEV.json`. Docs zaktualizowane (SECURITY/FEATURES/API_CONTRACTS/RISKS R-25; ADR-0012 oznaczone „Keycloak USUNIĘTY"). Build API OK (0 błędów). Wcześniej:

2026-06-13 — **Lokalny bypass autoryzacji (dev) — praca bez Entra ID**. Po merge auth był bezwarunkowy (`[Authorize]` na `BaseApiController`, `MsalGuard` na trasach) → bez tenanta nie dało się uruchomić apek. Dodano przełącznik (domyślnie OFF, nigdy w prod): **Backend** — `DevAuthHandler` (schemat `DevAuth`, sztuczny principal z rolami `Operator`+`Administrator`) aktywny gdy `Auth:DevBypass=true` **i** `!IsProduction()`; w tym trybie `Program.cs` pomija całe Entra, polityki `RequireApp*`→`RequireAuthenticatedUser`, `ICurrentUserProvider`=`HttpHeaderCurrentUserProvider` (CorporateKey/admin z nagłówków `X-Corporate-Key`/`X-App-Admin`), Graph=Disabled. **Frontend** — `AppAuthConfig.enabled` (+`isAuthDisabled`); gdy `auth.enabled=false`: nowy `authGuard` przepuszcza bez MSAL, `appAdminGuard`→true, interceptor z pustą `protectedResourceMap` (brak tokena). Trasy `MsalGuard`→`authGuard`. **Włączenie lokalnie:** backend — `appsettings.DEV.secrets.json` (poza repo) `{ "Auth": { "DevBypass": true } }`; front — `public/assets/configs/config.json` `{ "auth": { "enabled": false } }` (lokalnie, nie commitować). Build API OK, `ng build` OK, GUI **259/259**, `runtime-config.spec` 4/4. Wcześniej:

2026-06-13 — **Fix serializacji ProblemDetails + domknięcie obserwacji z testów**. `ExceptionHandlingMiddleware` serializował przez statyczny typ bazowy `ProblemDetails` → `ValidationProblemDetails.Errors` nie trafiało do body. Fix: `JsonSerializer.Serialize(problemDetails, problemDetails.GetType(), options)` (typ runtime) → 400 dla błędów walidacji zawiera teraz słownik `errors` zgrupowany po `PropertyName`. Test zaktualizowany na zachowanie docelowe (`...EmitsGroupedErrorsInBody`). Api **51** (+1), reszta bez zmian. Wcześniej:

2026-06-13 — **Pokrycie testami D2ApiViewerEditor: +26 testów (pipeline behaviours, metadata, exception middleware)**. Stan: Domain 67, Application **258** (+19), Api **50** (+7), Infrastructure 164 — wszystko zielone (Integration 6 skip). Pokryto wcześniej-nietestowane klasy pure-logic: `ExternalDocumentMetadata` (parser tolerancyjny null/empty/malformed/json-null, mapowanie pól, case-insensitive web defaults, `IsUserDownloadAllowed` tylko dla jawnego `true`, round-trip serialize↔parse), MediatR `ValidationBehaviour` (brak walidatorów→next, pass→next, fail→`ValidationException`+short-circuit next, agregacja błędów wielu walidatorów), `LoggingBehaviour` (przekazanie odpowiedzi, log wejście+wyjście, brak połykania wyjątku), oraz API `ExceptionHandlingMiddleware` (pass-through, mapowania Validation/Argument→400, KeyNotFound→404, OperationCanceled→400, unexpected→500 bez przeciekania szczegółów, camelCase). **Obserwacja:** middleware serializuje `ValidationProblemDetails` przez statyczny typ bazowy `ProblemDetails` → słownik `errors` nie trafia do body (test asercjonuje realne zachowanie, bez zmiany produkcji). **Niepokryte (świadomie, infra/seam):** `DeliveryAttemptRunner`, `DocumentDeliveryWorker`, `OoxmlAgileDecryptor`, repo/GCS. Wcześniej:

2026-06-13 — **Pokrycie testami D2ApiViewerEditor: +28 testów dla nietestowanych klas**. Stan: Domain 67, Application **239** (+25), Infrastructure **164** (+3) — zielone. Pokryto wcześniej-nietestowane: `GetDocumentsQueryHandler`, `GetDeliveryStatusQueryHandler`, `GetDocumentVersionContentQueryHandler`, `UpdateDocumentVersionCommandHandler` (w tym v1-immutable), `IngestExternalDocumentCommandHandler` (DOCX 2 wersje / PDF 1 / unsupported / empty / db-error) + `IngestExternalDocumentCommandValidator`, oraz **`DigitalSignatureService`** (realny round-trip sign→verify na self-signed cert + wykrycie modyfikacji + brak podpisów). Mock framework: NSubstitute (zgodnie z resztą Application). **Niepokryte (świadomie, wymaga infra/seam):** `DeliveryAttemptRunner` (brak publicznego przejścia w `Sending` — tylko claim SQL → integracja), `OoxmlAgileDecryptor` (brak fixtury zaszyfrowanego OOXML), repo/worker/GCS (DB/GCS), `GcpJsonSerilogFormatter` (brak projektu testowego w Services), `Api.UnitTests` (lock `Infrastructure.dll` gdy API działa). Wcześniej:

2026-06-13 — **Pokrycie testami D2ApiViewerEditor (nowy kod)**. Domain 67, Application **214** (+3), Infrastructure **161** (+3) — wszystko zielone. Dodane: `RequeueDeliveryCommandHandlerTests` (handler „Wznów" był nietestowany), EMF/WMF rasteryzacja (`Wmf_WithEmbeddedDib...ViaGenericScan`, `Emf_WithCorruptDib_FallsBackToPlaceholder...`), `LegacyDocBinaryConverter` Prc-skip. **Uwaga:** `Api.UnitTests` (w tym 8 `GcpJsonConsoleFormatterTests`) nie buduje się, gdy działa lokalna instancja API (lock `Infrastructure.dll`) — środowisko, nie błąd kodu; zatrzymać API i odpalić. **Luki:** `GcpJsonSerilogFormatter` (Services — brak projektu testowego), integracja normalizer→`.doc` (wymaga CFB; konwerter pokryty wprost). Wcześniej:

2026-06-11 — **„Pliki do wysłania": „Kopiuj link" + akcje do prawej + padding szczegółów**. Przycisk „Podgląd edycji" → **„Kopiuj link"**: zamiast nawigować do edytora, buduje pełny link (`origin + serializeUrl(/editor?masterId=&versionId=)`) i kopiuje do schowka (`navigator.clipboard.writeText`; sukces → notice, brak Clipboard API/błąd → actionError z linkiem do ręcznego skopiowania). `openInEditor` → `copyEditLink`. Przyciski akcji wyrównane do prawej (`justify-content:flex-end`). Panel szczegółów dostał oddech: `.delivery-details` margin+padding+ramka, większy gap, separator pod wierszem nagłówka. GUI `admin-deliveries.spec` 14 (testy openInEditor→copyEditLink: link z/bez versionId, clipboard). `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": wiersz nagłówka „Szczegóły" + akcje (70%)**. W panelu szczegółów na górze nowy wiersz pełnej szerokości `detail-header-row` (grid 30%/70%): label „Szczegóły" w 1. kolumnie, wszystkie akcje (Podgląd edycji / Edycja metadanych / Wznów / Anuluj, zależne od statusu) w 2. kolumnie zajmującej 70% (przyciski poziomo, zawijane). „Adres odbiorcy" przeniesiony na dół — tuż **nad „Komunikat błędu"** (oba pełna szerokość). Usunięty `detail-span2`. GUI `admin-deliveries.spec` 14, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": akcje w szczegółach + fix znikającej tabeli**. (BUG) Błąd akcji Wznów/Anuluj ustawiał `error()`, który w szablonie był w gałęzi `@else if` → **cała tabela znikała**. Fix: osobny sygnał `actionError` (baner NAD tabelą z przyciskiem ×, `dismissActionError()`), `error()` zostaje tylko dla błędu ŁADOWANIA. (UI) Usunięta kolumna akcji z wiersza; **wszystkie akcje przeniesione do panelu szczegółów** (3. kolumna „Akcje", pionowy stos, zależne od statusu): Podgląd edycji + **Edycja metadanych** (zmieniona nazwa z „Edytuj") + Wznów + Anuluj. „Adres odbiorcy" rozciągnięty na 2 kolumny (`detail-span2`). Tabela 6 kolumn (colspan 6). Komunikaty błędów akcji biorą `err.error.error` z backendu. GUI `admin-deliveries.spec` **14** (+1 regresja: actionError≠error). `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": przebudowa panelu szczegółów (3 kolumny)**. `delivery-details` grid 2→**3 kolumny**: rząd „Master ID | Version ID | Status", rząd „Utworzono | Ostatnia próba | Następna próba". **Usunięto „Deadline"** z panelu. „Podgląd edycji" **przeniesione z akcji wiersza do panelu szczegółów** (wiersz `detail-actions`, widoczne tylko po rozwinięciu) — w `col-action` zostają Edytuj/Wznów/Anuluj. GUI `admin-deliveries.spec` 13, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": usunięta też kolumna „Ostatnia próba"**. Kolumna `lastAttemptAt` usunięta z tabeli (nagłówek+filtr+komórka); wartość zostaje w panelu szczegółów („Ostatnia próba", już tam była). Tabela 7 kolumn (colspan 7). Wyczyszczony `filterLastAtt` + `setFilter` zwężony do `id|doc|att|created|lock`. GUI `admin-deliveries.spec` 13, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": usunięte kolumny Następna próba/Deadline/Ostatni błąd z tabeli**. Te 3 kolumny (nagłówki + filtry + komórki) usunięte z tabeli; wartości przeniesione do panelu szczegółów (rozwinięcie): dodano „Następna próba" i „Deadline" obok „Komunikat błędu" (lastError już tam był). Tabela ma teraz 8 kolumn (colspan 8). Wyczyszczone sygnały/logika filtrów `filterNextAtt`/`filterDeadline`/`filterError` + zwężony typ `setFilter`. „Podgląd edycji" nadal tylko po rozwinięciu. GUI `admin-deliveries.spec` 13, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": Podgląd edycji + Następna próba/Deadline/Ostatni błąd tylko po rozwinięciu**. W `admin-deliveries` przycisk „Podgląd edycji" (`openInEditor`) oraz wartości kolumn `nextAttemptAt`/`deadlineAt`/`lastError` renderują się tylko gdy wiersz jest rozwinięty (`@if (isExpanded(deliveryId))`); w stanie zwiniętym te komórki są puste, a przycisk ukryty. Pozostałe akcje (Edytuj/Wznów/Anuluj) i kolumny bez zmian. Filtry tych kolumn działają dalej na danych. `ng build` OK. Wcześniej:

2026-06-11 — **Ekran końcowy: wskazówka, gdy „Zamknij kartę" nie zadziała**. `window.close()` zamyka tylko karty otwarte skryptem (Chrome blokuje resztę), więc przycisk „Zamknij kartę" często nic nie robił. `closeFinishedTab()` po próbie ustawia (po 300 ms) `tabCloseBlocked` → overlay końcowy chowa przycisk i pokazuje wskazówkę o ręcznym zamknięciu (Ctrl/⌘+W). Timer sprzątany w `ngOnDestroy`. To ograniczenie przeglądarki, nie bug. GUI `document-editor.spec` 62, `ng build` OK. Wcześniej:

2026-06-11 — **„Otwórz w edytorze" przeniesione do wiersza wysyłki**. Usunięto osobną zakładkę/stronę „Otwórz plik w edytorze" (route `/admin/open`, komponent `admin-open-document` skasowany, pozycja sidebaru usunięta). Zamiast tego w `admin-deliveries` każdy wiersz ma przycisk **„Otwórz"** → `openInEditor(item)` nawiguje do `/editor?masterId=&versionId=` na podstawie danych wysyłki (`documentId`→masterId, `sourceVersionId`→versionId; bez versionId → podgląd). Testy: GUI `admin-deliveries.spec` **13** (+2 nawigacja z/bez versionId); usunięto `admin-open-document.spec`. `ng build` OK. Wcześniej:

2026-06-11 — **Wysyłka na returnUrl: multipart/form-data zamiast octet-stream**. `HttpDeliverySender` wysyła plik jako `multipart/form-data` w polu **`file`** (filename `document.docx`, part `Content-Type` = DOCX MIME) zamiast surowego `application/octet-stream` body. Nagłówki `Idempotency-Key`/`X-Content-SHA256` i klasyfikacja wyniku (perm/retryable) bez zmian. Uzasadnienie nazwy/typu: finish-and-send dostarcza zawsze edytowalny DOCX (PDF nie ma wersji edytowalnej). Testy: Infrastructure **+3** `HttpDeliverySenderTests` (multipart+nagłówki, 422=permanent, 503=retryable). **Uwaga kontraktowa:** odbiorca musi czytać plik z pola formularza `file`, nie z surowego body. Wcześniej:

2026-06-11 — **Observability: wyjątki w GCP Logs Explorer jako ERROR, nie INFO**. Przyczyna: domyślny formatter konsoli (MS logging w internal API; Serilog `WriteTo.Console()` w external API) pisze zwykły tekst na stdout → Cloud Logging taguje wszystko jako INFO (brak pola `severity`). Fix: oba hosty emitują **JSON z polem `severity`** (mapowanie LogLevel→LogSeverity GCP), wyjątek dołączony do `message` (Error Reporting). Internal API: nowy `GcpJsonConsoleFormatter` + `builder.AddGcpStructuredLogging()`. External API: `GcpJsonSerilogFormatter : ITextFormatter` w `WriteTo.Console(...)`. Włączane poza Development lub `Logging:UseGcpFormat=true`; lokalnie zostaje czytelny tekst. Zero nowych zależności. Testy: `Api.UnitTests` **43** (+8 `GcpJsonConsoleFormatterTests`). Oba hosty budują się OK. Follow-up (poza zakresem): `LoggingBehaviour` loguje `{@Request}` (pełny payload, np. base64) — do okrojenia. Patrz ADR-0014. Wcześniej:

2026-06-11 — **Fix: po „Zamknij" w modalu wysyłki użytkownik wracał do edycji**. `window.close()` działa tylko dla kart otwartych skryptem — dla zwykłych kart przeglądarka odmawia, więc po kliknięciu „Zamknij" znikał tylko modal, a użytkownik mógł dalej edytować zakończony dokument. Fix: `closeFinishModalAndExit()` wchodzi w stan końcowy `workFinished` (nowy sygnał), zatrzymuje auto-save (`stopAutoSave` + `autoSaveEnabled=false`) i nadal próbuje zamknąć kartę; gdy karta nie zostaje zamknięta, renderowany jest **blokujący ekran końcowy** (`work-finished-overlay`, z-index 3000 nad toolbarem) „Praca nad dokumentem zakończona" + przycisk „Zamknij kartę" (`closeFinishedTab()` → ponowna próba `window.close()`). Użytkownik nie wraca już do edycji. Testy: GUI `document-editor.spec` **62** (+1). `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": edycja adresu odbiorcy (returnUrl)**. Nowa akcja **Edytuj** w wierszu (dostępna, gdy zadanie nie jest `Sent`/`Sending`) → modal z polem adresu (returnUrl/recipientUrl) + Zapisz/Anuluj. Backend: `DocumentDelivery.UpdateRecipientUrl(url)` (walidacja http(s) + blokada dla Sent/Sending), `UpdateDeliveryRecipientUrlCommand`+handler, endpoint `PUT /deliveries/{id}/recipient-url` (`{ recipientUrl }`). Front: `updateDeliveryRecipientUrl()` w storage service, modal w `admin-deliveries` (sygnały `editingId`/`editUrlValue`/`editError`/`savingEdit`; autoodświeżanie pauzowane gdy modal otwarty). Po zmianie zwykle „Wznów", by ponowić na nowy adres. Testy: Domain `DocumentDelivery` **21** (+5), Application `UpdateDeliveryRecipientUrlCommandHandler` **3**, GUI `admin-deliveries.spec` **11** (+4). `dotnet build` sln OK, `ng build` OK. Bez migracji SQL (zmiana tylko wartości kolumny). Wcześniej:

2026-06-11 — **„Pliki do wysłania": autoodświeżanie 3 s + Anuluj/Wznów**. Lista delivery auto-odświeża się co 3 s (rxjs `interval`, ciche `fetch(silent)` bez spinnera/resetu strony; przełącznik „Autoodświeżanie", `OnDestroy` sprząta sub; pauza w trakcie ładowania/akcji). Akcje per wiersz: **Wznów** (`retryDelivery` → `/deliveries/{id}/retry`) dla `RetryScheduled`/`FailedPermanently`/`DeadLettered`/`Cancelled`; **Anuluj** (nowy `/deliveries/{id}/cancel`) dla `Pending`/`RetryScheduled`. Backend: nowy status **`Cancelled`** (enum `DeliveryStatus` + `IsTerminal`), `DocumentDelivery.Cancel()` (Pending/RetryScheduled→Cancelled, czyści lease) i **rozszerzony `Requeue()`** (dawniej tylko terminalne-nieudane → teraz też RetryScheduled „wyślij teraz" i Cancelled); `CancelDeliveryCommand`+handler (anulowanie zdejmuje dokument ze stanu Sending → `MarkSaved`); endpoint `POST …/cancel`. **Migracja SQL `008_add_delivery_cancelled_status.sql`** rozszerza CHECK constraint o `Cancelled` (uruchomić na środowiskach!). Worker nie claimuje Cancelled (poza due/active). Testy: Domain `DocumentDelivery` **16** (+6), Application `CancelDeliveryCommandHandler` **3**, GUI `admin-deliveries.spec` **7** (nowy). `dotnet build` sln OK, `ng build` OK, GUI **244**. Wcześniej:

2026-06-11 — **Admin: „Otwórz plik w edytorze"**. Nowa pozycja w sidebarze panelu admina (`admin-shell`) + strona `admin-open-document` (route `/admin/open`): formularz `Master ID` (wymagane) + `Version ID` (opcjonalne) + przycisk „Otwórz". Nawiguje do `/editor?masterId=&versionId=` (z `versionId` → edycja wskazanej wersji, bez → podgląd wersji bazowej) — te same query params, których edytor już używa. Stateless, sygnały + `canOpen` (blokada gdy brak masterId). Testy: GUI `admin-open-document.spec` **4** (nawigacja z/bez versionId, walidacja). `ng build` OK. Wcześniej:

2026-06-11 — **5 zgłoszeń: dialog hasła, .doc, GUI „wysyłanie", EMF→PNG, VersionId w mailu**. (P1) Dialog hasła nie zamyka się już klikiem w tło ani Escape — tylko `Anuluj`/`Otwórz`; `cancelPasswordDialog()` nie zostawia pustego dokumentu: bez `returnUrl` → `router.navigate(['/'])`, z `returnUrl` → `tryCloseBrowserTab()` (przepływ powrotu jak po „Zakończ"). (P3) Modal „Trwa wysyłanie pliku…" używa teraz tego samego spinnera co ekran ładowania (`.loading-spinner`, klasa `.finish-dialog-spinner`) — spójny wygląd z „Przetwarzanie…". (P5) Mail „Zgłoś" podstawia realny **Version ID** z `documentVersionId()` (było `documentMetadata().version` = zwykle `—`); fallback `—` tylko gdy wersji faktycznie brak; `Master ID` bez zmian. (P4) **EMF/WMF są teraz rasteryzowane do PNG** w przeglądarce: nowy DIB-extractor (rekordy `EMR_STRETCHDIBITS`/`SETDIBITSTODEVICE`/`BITBLT`/`STRETCHBLT`/`ALPHABLEND` + skan `BITMAPINFOHEADER`) → BMP → **SkiaSharp** (`SKBitmap.Decode`→PNG; pure-managed, Linux/GCP-safe). Placeholder „EMF — podgląd w Word" zostaje TYLKO dla czysto wektorowego metafile bez osadzonego rastra. Eksport: `data-original-src` niesie oryginalny EMF/WMF dla KAŻDEGO metafile (nie tylko placeholdera) → writer zapisuje wektorowy oryginał, PNG to wyłącznie podgląd. (P2) **Binarny .doc jest konwertowany do .docx**: nowy `LegacyDocBinaryConverter` parsuje FIB + piece table (CLX/PlcPcd, compressed CP1252/Latin1 i 16-bit Unicode) i buduje DOCX (tekst + akapity) przez OpenXML; wpięty w `DocumentInputNormalizer` (gałąź `WordDocument`), z fallbackiem na dotychczasowe kontrolowane odrzucenie przy niespójnej strukturze. Testy: Infrastructure **155** (+1 EMF→PNG, +4 .doc), Api `DocumentController` 13, GUI `document-editor.spec` **61** (+4). `dotnet build` sln OK, `ng build` OK. Wcześniej:
2026-06-10 — **Pełny wzorzec Qutas/D2WebCore dla Entra ID (ADR-0012)**. Backend: goły `JwtBearer` → **`Microsoft.Identity.Web` 3.12.0** (`AddMicrosoftIdentityWebApi`; pin JwtBearer usunięty — Identity.Web per-TFM). **Mapowanie grup→role**: `RolesOptions` (sekcja `Roles`) + `ClaimsTransformer` (claim `groups`→`APP_*`; App Roles zachowane, hybryda). **Graph v5 (5.103.0)**: `GraphUserService` app-only + `GET /api/identity/users` (`IdentityController`, admin); bez sekretu → `DisabledGraphUserService`. **GCP Secret Manager** (`EntraSecretLoader` → `AzureAd:ClientSecret`, guard+try/catch). **Keycloak dual-auth**: policy scheme `EntraOrKeycloak` wybierający po issuerze (gdy `Keycloak:Enabled`). Frontend: **runtime `config.json`** (`public/assets/configs/`) ładowany w `main.ts`→`RUNTIME_AUTH_CONFIG` (fallback `environment.auth`); fabryki MSAL/`appAdminGuard`/`CurrentUserService` z runtime-configu. Wszystkie wartości env = **placeholdery**. Testy: `Api.UnitTests` **41** (+6 `ClaimsTransformerTests`), GUI **236**, oba buildy OK. **Aktywacja per-środowisko** wymaga realnych wartości Entra/GCP/Keycloak + app consent Graph; runtime niezweryfikowany lokalnie. Adaptacja do SPA+API: bez serwerowego OIDC. Zrobione na bieżącym drzewie (merge `feature/azure` wciąż otwarty — `.claude/settings.json` do rozwiązania). Wcześniej:

2026-06-09 — **„Lista plików": statusy po polsku**. `admin-files.statusLabel` przetłumaczony: Saved→Zapisany, Editing→W edycji, Sending→Wysyłanie do odbiorcy, DeliveryFailed→Odbiorca nie odpowiada, Sent→Wysłany (wartości enuma API bez zmian; filtr tekstowy działa na nowych etykietach). `tsc` OK. Wcześniej:

2026-06-09 — **Panel administracji: domyślnie firmowa czcionka**. `admin-shell` `:host` dostał `font-family: var(--corporate-font-family, 'Calibri','Segoe UI',Arial,sans-serif)` — dziedziczy na sidebar i strony routowane (admin-files/admin-deliveries). Dodatkowo globalna reguła w `styles.scss` (`d2-admin-shell input,select,button,textarea { font-family: inherit }`), bo natywne kontrolki formularzy nie dziedziczą fontu. `--corporate-font-family` pochodzi z `assets/fonts/_corporate-font.scss` (gdy firmowy krój nieskonfigurowany → fallback Calibri). `ng build` (AOT) OK, admin-shell spec 3/3. Wcześniej:

2026-06-09 — **Panel „Pliki do wysłania": rozwijane szczegóły wysyłki**. Klik w wiersz rozwija pod nim panel ze szczegółami: **adres odbiorcy (recipientUrl)**, **Master ID** (=documentId), **Version ID** (=sourceVersionId), status, utworzono, ostatnia próba, **komunikat błędu**. Backend: `DeliveryListItemDto` rozszerzony o `SourceVersionId` + `RecipientUrl` (mapowanie w `GetDeliveriesByStatusQueryHandler`). Front: `DeliveryListItem` +`sourceVersionId`/`recipientUrl`; `admin-deliveries` sygnał `expandedId` + `toggleExpand`/`isExpanded` (jeden rozwinięty naraz), klik w `.doc-row` (retry ma `stopPropagation`), caret ▸ obraca się. Testy: Application **205** (+1 mapowanie pól; razem `GetDeliveriesByStatusQueryHandlerTests` 8), GUI AOT build OK. Wcześniej:

2026-06-09 — **Panel „Pliki do wysłania": domyślny filtr = wszystkie statusy**. Panel pokazywał pusto, bo domyślny filtr to był `DeadLettered` (a świeże wysyłki mają Pending/Sending/Sent). Endpoint `GET …/deliveries` wymagał konkretnego statusu. Teraz: parametr `status` **opcjonalny** — pusty/`null`/`"all"` (case-insensitive) ⇒ wszystkie. Backend: nowa `IDocumentDeliveryRepository.GetAllAsync` (+impl, `OrderByDescending(CreatedAt)`), `GetDeliveriesByStatusQuery.Status` → `string?` z gałęzią all/konkret/nieznany, kontroler default `"DeadLettered"`→`""`. Front: `getDeliveriesByStatus`→**`getDeliveries(status?: DeliveryStatus|null)`** (pomija param gdy brak), `admin-deliveries` sentinel **`'all'`** jako domyślny + opcja „Wszystkie" w dropdownie. Testy: Application **204** (+7 nowy `GetDeliveriesByStatusQueryHandlerTests`), GUI AOT build OK, `tsc` OK. Uwaga: nadal nie zobaczysz nic, jeśli tabela `document_deliveries` jest pusta (nikt nie zakończył wysyłki). Wcześniej:

2026-06-09 — **UX „Zakończ": modal wysyłki + odliczanie + zamknięcie karty**. Po kliknięciu „Zakończ" (po zapisie + `finishAndSend`) pokazujemy modal „Trwa wysyłanie pliku do Twojej aplikacji" z przyciskiem „Zamknij (30…0)". Dojście do 0 **lub** klik „Zamknij" → zamknięcie modala + best-effort `window.close()` (w try/catch — gdy przeglądarka odmówi, neutralny stan, bez crashu). Nie czekamy na pełne dostarczenie — robi to worker (retry/backoff do 24h, `finish` idempotentny). Błąd natychmiastowego zakolejkowania → toast „Nie udało się natychmiast wysłać pliku. Ponowimy próbę wysłania w tle." (modal zostaje, brak blokady). Guard `isFinishing` + `[disabled]` na przycisku chronią przed wielokrotnym wywołaniem. **Usunięto** blokujący `pollDeliveryStatus` (z kontradyktoryjnymi toastami). Backend bez zmian. Wydzielony `onFinishCountdownTick` (testowalny bez fake-timerów). `finishSendSub` anulowany w `ngOnDestroy` (brak zapisów po teardownie). GUI **229** (+8, `document-editor.spec` 57). `tsc` OK. Wcześniej:

2026-06-09 — **Security: podbicie podatnej zależności (PRISMA)**. `System.Security.Cryptography.Xml` wchodził tranzytywnie (NPOI/OpenXml) w **8.0.2** — flagowany przez PRISMA. Dodany **jawny** `PackageReference` **10.0.6** w `D2ViewerEditor.Infrastructure.csproj` (wyższa bezpośrednia wygrywa nad tranzytywną); propaguje się do obu hostów API przez `ProjectReference`. Zweryfikowane: `dotnet list package --include-transitive` = 10.0.6 we wszystkich 3 projektach; `dotnet build -c Release` Infrastructure + oba API = OK, 0 błędów, brak NU1605. net8.0 konsumuje 10.0.x bez problemu (precedens: `Microsoft.Extensions.Configuration.Abstractions 10.0.4`). Zero zmian kodu. Wcześniej:

2026-06-09 — **Dwa bugi naprawione**. (1) **Dekrypcja hasłem**: NPOI 2.8.0 nie ma implementacji Agile (brak `AgileEncryptionInfoBuilder` we wszystkich TFM) → każdy plik dostawał „WrongPassword". Nowy **`OoxmlAgileDecryptor`** — własna, czysto zarządzana dekrypcja MS-OFFCRYPTO Agile (SHA512 spinCount + AES-256-CBC); NPOI tylko czyta kontener CFB. Round-trip encrypt→decrypt przetestowany (poprawne/złe/brak hasła). (2) **Font-size input gubił selekcję (FMT-004)**: `saveSelection` zapisywał zwinięte selekcje na blur/selectionchange → klik w input nadpisywał zaznaczenie pustą karetką. Fix: strażnik `editorHasFocus()` — zapis tylko gdy edytor ma fokus. Backend Infrastructure **150** (+2), GUI **213** (+1). Wcześniej:

2026-06-08 — **FMT-005: zmiana czcionki nie wpływała na nowo wpisywany tekst**. `setFontFamily` wołał `editor.focus()` **przed** odtworzeniem selekcji → wybór z natywnego `<select>` czyści selekcję contenteditable, `focus()` przywraca karetkę na początek dokumentu (`rangeCount>0`, więc strażnik `===0` nie odpalał restore), ZWS-span z `font-family` lądował w złym miejscu, a tekst pisany w realnej pozycji dziedziczył domyślną/firmową czcionkę. Fix: odtworzenie selekcji **ZANIM** dotkniemy fokusu (lustro `setFontSize`); aktualizacja istniejącego ZWS-spana zamiast zagnieżdżania; zapis karetki + `updateFormattingState`. GUI `wysiwyg-editor.toolbar-actions.spec.ts` **7** (+1). `tsc` OK. Wcześniej:

2026-06-08 — **EMF/eksport + import font-size + import page-break + selekcja**. (IMG-009) writer pisał placeholder SVG jako goły `a:blip` = uszkodzony DOCX; teraz reader niesie oryginał w `data-original-src`, writer zapisuje prawdziwy **EMF part** + twardy guard „nigdy gołego SVG blip". Przy okazji OpenXmlValidator wykrył i naprawiono **2 pre-existing bugi kolejności w `styles.xml`** (heading rPr/pPr) psujące każdy zapis. (IMP-005) „14pt→10pt": reader OK, ale front `_flattenTopBlocks` rozwijał `.document-content` z defaultem → `_captureDocumentDefaults` + bind font-size/family na stronie. (IMP-008) „3 strony→1": reader ignorował `w:pageBreakBefore` → teraz emit marker page-break. (FMT-004) `savedSelection` tylko na blur (za późno) → zapis na `onSelectionChange`. Backend **144**, GUI **211**. Wcześniej:

2026-06-08 — **Edytor: 4 realne naprawy live-bugów**. (PAG-002) `_schedulePaginate` debounce 600→**250 ms** — strona przestaje rosnąć poza A4 przed reflow (Enter). (PAG-001) nowy `_tryMergeAcrossPageBackwards` — Backspace na początku strony z AUTO-paginacji scala treść z poprzednią stroną / usuwa pustą stronę (wcześniej tylko manualny page-break był obsłużony). (UI-006) `onContextMenu` clamp `Math.max(8,…)` — menu nie wjeżdża nad toolbar na niskim oknie. (PDF-010) `pdf-viewer.ts` loguje konkretny błąd + rozróżnia awarię workera od uszkodzonego pliku (koniec maskowania). GUI **209** (+7). tsc OK. Wcześniej:

2026-06-08 — **Akapit „Ustaw jako domyślne" — naprawa (Qutas-PAR-007)**. Przycisk wołał `resetParagraphDefaults()` (reset do wartości bazowych) zamiast zapisać bieżące ustawienia → repointowany na nowe `setParagraphAsDefault()`: snapshot ustawień do `_paragraphDefaults` (default per-sesja) + `applyParagraphSettings()` na bieżącym akapicie (nowe akapity dziedziczą przez contenteditable). `readCurrentParagraphSettings()` przy braku selekcji seeduje formularz z defaultu. Bez localStorage/API. GUI **202** (+3). Diagnostyka pozostałych 10 zgłoszeń edytora: już wdrożone w sesjach 2026-06-03/04 (zweryfikowane w drzewie). Wcześniej:

2026-06-04 — **Health-check API: pierwszy strzał natychmiast + dedup**. `ConnectionStatusService.checkApi()` → publiczne **`checkNow()`** ze strażnikiem in-flight (`_checkInFlight`) — brak duplikatów przy nakładających się wyzwalaczach (start + `online` + interwał). `app.config.ts` `provideAppInitializer` woła `checkNow()` jawnie (nie polega na side-effekcie konstruktora) → pierwszy `/health` zaraz po starcie, przed renderem, non-blocking; guard scala z checkiem konstruktora w 1 żądanie. Polling 30 s/banner bez zmian. GUI **199** (+2). Wcześniej:

2026-06-04 — **Konwerter grafik VML/EMF/WMF → web** (ref: `.ai/GRAPHICS_CONVERSION.md`). Reader emitował EMF/WMF jako `data:image/x-emf` (nierenderowalne) → złamany obraz. Nowy `IGraphicConversionService` (Domain) + `GraphicConversionService` (Infrastructure) — **pure-managed, bez LibreOffice/GDI/System.Drawing** (Linux/GCP-safe): detekcja magic-bytes, wymiary z nagłówków (EMF rclFrame/WMF placeable), ekstrakcja osadzonego rastra, **placeholder SVG** + **pass-through oryginału** (Word renderuje prawdziwą grafikę), sanitizer SVG (XXE/script blok), VML rect/oval/line→SVG. Hook `DocxToHtmlConverter.WebGraphicForLegacy` (a:blip + v:imagedata). Testy: Infrastructure **137** (+21 unit/security/integration). Benchmark `GraphicConversionBenchmarks`. Decyzja: zero nowych zależności graficznych; rasteryzacja EMF przez sidecar = roadmapa. Wcześniej:

2026-06-04 — **Edytor: 8 błędów naprawionych/przeanalizowanych** (ref: `.ai/EDITOR_KEYBOARD.md`). (1) ENTER cofał kursor — kotwica karetki przy repaginacji zmieniona z globalnego offsetu tekstowego na **`{block,offset}`** (pusty akapit ≠ poprzednia linia). (2) font-size input ukrywał tekst — `setFontSize` odrzuca `0`/NaN/zakres. (3) font-family wracała do domyślnej — **root cause backend**: `innerHTML` daje `&quot;`, writer regex ucinał nazwę na `;` w encji → HTML-decode stylu przed parsowaniem. (4) „Wklej bez formatowania" — `insertText` odtwarza selekcję/fokus (klik menu gubił karetkę). (5) interlinia — mapowanie OOXML→CSS **poprawne** (pinned testami); różnice z Word to nieusuwalne metryki fontu + kolaps marginesów. (6) link — `insertLink` odtwarza selekcję + normalizuje URL. (7) `.doc` — **Wariant B: jawne odrzucenie** z instrukcją konwersji (brak konwertera w pipeline). (8) Backspace usuwa manualny page-break na początku strony (`_tryDeletePageBreakBackwards`); strzałki między stronami złagodzone. Testy: backend Infrastructure **116** (+9), Api **34** (+1), App 193, Domain 57; GUI **197** (+12). Import/eksport/autozapis/undo/tabele/obrazy nietknięte. Wcześniej:

2026-06-04 — **Layout: banner środowiska spychał dashboard w dół**. Shell (`d2-root` flex-column `height:100vh`) daje stronie obszar `100vh − banner` (`d2-root > d2-<page>{flex:1 1 0;min-height:0;overflow:hidden}`). Edytor robił to dobrze (`:host{height:100%}`). Bug: `dashboard.scss .dashboard-wrapper` i `pdf-maintenance .maintenance-wrapper` miały **`min-height:100vh`** → wymuszały pełny viewport wewnątrz mniejszego hosta → treść zjeżdżała o wysokość bannera, stopka ucinana. Fix: `100vh`→**`100%`** (banner-agnostyczny idiom `100%-of-host`, jak edytor) — bez magicznych marginesów/zmiennych/`!important`/global CSS. `app.scss .app-container` = martwy CSS, bez zmian. Testy: nowy `pages/layout-shell.spec.ts` (2: wrapper CSS nie zawiera `100vh`, ma `min-height:100%`), GUI **185** (+2). Edytor i bannery nietknięte. Wcześniej:

2026-06-04 — **3 poprawki renderowania (font fallback / nawigacja kursora / numer strony)**. (P1) Reader emitował font jako `,'X',sans-serif` → brakujący Times New Roman renderował się bezszeryfowo (jak Calibri). Fix: `FontFamilyCss`/`GenericFontFallback` dobiera generyk (serif/monospace/sans-serif) wg nazwy kroju; bez hardcode/plików czcionek. (P2) Strony to osobne `contenteditable`, więc ArrowDown/ArrowUp nie przechodził między nimi. Fix: `_tryMoveCaretAcrossPages` + `_isCaretOnEdgeLine` + `_placeCaretAtEditorEdge` w `handleKeyboard` (tylko zwinięta karetka, bez modyfikatorów — Shift/zaznaczenia nietknięte). (P3) Pole PAGE w stopce ignorowało font-size runów stopki. Fix: reader `FieldSpan` niesie rPr pola; writer `BuildFieldRun` → poprawny `w:fldSimple` z wewnętrznym runem (rPr), `FlushPending` uznaje paragraf z `SimpleField`; frontend `_pageNumberHtml`/`_inlineFieldFontSize` + CSS `font-size:inherit`. Testy: backend Infrastructure **107** (+4), Application **193**, Domain **57**; GUI **183** (+7). Api.UnitTests pominięte (lock DLL od działającej instancji API — nie dotyczy Infrastructure). Ograniczenia: edge-line detection layout-zależne (jsdom nie odtwarza → stub w testach); fallback nie podstawia brakującego kroju (świadomie). Wcześniej:

2026-06-03 — **Font Times New Roman + page break display (druga iteracja)**. (Font) Reader ignorował font **domyślnego stylu akapitu `Normalny` (`w:default="1"`, ascii=Times New Roman)**, który nadpisuje docDefaults (asciiTheme→Cambria) → edytor pokazywał Calibri. Fix: `ApplyDefaultParagraphStyleFont` ustawia font kontenera z default-style. Realny plik: kontener `font-family:'Times New Roman';font-size:10.5pt`. (Page break) Round-trip był OK (R-15), ale **display** wciąż gubił podział: `_splitHtmlIntoPages` **konsumował** marker → `_repaginateNow` scalał. Fix: split **zachowuje** marker → repaginate honoruje (`_isPageBreakBlock`), zapis go nie gubi; usunięty martwy `_joinPagesWithBreaks`. **386 backend** (+4 `DefaultStyleFontTests`), **176 GUI** (+1). Wymaga reloadu dokumentu w edytorze. Ograniczenia: font niedostępny w przeglądarce → fallback (nazwa zachowana); autosave bezstanowy R-20. Wcześniej:

2026-06-03 — **Page break „od nowej strony" w edytorze (display)**: manualny break nie zaczynał nowej strony w widoku (np. „PROTOKÓŁ…" pod podpisami). Reader emituje break-only akapit jako **top-level** `<div class="page-break">` (`IsPageBreakOnlyParagraph`) zamiast zagnieżdżony (psuł `_splitHtmlIntoPages` + ignorowany przez height-paginację). Frontend `_repaginateNow` **wymusza nową stronę** na bloku page-break (`_isPageBreakBlock`), marker zostaje w treści (przeżywa zapis). Backend **378 pass** (+1), GUI **171** (+1). Wymaga ponownego wczytania dokumentu w edytorze. Wcześniej:

2026-06-03 — **R-15/R-16/R-17**: (R-15 ZAMKNIĘTE) writer konwertuje page-break (zagnieżdżony/blok/CSS) → `w:br type=page`, round-trip zachowany (AFTER=1=GOOD). (R-16 CZĘŚCIOWE) `ConvertPreservingPackage` — pass-through `styles.xml`/`theme`/`fontTable` z oryginału (AFTER: **164 style + Cambria** vs BAD 16+Calibri); wpięte w `DownloadEditedDocument` (ładuje v1, fallback gdy brak). (R-17 ZAMKNIĘTE) scalanie fragmentów split-table w `getContent` (`data-split-table-id`+`_mergeSplitTables`, colgroup zachowany; niezależne tabele bez zmian). Testy: backend **377 pass** (+10 Infra, +1 App), GUI **170** (+2). `zapisany_AFTER.docx` zregenerowany pass-through. **Pozostaje: R-19** (numbering nieprzenoszony), **R-20** (pass-through tylko Download, nie autosave — autosave wciąż stratny), **R-21** (`styles2.xml` artefakt SDK). Checklista weryfikacji Word: `DOCX_CONVERSION.md` §6b. Wcześniej:

2026-06-03 — **Round-trip fidelity: naprawa 4→7 stron + powiększonych tabel** (analiza orginał_GOOD vs zapisany_BAD). 4 fixy źródłowe: (1) reader domyślny padding komórek `4px`→**0** góra/dół (Word default) — tabele nie rosną w pionie; (2) writer `tblW` domyślnie **auto** zamiast `pct 5000` — tabele nie rozciągane do 100%; (3) writer tabelowy `tblCellMar` 40/80→**0/108**; (4) writer `AddPageSettings` bez inflacji marginesów + **rekonstrukcja header/footer distance** = `clamp(margin−band,0,720)`. Frontend: `getContent()` scala strony **bez** `<div class="page-break">` (nie materializuje auto-paginacji wg wysokości → koniec nadmiarowych twardych breaków). **Weryfikacja na realnym pliku** (reader→writer→AFTER): top 567/bottom 850 (vs BAD 1281/1231), header 6/footer 339 (vs 720/720), tblW auto (vs pct 5000), tcMar góra+dół=0 (vs 8160 tw), twarde breaki 0 (vs 3) — **AFTER ≈ GOOD**. Testy: backend **366 pass** (+4 `RoundTripLayoutFidelityTests`, +regen 2 golden), GUI **168** (+2 `getContent`). Ograniczenia: R-15 (1 oryg. break nieodtworzony), R-16 (model stratny: styles/numbering/theme/font), R-17 (split-table=2 tabele), R-18 (zapiekane trHeight). Wcześniej:

2026-06-03 — Dokumentacja: nowy **`.ai/DOCX_CONVERSION.md`** — zweryfikowany w kodzie reference konwersji DOCX↔HTML (pipeline + 3 diagramy Mermaid, model `DocumentContent`, macierz statusów Implemented/Partially/Planned z kierunkiem R/W i ścieżkami, roadmapa R-10 / computed-style / domknięcia 5,6,7, harness testów). `INDEX.md` + cross-linki z `FIDELITY_REPORT`/`FEATURES`. Zero zmian kodu (testy bez zmian: backend 358, GUI 166). Kluczowe ustalenia: multi-section = tylko 1 sekcja (R-10); computed-style częściowy (brak centralnego resolvera); rotacja obrazów brak; VML tylko podstawowy; `tcMar` jest, `tblCellSpacing` brak; `w:tabs` na zapisie tylko w stylach Header/Footer. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 8 (audyt izolacji CSS — bez zmian kodu)**: ustalono, że treść jest już izolowana (inline > klasy przez specyficzność). Faktyczne `!important` w `wysiwyg-editor.scss` = **6** (nie 65; tylko 1 dotyczy treści — świadoma Calibri Light→Calibri w nagłówkach); reszta + `styles.scss`/`document-editor.scss` to UI (podświetlenia/paddingi), nie nadpisują treści. `FIDELITY_REPORT` skorygowany. Brak zmian kodu → testy bez zmian (backend 358, GUI 166). **Cały plan 0–9 pokryty** (3,5,6,7 jako wartościowe slice'y; pozostaje multi-section R-10 i pełny computed-style — udokumentowane). Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 7 (tab-stopy nagłówka/stopki)**: akapit z tab-stopem Center/Right → `display:flex`; run-tylko-tab → rosnący spacer `flex:1 1 0` (bezpośrednie dziecko flexa) → układ lewo⇥środek⇥prawo rozkłada się na szerokości. Znak taba zachowany (round-trip OK), mieszane runy bez zmian. **80/80 Infrastructure** (+golden `TabStopLeftCenterRight`). Ograniczenia: środek nie zawsze idealnie centrowany; writer nie emituje `w:tabs` (po zapisie układ wraca do tabów, bez regresji); multi-section (R-10) wciąż otwarte. Kolejność: 0✅1✅2✅3◐4✅5◐6◐7◐9✅ → **8 (izolacja CSS front)** → domknięcia. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 9 (raport zgodności)**: nowy `.ai/FIDELITY_REPORT.md` — macierz wierności (wysoka/częściowa/niemożliwa + przyczyny techniczne), pokrycie testami (backend **358 pass**, GUI **166 pass**), rekomendowane kolejne kroki (priorytet: Etap 7 multi-section/tab-stopy, Etap 3 cd. computed-style, Etap 8 izolacja CSS). Indeks `.ai/INDEX.md` rozszerzony. Dostarczone w sesji: Etapy 0,1,2,3◐,4✅,5◐,6◐,9. Pozostaje: 7 (header/footer multi-section/tabs), 8 (izolacja CSS front), domknięcia 3/5/6. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 6 (obrazy — alt text)**: round-trip tekstu alternatywnego `<img alt>` ↔ `wp:docPr/@descr` (reader: descr/title→alt tylko gdy niepuste; writer: alt→descr w inline+anchor, `DeEntitize` p/podwójnemu escape). **79/79 Infrastructure** (+3 `ImageAltTextRoundTripTests`), golden niezmienione. Obrazy wcześniej: floating/border/crop/EMU round-trip. Pozostaje: rotacja, skalowanie crop, VML. Kolejność: 0✅1✅2✅3◐4✅5◐6◐ → **7 (header/footer)** → 8 (izolacja CSS) → 9 (raport). Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 4 full-stack ZAMKNIĘTY (rozmiar strony + orientacja)**: pełny round-trip DOCX→reader→`pageSize`→front→zapis→writer `w:pgSz`. Backend: `PageSize` przez 3 komendy/handlery/kontrolery (Save/Sign/Download). Front: `document.model.ts` (`PageSize`), sygnał `documentPageSize` (load + `buildSaveRequest`), 2 testy Vitest. Backend **358 pass**, GUI **166 pass**, build OK. R-14 zamknięte. Kolejność: 0✅1✅2✅3◐4✅5◐ → **6 (obrazy)** → 7 (header/footer) → 8 (izolacja CSS) → 9 (raport). Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 4 backend (rozmiar strony + orientacja)**: model `PageSize` (cm + portrait/landscape) w `DocumentContent`/`SaveDocumentRequest`/`SignDocumentRequest`; reader `ExtractPageSize` z modelu `PageSettings`; writer `Convert(... PageSize? pageSize)` emituje `w:pgSz`(+`Orient`) z fallbackiem A4 (bez regresji). Cała solucja **358 pass / 0 fail / 6 skip** (+3 `PageSizeRoundTripTests`). Interfejs `IHtmlToDocxConverter` rozszerzony (opcjonalny param → mocki Moq zaktualizowane). **Pozostaje full-stack:** przewlec `PageSize` przez komendy/DTO + front (model/editor/ruler/scss/Vitest), by zapis round-tripował rozmiar (R-14). Kolejność: 0✅1✅2✅3◐4◐(backend) 5◐ → front Etapu 4 → 6 → 7 → 8 → 9. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 3 (style znakowe)**: `ConvertRunToHtml` rozwiązuje `w:rStyle` — run dostaje CSS nazwanego stylu znakowego (z dziedziczeniem `basedOn`) pod formatowaniem bezpośrednim (direct wygrywa). Naprawia runy formatowane wyłącznie stylem znakowym (Hyperlink/Strong/własny), które wcześniej traciły formatowanie. **73/73 Infrastructure** (+golden `CharacterStyleRun`); istniejące snapshoty niezmienione. Pozostaje w Etapie 3: pełny computed-style (docDefaults folding, numbering/linked styles, theme). Kolejność: 0✅1✅2✅3◐ → **4 (page size full-stack)** → 5◐ → 6 → 7 → 8 → 9. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 2 (model pośredni, read-side, strangler)**: nowy `Infrastructure/DocxModel/` — `PageSettings` (geometria strony/sekcji w surowych twipsach + page size + orientacja) + `SectionPropertiesReader` (czysty parser `sectPr`→model). `DocxToHtmlConverter` liczy margines i wysokość pasma nag/stopki z modelu (`ComputeBandHeightCm`), zachowanie **1:1** (golden-snapshoty niezmienione). Page size/orientacja już parsowane (fundament Etap 4). **72/72 Infrastructure** (+5 testów modelu). Kolejność realizacji: 0✅ 1✅ 2✅ → **3 (style resolver)** → 4 (page size full-stack) → 5(tabele częściowo✅) → 6 → 7 → 8 → 9. ADR-0009. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 5 (tabele)**: `ConvertTableToHtml` emituje `<colgroup>` z `tblGrid` + `table-layout:fixed` (gdy `tblLayout=fixed`/jawna szerokość; fallback szerokości = suma kolumn grid) → przeglądarka honoruje geometrię kolumn Worda. Naprawiony bug `rowspan` przy `vMerge` sąsiadującym z `gridSpan` (dopasowanie wg pozycji kolumny w gridzie, nie indeksu) + obsługa jawnego `val="continue"`. Writer ignoruje `<colgroup>` (czyta `.//tr`, odtwarza grid) → round-trip bez regresji. **67/67 Infrastructure** (+golden `MergedCellsTable`). Pozostaje: cellMargins/shading/cellSpacing, fixed na zapisie. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 0+1** (fundament wierności z Wordem; plan: `~/.claude/plans/fancy-spinning-wirth.md`). **Etap 1**: nowy `Infrastructure/Conversion/OoxmlUnits.cs` — jedno źródło prawdy dla jednostek OOXML (twips/EMU/half-points/cm/px, nazwane stałe, DPI=96). Oba konwertery (`DocxToHtmlConverter` 2950 l., `HtmlToDocxConverter` 2525 l.) przekierowane na `OoxmlUnits`; usunięte zduplikowane helpery i magiczne stałe (`567`/`1440`/`914400`/`9525`/`/20`/`*0.75`/`/2`) — reguła 14 spełniona. Zachowanie liczbowe niezmienione (lokalny rounding zostaje); cm liczone dokładnym `1440/2.54` zamiast przybliżenia `567`. **Etap 0**: harness regresji `Infrastructure.UnitTests/Golden/` — `GoldenDocuments` (5 deterministycznych DOCX in-memory), `HtmlSnapshot` (normalizacja base64/`data-image-id`/daty, approve-on-missing → `__snapshots__/*.approved.html`), `GoldenSnapshotTests` (snapshoty + asercje punktowe jednostek). **66/66 Infrastructure pass** (54+7+5), build solucji OK, snapshoty stabilne 2×. Kontrakt `DocumentContent`/API bez zmian, front nietknięty. **Następny: Etap 2** (jawny model pośredni read-side, strangler) — duża zmiana, wymaga decyzji. ADR-0008. Wcześniej:

2026-05-29 — Word-like obraz: **7 trybów zawijania + obramowanie + przycinanie** (round-trip `a:ln` + `a:srcRect`). Panel: 7 trybów wg menu Worda (5 działa: Równo z tekstem / Ramka / Góra i dół / Za tekstem / Przed tekstem; 2 disabled: Przylegle / Na wskroś — wymagają reflow tekstu, niedostępne natywnie w HTML/CSS). Border section: checkbox+color+grubość 1–20 px+styl ciągła/przerywana/kropkowana. Crop section: 4 inputs % (0–95) + Resetuj. `wysiwyg-editor` extended: `setSelectedImagePositionMode` obsługuje `square` (`float: left`) i `topBottom` (`display: block; clear: both`), nowe `setSelectedImageBorder` (inline CSS border + persist `data-border-*`), `setSelectedImageCrop` (`clip-path: inset()` + persist `data-crop-*`), `resetSelectedImageCrop`. `wrapExistingImages` przywraca po loadzie square/topBottom/border/crop z data-attrs. Backend writer (`HtmlToDocxConverter`): `data-border-*` → `a:ln`+`a:solidFill`+`a:prstDash` (`solid`/`dashed`/`dotted` mapping), `data-crop-*` → `a:srcRect` (1/1000 %). Reader (`DocxToHtmlConverter`): czyta `a:ln`/`a:srcRect` → `data-border-*`/`data-crop-*`. Tests: nowy `ImageBorderCropRoundTripTests` **7/7 pass** (border 3 style + bad-color fail-safe + crop 2 cases + plain-image guard) + 4/4 floating round-trip + 4 nowe testy panelu (7 modes z disabled, 5 emisji, border toggle, crop clamp, resetCrop). GUI **164/164**, Backend **329/329** (Infrastructure 50 +11 nowych). Wcześniej:

2026-05-29 — Word-like floating: **Przed/Za tekstem** + drag w obrębie strony + **round-trip `wp:anchor`**. Panel boczny: 3 przyciski trybu z aktywnym podświetleniem (`W tekście` / `Przed` / `Za`); `ImageSelectionState.positionMode` + `@Output() positionModeChange`. Editor: `setSelectedImagePositionMode()` (przy floating: `position: absolute`, `z-index` 10/−1, persist `data-x/y-px/-emu`); drag-aware mousedown (floating → update left/top zamiast DOM reinsert); `wrapExistingImages` przywraca floating po imporcie. CSS: `.editor-image-wrapper[data-pos-mode="front"]` z=10, `[data-pos-mode="behind"]` z=−1; edytory dostają `position: relative; z-index: 0` (kontekst stacking). **Backend**: `HtmlToDocxConverter` emituje `wp:anchor` (positionH/V relativeFrom=page + offsety z `data-x/y-emu` + `wp:wrapNone` + `BehindDoc`) gdy `data-pos-mode` set; `DocxToHtmlConverter` czyta `wp:anchor` → `data-pos-mode`/`data-x-emu`/`data-y-emu` na `<img>`. Tests: **4 nowe `ImageFloatingRoundTripTests` pass** (front/behind round-trip pozycji+trybu, inline-guard, size+pozycja); GUI **162/162 pass** (+2 testy panelu). **Pozostaje**: wrap modes square/tight/through (text reflow — niedostępne natywnie w HTML), drag między stronami/sekcjami. Wcześniej:

2026-05-29 — Word-like pozycjonowanie obrazów (MVP): czwarty docked panel **`d2-image-properties-panel`** (Rozmiar/Wyrównanie/Opływanie info-only/Usuń) w slocie Wyszukiwanie/Tabele/Nagłówek+Stopka. Stateless — wszystkie akcje przez nowe public methods na `wysiwyg-editor`: `setSelectedImageWidth/Height(px, lockAspect)`, `setSelectedImageAlignment(L/C/R/null)`, `resetSelectedImageAspect()`, `removeSelectedImage()`. Nowy `@Output() imageSelectionChange` emitowany przy select/deselect/resize-end/drop-end. Sygnał `selectedImage` + `imageLockAspect` (default true) w `document-editor`; `showImagePanel = selectedImage()!=null && !showFindReplace() && !showTablePanel()`; **panel obrazu ma pierwszeństwo nad HF panelem** (rozsądny UX: formatowanie logo w nagłówku w trakcie edycji). Round-trip rozmiaru przez istniejące `data-*-emu` na `<img>`; alignment przez `text-align` na akapicie. Drag/resize mechaniki bez zmian — tylko emit snapshot po każdej operacji. Tests: **160/160 pass** (+9 panel, +1 koordynacja). **Ograniczenie MVP**: floating wp:anchor (square/tight/behind/in-front wrap) odłożone — panel pokazuje sekcję info-only „W tekście (inline). Floating w przygotowaniu." Wcześniej:

2026-05-29 — Reguła `userDownload` (kontrola pobierania edytowanego pliku). Opcjonalne pole w `documents.metadata` JSON: default **false**; brak/null/non-true ⇒ blokada; tylko jawne `true` zezwala. **Local upload** (`UploadDocumentCommandHandler`) **automatycznie** ustawia `userDownload=true` (anti-tamper — system, nie klient). **External ingest** (`POST /api/v1/document`) dostaje opcjonalne `UserDownload: bool?`. Nowy gated endpoint `POST /api/documentstorage/{masterId}/user-download` (`DownloadEditedDocumentCommand` + handler) → 403 gdy off (sentinel `USER_DOWNLOAD_FORBIDDEN:`). Frontend: sygnał `userDownload` + `canUserDownload` computed; pozycja menu „Pobierz dokument" gated; `downloadDocument()` POST na nowy endpoint z obsługą 403. Wspólny `ExternalDocumentMetadata` (parser+serializer) zastąpił 4 lokalne kopie. DTO metadanych/statusu eksponują flagę. **325/325 backend pass, 150/150 GUI pass**. **Ograniczenie**: `GET .../download` i `GET .../versions/{vid}/download` (load do edytora) celowo nie gated — zwracają ostatnio zapisaną wersję, nie aktualny stan edytora; do follow-up. Wcześniej:

2026-05-29 — External API: 3 nowe endpointy (callback URL / unlock / status). Wszystkie na **`masterGuid`** (returnUrl w master metadata; brak osobnego user-locka — `Editing` to lock; status = atrybut master). Implementacja: domena (`Document.MarkSaved()` + `Document.UpdateMetadata()`); 2 commands + 1 query w `Application/Features/Documents/`; 3 akcje w `D2ServicesViewerEditor.Api/Controllers/DocumentController` (`PUT .../callback-url`, `POST .../unlock`, `GET .../status`). Walidacja URL współdzielona z `DocumentDelivery.IsValidRecipientUrl`. `callbackUrl` nigdy nie surfacowany w response (`hasCallbackUrl: bool`) ani w logach (token-safe). Idempotencja: callback-url (zapis tego samego URL), unlock (Saved → `Changed=false`). Stany wysyłki blokują obie zmiany (409). Testy: +25 (Application UnitTests); pełna solucja **305/305 pass**. `.ai/API_CONTRACTS` rozszerzony o tabelę endpointów + uzasadnienie decyzji identyfikatora. Wcześniej:

2026-05-29 — Menu „Pomoc" + status autozapisu w stopce: (a) nowa zakładka menu „Pomoc" po „Widok" z jedną pozycją „Zgłoś" delegującą do istniejącego `openReportEmail()` (bez duplikacji); sygnał `showHelpMenu` + `toggleHelpMenu()` + `closeAllMenus` rozszerzony; **stary przycisk „Zgłoś" usunięty** z `.header-right` (toolbar czystszy). (b) **Data/czas autozapisu przeniesione z toolbara do stopki** — `.autosave-status` (min-width:70px) usunięty; przy switchu zostaje tylko switch + label „Autozapis"; w `.status-right` nowy `.status-autosave` („Autozapis: HH:MM:SS" / „zapisywanie…" / „błąd zapisu" / „włączony"; ukryty gdy off). Bez zmian w logice (te same sygnały `autoSaveEnabled`/`autoSaveStatus`/`lastAutoSaveAt`). Testy: +4 w `document-editor.spec.ts`, **146/146 pass**. Wcześniej:

2026-05-29 — Porządek panelu nagłówka/stopki: (a) **usunięty primary CTA „Zamknij nagłówek i stopkę"** z `d2-header-footer-panel` (redundantny z X i ESC) — X jako jedyne zamknięcie z panelu (spójne z `Wyszukiwanie`/tabela); (b) **NG8107**: 9× `editor?.` → `editor.` w bindingach panelu w `document-editor.html` (`editor` jest `!:`, panel renderuje się dopiero po `editingSection() !== 'body'` emitowanym przez edytor — operand nigdy nie null). Tests **142/142**, `ng serve` czysty. Wcześniej:

2026-05-29 — 4 UX poprawki: (1) **autozapis stabilny** — `.autosave-status` rezerwuje przestrzeń (min-width:70px) niezależnie od stanu toggle, więc Zgłoś/Zapisz/Zakończ nie skaczą; (2) **prowadnice marginesów domyślnie ukryte** (`showMarginGuides=false` w obu komponentach); (3) **obraz w trybie edycji nagłówka NIE jest reskalowany** — `wrapExistingImages` nie usuwa już inline width/height; CSS `.editor-image-wrapper img` bez `width:100%`; obraz wygląda tak samo w preview i edit; (4) **pływający pasek nagłówka/stopki USUNIĘTY** — nowy komponent `d2-header-footer-panel` (dock po lewej, ten sam slot co Wyszukiwanie / Tabela), stateless, wszystkie akcje delegowane do `wysiwyg-editor` (Inna pierwsza strona / Wstaw obraz / Numery stron / Format / Usuń / **Primary „Zamknij nagłówek i stopkę"**). Koordynacja single-mode: `showHeaderFooterPanel = computed(() => editingSection()!='body' && !showFindReplace() && !showTablePanel())` — Find/Tables mają pierwszeństwo. Testy `header-footer-panel.spec` (6) + `document-editor.spec` +5 koordynacja. `npm test` **142/142 pass**. **Manual verify**: top-margin drag w trybie edycji nagłówka (overlay usunięty, ale interakcja z `d2-ruler` wymaga weryfikacji w przeglądarce). Wcześniej:

2026-05-28 — Faza 2: audyt trybu edycji nagłówka/stopki. Naprawione realne gapy: (a) **routing wariantu** — `startEditingHeader/Footer` + `onHeaderInput/Footer` + `onHeaderBlur/Footer` zapisują/ładują wariant aktywny na stronie 0 (`firstPageHtml` gdy `differentFirstPage`, inaczej `_headerHtml`); wcześniej edycja first-page niewidocznie nadpisywała default (łamanie rule 10); (b) `_computeHeaderContent/Footer` w trybie odd/even używa kanonicznego `_headerHtml/_footerHtml` dla odd (= default w OOXML); (c) **ESC** w `document-editor.ts` zamyka tryb edycji nag/stopki (priorytet nad bocznymi panelami); (d) przycisk **„Zamknij nagłówek i stopkę"** w obu toolbarach (spec funkcjonalny #3). Nowy `wysiwyg-editor.spec.ts` (7) + `document-editor.spec.ts` (+4 ESC). `npm test`: **131/131 pass**. Ograniczenie: edycja even-page nieosiągalna z UI (template renderuje contenteditable tylko dla strony 0; even round-tripuje z importu). Wcześniej:

2026-05-28 — Faza 1b: pomiary wierności importu nag/stopki na `stupki.docx`. Nowy `DocxToHtmlConverterFidelityTests.cs` (9): geometria (Height ≈ 1.25 cm, Margins ≈ 2.5 cm), font-family z `docDefaults`, image EMU 133×34 px + aspect (4:1 stays 4:1) + `data-width-emu`/`data-height-emu` do round-tripu, L/C/R alignment. Realny plik nie odsłonił nowych gapów na T1–T12. Pełny `Infrastructure.UnitTests` **43/43 pass**. R-11 pozostaje (brak sygnału z tego pliku). Wcześniej:

2026-05-28 — Round-trip first/even header/footer (zapis): `HtmlToDocxConverter` zrefaktorowany — `WriteHeaderPart`/`WriteFooterPart(html, type)` tworzą osobne party Default/First/Even + `HeaderReference`/`FooterReference` z `Type`; `EnsureTitlePage` (do sekcji) i `EnsureEvenAndOddHeaders` (do `settings.xml`). 6 testów round-trip w `HeaderFooterRoundTripTests` (write → read potwierdza pola `DifferentFirstPage`/`FirstPageHtml`/`DifferentOddEven`/`EvenHtml`). Pełny `Infrastructure.UnitTests` **34/34 pass**. Edycja wariantów first/even jest teraz trwała (reguła 10). Wcześniej:

2026-05-28 — Import nagłówka/stopki (Faza 1, fidelity): `DocxToHtmlConverter.ExtractHeader/ExtractFooter` wybierają part **default** wg `sectPr`/`HeaderReference`/`FooterReference` (nie `FirstOrDefault()`, który mógł zwrócić pusty even/first), **first-page** przez `titlePg` → `DifferentFirstPage`/`FirstPageHtml`, oraz **even/odd** przez `settings/evenAndOddHeaders` → `DifferentOddEven`/`EvenHtml` (model domeny rozszerzony o te 2 pola). Front (`document-editor.ts`) spread'uje cały obiekt header/footer na obu ścieżkach load. `.ai/FEATURES.md` uzupełnione (sekcja nagłówek/stopka). Testy backendu **13/13** (`DocxToHtmlConverterSectionReferenceTests` 8 + Header/Footer 5), `tsc --noEmit` OK. R-10 częściowo zamknięte (**pozostaje: round-trip first/even na zapisie + multi-section**). Diagnoza na `stupki.docx`. **Uwaga:** w drzewie roboczym są też 4 niezacommitowane poprawki GUI (env init via `provideAppInitializer`, switch „Autozapis", widoczność „Zakończ" wg `returnUrl`, niższe nagłówki/stopki dialogów) — patrz niżej. Wcześniej:

2026-05-27 — 4 poprawki GUI: (1) admin nazewnictwo `Wysyłki`→`Pliki do wysłania` (`admin-shell`/`admin-deliveries`); (2) dok boczny `syncTablePanel` — ponowne zaznaczenie tabeli przy otwartym wyszukiwaniu przełącza dok na formatowanie tabeli (zamyka search; jeden aktywny tryb; nadpisuje ADR-0006); (3) menu „Wstaw" — QR/Linia pozioma/Podział strony owinięte `@if (false)` (ukryte, logika zostaje); (4) toolbar — przycisk „Akapit" (`editor-toolbar` `@Output openParagraph` → istniejący `openParagraphDialog()`, ta sama ikona co menu „Narzędzia", bez duplikacji). Testy 101/101 (nowe: `admin-shell.spec` 3, toolbar +1, document-editor +5). Build OK. Wcześniej:

2026-05-27 — Globalne bannery górne: nowy kontener `d2-global-banners` w `App` (flow flex, kolejność: środowisko → offline/API), banner środowiska `d2-environment-banner` (źródło: `BuildInfoService.environment()`; mapowanie w `core/utils/environment-banner.util.ts` — Local niebieski / DEV zielony / UAT,TST jasnożółty / PRE purpurowy / PROD,PRD ukryty / nieznane fallback). Pasek braku API (`offline-banner.scss`): usunięty arbitralny `z-index:10000`, w flow → nie przykrywa edytora; logika `ConnectionStatusService` bez zmian. **Uwaga:** brak `fileReplacements` w `angular.json` → `environment.ts` zawsze `PRD`, dlatego env bierzemy z health-check API. Naprawiony stary scaffold `app.spec.ts`. Testy: 92/92 (nowe: util 10, env-banner 10, global-banners 4). Build OK. Wcześniej:

2026-05-27 — ESC zamyka aktywny boczny panel (Wyszukiwanie / właściwości+stylizacja tabeli) tą samą logiką co × — `document-editor.ts` `@HostListener('document:keydown.escape')` → `closeActiveSidePanel()` (deleguje do `closeFindReplace`/`closeTablePanel`); guardy: `event.defaultPrevented` (deselekcja obrazu w edytorze ma pierwszeństwo), otwarty dialog/menu kontekstowe, brak otwartego panelu → no-op; focus wraca do edytora tylko gdy był w panelu. Przycisk „Cofnij" (undo) zweryfikowany — działa, ten sam `undo()` co Ctrl+Z; operacje tabeli wchodzą do historii przez `notifyEditorChange()`→`input`→`onContentChange`→`saveToUndoStack`. Nowe testy: `editor-toolbar.spec.ts` (6) + `document-editor.spec.ts` (7), 13/13 OK. Build OK. Wcześniej:

2026-05-27 — Fix nagłówka/stopki DOCX→HTML: kontener `.header-footer-content` z domyślnym rozmiarem/krojem z `docDefaults` (backend `DocxToHtmlConverter`) + fallback CSS frontu (11pt, `#000`) — naprawia „za duży tekst" i zgubiony kolor. Testy backendu 5/5. Otwarte: R-10 (first-page/even-odd/wiele sekcji — bierzemy tylko pierwszy header part), R-11 (round-trip rozmiaru). Wcześniej:

2026-05-27 — Bugfix cyklu życia aktywnej tabeli: `detectTableContext` nie czyści tabeli przy selekcji poza edytorem (klik w panel boczny ≠ opuszczenie tabeli; model „last known context" w `core/utils/table-context.util.ts`), podpięto `selectionChange` edytora (klik akapitu poza tabelą czyści kontekst + wizualne zaznaczenie komórek). Build OK; `ng test` 55 passed. Wcześniej:

2026-05-27 — Auto-zakres obramowań: usunięto ręczny wybór celu; zakres wnioskowany z zaznaczenia (`classifyBorderTarget`: komórka/wiersz/kolumna/tabela/zakres), panel pokazuje podpis „Zastosowanie: …". Domyślnie (sama karetka) → aktywna komórka. Build OK; `ng test` 49 passed. Wcześniej:

2026-05-27 — Szczegółowy edytor obramowań tabeli (zakładka „Obramowania" w `d2-table-properties-panel`): rodzaj linii (ciągła/przerywana/kropkowana/podwójna/brak), grubość, kolor (paleta+picker+reset), 10 ikon miejsca (wszystkie/brak/zewn./wewn./poziome/pionowe/góra/dół/lewa/prawa) oraz **cel: cała tabela / komórka / zaznaczenie**. Galeria presetów usunięta. Logika = `core/utils/table-style.util.ts` (`applyBorderToCells` względem prostokąta zbioru komórek); styl inline (ADR-0007). Build OK; `ng test` 42 passed. Wcześniej:

2026-05-27 — Stylizacja tabel w panelu `d2-table-properties-panel`: zakładki „Układ"/„Style", gotowe style (`TABLE_STYLE_PRESETS`), obramowania (kolor/grubość/zakres), wiersz nagłówka, wiersze naprzemienne, pierwsza/ostatnia kolumna, reset. Logika = czyste funkcje `core/utils/table-style.util.ts`; styl jako inline na `<table>/<tr>/<td>` (przeżywa zapis HTML→DOCX — ADR-0007). Bez zmian backendu. Build OK; `ng test` 41 passed. Wcześniej:

2026-05-27 — Konfiguracja tabeli przeniesiona z poziomego paska `.table-toolbar` do bocznego panelu `d2-table-properties-panel` (dokowany po lewej, spójny z panelem „Wyszukiwanie"; auto-otwierany po kliknięciu tabeli, wyszukiwanie ma pierwszeństwo w doku — ADR-0006). Naprawiono uruchamianie testów GUI (`angular.json` cel `test` + `src/test-setup.ts` z Zone.js). Build OK; `ng test` panel 6/6 (2 stare scaffoldowe `app.spec.ts` padają niezależnie). Wcześniej:

2026-05-27 — funkcja „Wklej tylko tekst" w edytorze WYSIWYG: util `core/utils/paste-text.util.ts` (htmlToText/normalizeWhitespace/resolvePlainText + 17 testów) i skrót `Ctrl/Cmd+Shift+V` w `wysiwyg-editor.ts`. Build + vitest OK. Otwarte: słaby regexowy `sanitizeHtml` (R-09, rekomendacja DOMPurify), brak przycisku toolbara/menu. Wcześniej:

2026-05-25 — panel admina `/admin/deliveries` (lista wysyłek + filtry + retry + `locked_by`, statusy PL) + uruchomienie migracji `infra/sql/001..007` na lokalnym PostgreSQL (Podman). Wcześniej: funkcja „Zakończ i wyślij" (kolejka `document_deliveries` + worker) + audyt/synchronizacja `.ai/` z kodem.

## Aktualny cel prac

Domknięcie przepływu Krok 1–4 (ingest → podgląd → edycja → zakończ i wyślij). Krok 4 zaimplementowany; Krok 2 (podgląd v1) wciąż w toku.

## Aktualny etap

- Branch: `feature/baz-astral-obciaga-mb`
- Główne obszary kodu:
  - `D2ApiViewerEditor/` (internal API + Application/Domain/Infrastructure)
  - `D2ServicesViewerEditor/` (external ingest API)
  - `D2GuiViewerEditor/` (Angular)
  - `infra/sql/` (schemat)

## Co zostało zrobione (ta i poprzednie sesje)

- Ingest zewnętrzny: `IngestExternalDocumentCommand` + endpoint `POST /api/v1/document` (DOCX → v1+v2, PDF → v1).
- Płaski zapis: `Document.UpdateVersion` + `UpdateDocumentVersionCommand` + `PUT /api/documentstorage/{masterId}/versions/{versionId}` (nadpisanie GCS pod tym samym versionId).
- `DocumentVersion.ModifiedAt` + skrypt `infra/sql/005_add_version_modified_at.sql` + mapowanie EF.
- Endpoint metadanych: `GetDocumentMetadataQuery` + `GET .../{masterId}/metadata`.
- GUI: mechanizm auto-save (timer, `environment.autoSave { enabled, intervalSeconds: 30 }`), switch „AutoSave", sygnały `documentVersionId/autoSaveEnabled/autoSaveStatus/lastAutoSaveAt`.
- GUI: ujednolicony „Zapisz" (API, PUT/POST) + „Pobierz dokument" (dawne „Zapisz" = download lokalny). Usunięto `saveDocumentAs()`.
- Dokumentacja `.ai/` przepisana wg wzorca z `template/`.
- „Zakończ i wyślij": `FinishAndSendDocumentCommand` + tabela `document_deliveries` (SQL 007) + worker `DocumentDeliveryWorker` (claim `FOR UPDATE SKIP LOCKED`, retry/backoff do 24h, snapshot GCS) + endpointy `finish`/`deliveries`/`retry` + GUI `finishDocument()` z pollingiem. Patrz `DECISIONS.md` ADR-0005.
- `documents.status` (`DocumentStatus`) + skrypt SQL 006 (dodane przed tą sesją, teraz udokumentowane).
- Panel admina `/admin/deliveries` (`admin-deliveries`): lista zadań wysyłki (filtr per kolumna + dropdown statusu + paginacja, spójny z `/admin/files`), `locked_by`/`locked_until`, przycisk „Ponów" (`POST deliveries/{id}/retry`), statusy tłumaczone na PL. `DeliveryListItemDto` rozszerzony o `LockedUntil`/`LockedBy`.
- Migracje `infra/sql/001..007` wykonane na lokalnym PostgreSQL (Podman `d2viewereditor_postgres`); `007` utworzył `document_deliveries`.
- Kontrola dostępu do dokumentu (`allowedCorporateKeys`): `DocumentAccessPolicy` (Domain) + `IDocumentAccessGuard` (App). Egzekwowanie → 403 w handlerach treści/metadanych. GUI: `documentAccessGuard` + `DocumentAccessDeniedComponent` (`/access-denied`). Brak migracji DB (klucze w `metadata`).
- **Microsoft Entra ID (docelowo, ADR-0011):** backend JwtBearer + policy `RequireAppOperator`/`RequireAppAdmin`, `[Authorize]` na `BaseApiController` (cała aplikacja za logowaniem), endpointy admina → `RequireAppAdmin`, `ClaimsCurrentUserProvider` (CorporateKey z claimu access tokena), `Administrator` omija `allowedCorporateKeys`. Front: MSAL (`MsalGuard`/`appAdminGuard`/`MsalInterceptor`), sekcja `auth` w `environment`. **Wymaga `dotnet restore` (JwtBearer) + `npm install` (MSAL) + konfiguracji Entra (placeholdery w `appsettings`/`environment`).** Patrz `SECURITY.md`, `DECISIONS.md` ADR-0011.

## Jak uruchomić projekt lokalnie

Patrz `DEVOPS_DEPLOYMENT.md`. Skrót:

```bash
cd D2ApiViewerEditor && dotnet run --project D2ViewerEditor.Api      # 5190
cd D2ServicesViewerEditor && dotnet run --project D2ServicesViewerEditor.Api  # 15112 (swagger: /swagger)
cd D2GuiViewerEditor && npm install && npm start                     # 4200
# Wymaga: PostgreSQL (skrypty infra/sql/ 001..005) + GCS/fake-gcs-server (bucket d2viewereditor-documents)
```

## Jak zweryfikować stan

```bash
dotnet build D2ApiViewerEditor/D2ViewerEditor.sln   # ostatnio: OK (0 błędów)
cd D2GuiViewerEditor && npm run build               # ostatnio: OK
```

## Znane problemy / niedokończone

| Problem | Wpływ | Status |
|---|---|---|
| Tryb podglądu (Krok 2) ładuje aktywną wersję (po DOCX = v2), nie v1 oryginał | Medium | Open — przełączyć GUI na `/download` |
| `finishDocument()` — zaimplementowane: async wysyłka na returnUrl (kolejka `document_deliveries` + worker) | — | Zamknięte |
| `SaveDocumentVersionCommandHandlerTests` oczekuje `UpdateAsync` (płaski zapis go nie woła) | Low | Zamknięte — test asercją `DidNotReceive().UpdateAsync` + `Received(1).SaveChangesAsync` |
| Budżet SCSS `document-editor.scss` przekraczał limit | Low | Zamknięte — `angular.json` `anyComponentStyle` podniesiony (error 48 kB / warn 24 kB) |
| Ręczny „Zapisz" przy włączonym auto-save jest częściowo redundantny | Low | Akceptowalne (wymuszenie zapisu) |
| Port External API potwierdzony: 15112 (`appsettings.json` → `Urls`) | — | Zamknięte |

## Ostatni bezpieczny punkt

Build backendu i GUI przechodzą po zmianach auto-save / „Zapisz/Pobierz".
