# Current State

> Plik krótki i aktualny. Czytaj na początku każdej sesji.

## Ostatnia aktualizacja

2026-06-26 — **corpKey ustalany wyłącznie po stronie API (token), bez transportu z GUI**. Komendy `SaveDocumentVersion`/`UpdateDocumentVersion`/`FinishAndSendDocument` straciły parametr `CorporateKey`; handlery czytają `_currentUser.CorporateKey` (claim z tokenu) i zwracają `Result.Failure`, gdy brak. Controller DTO `SaveDocumentVersionRequest` bez pola `CorporateKey`. GUI `document-editor.ts` nie odczytuje już claimu `corpKey` z `idTokenClaims` i nie wysyła `corporateKey` (zapisy = `{ content }`). Powód: snapshot `getActiveAccount().idTokenClaims` bywał null → `SaveDocumentVersionCommand.CorporateKey` null; token wysyłany do API i tak niesie `corpKey`, więc `_currentUser` jest niezawodnym źródłem. Wyświetlanie `corporateKey` na listach admina (z `DocumentDelivery`) bez zmian. Build backend 0 błędów; `Api.UnitTests` **113/113**, `Application.UnitTests` **295/295**; GUI build OK. Decyzja: **ADR-0021**. Wcześniej:

2026-06-24 — **corpKey z Entra ID przy zapisach z edytora + kolumna „Kto modyfikował" w administracji**. GUI (`document-editor.ts`) odczytuje claim `corpKey` z `idTokenClaims` aktywnego konta MSAL (sygnał `corporateKey`) i przekazuje go w body przy `saveDocumentVersion`/`updateDocumentVersion` (auto-save + nowy dokument) oraz `finishAndSend`; `SaveDocumentVersionRequest` (GUI) +`corporateKey?`. API: controller DTO i komendy `SaveDocumentVersion`/`UpdateDocumentVersion`/`FinishAndSendDocument` +`CorporateKey` (opcjonalne, fallback do claimu `_currentUser.CorporateKey`). Domena: `Document.LastModifiedBy` (+`SetLastModifiedBy`, aktualizuje tylko jedną kolumnę jak `MarkEditing`), kolumna `documents.last_modified_by` (migracja **`infra/sql/011`**), mapowanie EF w `DocumentConfiguration`. Listy admina: `DocumentListItemDto.LastModifiedBy` (z `Document`), `DeliveryListItemDto.CorporateKey` (reużycie istniejącego `DocumentDelivery.CorporateKey`); nowa kolumna „Kto modyfikował" + filtr w `admin-files` (`lastModifiedBy`) i `admin-deliveries` (`corporateKey`). Build backend 0 błędów; `D2Api.Api.UnitTests` **113/113**, `Application.UnitTests` **295/295**; GUI build OK; `ng test` **268/269** (jedyny fail `spec-layout-shell.spec` — pre-existing, niezwiązany). TODO deploy: zastosować SQL `011` na środowiskach. Wcześniej:

2026-06-22 — **Grafiki: usunięcie widocznego placeholdera (EMF/WMF/TIFF) + cache + nowe formaty**. `GraphicConversionService` przepisany na czytelny łańcuch strategii (`Execute`→per-format) z raportowaniem strukturalnym (`AttemptedStrategies`/`FailureReason`/`CacheKey`/`SourcePath`). **Koniec szarego placeholdera „… — podgląd w Word"**: metafile bez osadzonego rastra → **przezroczysty pusty SVG** (`IsBlankFallback`, wymiary z layoutu), oryginał pass-through do DOCX (Word renderuje wektor). Dodano deduplikację po SHA-256 (bounded cache), detekcję/obsługę **TIFF**(→PNG/Skia)**, WEBP/ICO** (web-native), raster-rescue Skia dla Unknown. `WebGraphicRepresentation.IsPlaceholder`→`IsBlankFallback`; atrybut `data-legacy-graphic="blank"`; reader obejmuje TIFF. Decyzja: **ADR-0020**. Testy: GraphicConversion **35/35**, Infrastructure.UnitTests **188/188**, `dotnet build sln` 0 błędów. Ograniczenie: czysty wektor EMF/WMF nadal nie rasteryzowany w web (sidecar = roadmapa). Wcześniej:


2026-06-22 — **Security hardening upload + returnUrl (central policy services)**. Dodano centralne serwisy w Application: `IReturnUrlValidator` (`ReturnUrlValidator` + `ReturnUrlSecurityOptions`) oraz `IFileUploadSecurityService` (`FileUploadSecurityService` + `UploadSecurityOptions`) z kontrolą: spójność extension↔MIME↔magic-bytes, inspekcja archiwum DOCX (zip-slip, limity entry/uncompressed/compression ratio, wymagane part-y OOXML, blokada VBA), sanitizacja MIME dla obrazów i abstrakcja skanera malware (`IFileScanner`, `NoOpFileScanner`, fail-closed policy). Handlery podpięte do centralnych reguł: `UploadDocument`, `UploadImage`, `IngestExternalDocument`, `UpdateCallbackUrl`, `UpdateDeliveryRecipientUrl`, `FinishAndSendDocument`. Hosty (`D2Api` i `D2Services`) bindują sekcje `Security:Upload` i `Security:ReturnUrl`. Dodano testy security: `ReturnUrlValidatorTests`, `FileUploadSecurityServiceTests` + aktualizacje istniejących testów handlerów pod nowe zależności. Wynik walidacji: Application.UnitTests **295/295**, D2Api.Api.UnitTests **110/110**, D2Services.Api.UnitTests **39/39**.

2026-06-22 — **D2Services: domknięcie observability do standardu ELK/GCP**. W `Program.cs` podłączono `UseRequestObservability()` + `UseExceptionHandlingMiddleware()` (pipeline), a `UseSerilogRequestLogging` wzbogacono o `httpMethod/httpPath/statusCode/requestId/correlationId`. Dodano `Middleware/RequestObservabilityMiddleware.cs` (propagacja `X-Correlation-ID`, scope + Serilog `LogContext`). `ExceptionHandlingMiddleware` rozszerzony o `correlationId` w ProblemDetails oraz serializację runtime-type (`ValidationProblemDetails.errors`). `GcpJsonSerilogFormatter` ujednolicony do standardu D2Api: `severity+level+service+environment+traceId/spanId+flatten properties`. Testy D2Services.UnitTests: **28/28 pass** (nowe testy dla RequestObservability, MiddlewareExtensions, formattera i correlation/errors w exception middleware). Wcześniej:

2026-06-21 — **D2Services: dodatkowe testy Health + MiddlewareExtensions**. W nowym projekcie `D2ServicesViewerEditor.Api.UnitTests` dodano: `HealthControllerTests` (payload `/api/health` i `/api/health/detailed`) oraz `MiddlewareExtensionsTests` (`UseExceptionHandlingMiddleware` zwraca ten sam builder). Wynik projektu testowego: **22/22 pass** (wcześniej 19). Wcześniej:

2026-06-21 — **Zwiększenie pokrycia testami jednostkowymi: D2Api + D2Services**. Dodano **nowy projekt** `D2ServicesViewerEditor.Api.UnitTests` (NUnit): testy `DocumentController` (walidacja wejścia, mapowanie statusu), `ExceptionHandlingMiddleware` (mapowania wyjątków), `GcpJsonSerilogFormatter` (severity/category/exception). Wynik: **19/19 pass**. W `D2ApiViewerEditor.Api.UnitTests` dodano 3 testy brzegowe middleware: `RequestObservabilityMiddleware` (odrzucenie whitespace i >128 znaków w `X-Correlation-ID`) + `ExceptionHandlingMiddleware` (propagacja `correlationId` do `ProblemDetails.Extensions`). Wynik pełnego projektu API unit tests: **76/76 pass**. Zmieniono też `D2ServicesViewerEditor.sln` (podpięcie nowego projektu testowego). Wcześniej:

2026-06-21 — **„Zakończ" → synchroniczna wysyłka + statusy + „Pobierz oryginał" + ukrywanie „Ostatnia modyfikacja"**. Cztery zadania:
(1) **Pobierz oryginał dokumentu** — nowa pozycja menu „Plik" widoczna gdy `loadedFromDisk || userDownload`; zwraca ZAWSZE oryginał (v1): plik z dysku trzymany w pamięci (`diskOriginalFile`) albo wersja bazowa z API (`downloadBaseVersion` = `GET {masterId}/download`, już istniał). „Pobierz dokument" (edytowana kopia) bez zmian.
(2/2B) **„Zakończ"** przerobiony z modala-z-timerem na **synchroniczną pierwszą próbę**. `FinishAndSendDocumentCommandHandler`: zapis v2 → snapshot → delivery (Pending) → `document.Queued` → `BeginInlineAttempt` (`document.Sending`) → `IDeliverySender.SendAsync`. Sukces → `delivery.MarkSent` + `document.Sent` (delivered=true). Błąd → `delivery.HoldAfterFailedInlineAttempt` (RetryScheduled „zaparkowane" na DeadlineAt — worker nie przejmie) + `document.DeliveryFailed` (delivered=false). GUI: „Trwa wysyłanie dokumentu ...", potem na błędzie modal „Wystąpiły problemy z dostarczeniem dokumentu" + **„Przerwij"** (`POST /{masterId}/abort-send` → delivery `Cancelled` + `document.SendAborted` + powrót do edytora, bez zamknięcia karty) / **„Kontynuuj wysyłkę w tle"** (`POST /{masterId}/continue-delivery` → `Requeue` + `document.Queued` + zamknięcie karty; worker dokańcza). Nowe `DocumentStatus`: **`Queued`** („Zlecono do wysyłki"), **`SendAborted`** („UzytkownikPrzerwałWysyłkę"); „W trakcie wysyłki"=`Sending`, „Wysłano"=`Sent`, „Błąd wysyłki"=`DeliveryFailed` (istniały). Migracja **`infra/sql/010`** (tylko COMMENT — kolumna bez CHECK). `finish` zwraca teraz **200** `{deliveryId,status,documentStatus,delivered,error?}` (było 202).
(3) **Ostatnia modyfikacja** — w stopce gated `showSaveState()` (wcześniej zawsze widoczna); reguła `showSaveState` rozszerzona o ten element.
(4) **GET `/api/v1/document/{masterId}/status`** — już zwracał wyłącznie `{ masterId, status }` (`DocumentStatusResponse`) — bez zmian, zweryfikowane.
Build: D2Api + D2Services 0 błędów. Testy: Domain **77**, Application **282** (nowe: AbortSend/ContinueDelivery + przepisane FinishAndSend + DocumentDelivery inline), GUI Vitest **267** (document-editor 68, +Pobierz oryginał, przepisany flow „Zakończ"). `ng build` AOT kompiluje się; „failed" wyłącznie z powodu **pre-existing budżetu** bundla (`document-editor.scss` 38 kB — nietknięty). Wcześniej:

2026-06-19 — **`showSaveState` (ingest) — ukrywanie UI zapisu w edytorze**. Nowe opcjonalne pole `ShowSaveState: bool?` w `POST /api/v1/document` (D2Services). Inverse-default: brak/`true` → widoczne (jak dotąd), tylko jawne `false` zapisywane do `documents.metadata.showSaveState` i ukrywa w edytorze switch Autozapis + status w stopce + przycisk „Zapisz". Łańcuch: `CreateDocumentRequest` → metadata JSON → `ExternalDocumentMetadata.ShowSaveState`/`IsSaveStateVisible` (inverse-default `!= false`, pozycyjny param z domyślnym `null` — nie łamie wywołań) → `DocumentMetadataDto.ShowSaveState` → GUI sygnał `showSaveState` (z `meta.showSaveState !== false`). Reguła **prezentacyjna**: autozapis dalej działa w tle, „Zakończ i wyślij" bez zmian. Build D2Api+D2Services 0 błędów; Application metadata 31/31; GUI Vitest **263/263** (+2 `showSaveState`). Docs: API_CONTRACTS (reguła `showSaveState`), CHANGELOG. Wcześniej:

2026-06-13 — **Usunięto autoryzację po Keycloak (dual-auth)**. Zostaje wyłącznie Entra ID. Backend (`D2ApiViewerEditor.Api`): z `Program.cs` wycięta gałąź `if (keycloak.Enabled…)` (policy scheme `EntraOrKeycloak` + drugi `JwtBearer` + `SelectByIssuer`) — zostaje jeden tor `AddAuthentication(JwtBearer).AddMicrosoftIdentityWebApi(...)`. Skasowane pliki: `Security/KeycloakOptions.cs`, `Security/AuthSchemes.cs`, `Api.UnitTests/Security/AuthSchemesTests.cs`. Usunięta sekcja `"Keycloak"` z `appsettings.json` i `appsettings.DEV.json`. Docs zaktualizowane (SECURITY/FEATURES/API_CONTRACTS/RISKS R-25; ADR-0012 oznaczone „Keycloak USUNIĘTY"). Build API OK (0 błędów). Wcześniej:

2026-06-13 — **Lokalny bypass autoryzacji (dev) — praca bez Entra ID**. Po merge auth był bezwarunkowy (`[Authorize]` na `BaseApiController`, `MsalGuard` na trasach) → bez tenanta nie dało się uruchomić apek. Dodano przełącznik (domyślnie OFF, nigdy w prod): **Backend** — `DevAuthHandler` (schemat `DevAuth`, sztuczny principal z rolami `Operator`+`Administrator`) aktywny gdy `Auth:DevBypass=true` **i** `!IsProduction()`; w tym trybie `Program.cs` pomija całe Entra, polityki `RequireApp*`→`RequireAuthenticatedUser`, `ICurrentUserProvider`=`HttpHeaderCurrentUserProvider` (CorporateKey/admin z nagłówków `X-Corporate-Key`/`X-App-Admin`), Graph=Disabled. **Frontend** — `AppAuthConfig.enabled` (+`isAuthDisabled`); gdy `auth.enabled=false`: nowy `authGuard` przepuszcza bez MSAL, `appAdminGuard`→true, interceptor z pustą `protectedResourceMap` (brak tokena). Trasy `MsalGuard`→`authGuard`. **Włączenie lokalnie:** backend — `appsettings.DEV.secrets.json` (poza repo) `{ "Auth": { "DevBypass": true } }`; front — `public/assets/configs/config.json` `{ "auth": { "enabled": false } }` (lokalnie, nie commitować). Build API OK, `ng build` OK, GUI **259/259**, `runtime-config.spec` 4/4. Wcześniej:

2026-06-13 — **Fix serializacji ProblemDetails + domknięcie obserwacji z testów**. `ExceptionHandlingMiddleware` serializował przez statyczny typ bazowy `ProblemDetails` → `ValidationProblemDetails.Errors` nie trafiało do body. Fix: `JsonSerializer.Serialize(problemDetails, problemDetails.GetType(), options)` (typ runtime) → 400 dla błędów walidacji zawiera teraz słownik `errors` zgrupowany po `PropertyName`. Test zaktualizowany na zachowanie docelowe (`...EmitsGroupedErrorsInBody`). Api **51** (+1), reszta bez zmian. Wcześniej:

2026-06-13 — **Pokrycie testami D2ApiViewerEditor: +26 testów (pipeline behaviours, metadata, exception middleware)**. Stan: Domain 67, Application **258** (+19), Api **50** (+7), Infrastructure 164 — wszystko zielone (Integration 6 skip). Pokryto wcześniej-nietestowane klasy pure-logic: `ExternalDocumentMetadata` (parser tolerancyjny null/empty/malformed/json-null, mapowanie pól, case-insensitive web defaults, `IsUserDownloadAllowed` tylko dla jawnego `true`, round-trip serialize↔parse), MediatR `ValidationBehaviour` (brak walidatorów→next, pass→next, fail→`ValidationException`+short-circuit next, agregacja błędów wielu walidatorów), `LoggingBehaviour` (przekazanie odpowiedzi, log wejście+wyjście, brak połykania wyjątku), oraz API `ExceptionHandlingMiddleware` (pass-through, mapowania Validation/Argument→400, KeyNotFound→404, OperationCanceled→400, unexpected→500 bez przeciekania szczegółów, camelCase). **Obserwacja:** middleware serializuje `ValidationProblemDetails` przez statyczny typ bazowy `ProblemDetails` → słownik `errors` nie trafia do body (test asercjonuje realne zachowanie, bez zmiany produkcji). **Niepokryte (świadomie, infra/seam):** `DeliveryAttemptRunner`, `DocumentDeliveryWorker`, `OoxmlAgileDecryptor`, repo/GCS. Wcześniej:

2026-06-13 — **Pokrycie testami D2ApiViewerEditor: +28 testów dla nietestowanych klas**. Stan: Domain 67, Application **239** (+25), Infrastructure **164** (+3) — zielone. Pokryto wcześniej-nietestowane: `GetDocumentsQueryHandler`, `GetDeliveryStatusQueryHandler`, `GetDocumentVersionContentQueryHandler`, `UpdateDocumentVersionCommandHandler` (w tym v1-immutable), `IngestExternalDocumentCommandHandler` (DOCX 2 wersje / PDF 1 / unsupported / empty / db-error) + `IngestExternalDocumentCommandValidator`, oraz **`DigitalSignatureService`** (realny round-trip sign→verify na self-signed cert + wykrycie modyfikacji + brak podpisów). Mock framework: NSubstitute (zgodnie z resztą Application). **Niepokryte (świadomie, wymaga infra/seam):** `DeliveryAttemptRunner` (brak publicznego przejścia w `Sending` — tylko claim SQL → integracja), `OoxmlAgileDecryptor` (brak fixtury zaszyfrowanego OOXML), repo/worker/GCS (DB/GCS), `GcpJsonSerilogFormatter` (brak projektu testowego w Services), `Api.UnitTests` (lock `Infrastructure.dll` gdy API działa). Wcześniej:

2026-06-13 — **Pokrycie testami D2ApiViewerEditor (nowy kod)**. Domain 67, Application **214** (+3), Infrastructure **161** (+3) — wszystko zielone. Dodane: `RequeueDeliveryCommandHandlerTests` (handler „Wznów" był nietestowany), EMF/WMF rasteryzacja (`Wmf_WithEmbeddedDib...ViaGenericScan`, `Emf_WithCorruptDib_FallsBackToPlaceholder...`), `LegacyDocBinaryConverter` Prc-skip. **Uwaga:** `Api.UnitTests` (w tym 8 `GcpJsonConsoleFormatterTests`) nie buduje się, gdy działa lokalna instancja API (lock `Infrastructure.dll`) — środowisko, nie błąd kodu; zatrzymać API i odpalić. **Luki:** `GcpJsonSerilogFormatter` (Services — brak projektu testowego), integracja normalizer→`.doc` (wymaga CFB; konwerter pokryty wprost). Wcześniej:

2026-06-11 — **„Pliki do wysłania": „Kopiuj link" + akcje do prawej + padding szczegółów**. Przycisk „Podgląd edycji" → **„Kopiuj link"**: zamiast nawigować do edytora, buduje pełny link (`origin + serializeUrl(/editor?masterId=&versionId=)`) i kopiuje do schowka (`navigator.clipboard.writeText`; sukces → notice, brak Clipboard API/błąd → actionError z linkiem do ręcznego skopiowania). `openInEditor` → `copyEditLink`. Przyciski akcji wyrównane do prawej (`justify-content:flex-end`). Panel szczegółów dostał oddech: `.delivery-details` margin+padding+ramka, większy gap, separator pod wierszem nagłówka. GUI `admin-deliveries.spec` 14 (testy openInEditor→copyEditLink: link z/bez versionId, clipboard). `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": wiersz nagłówka „Szczegóły" + akcje (70%)**. W panelu szczegółów na górze nowy wiersz pełnej szerokości `detail-header-row` (grid 30%/70%): label „Szczegóły" w 1. kolumnie, wszystkie akcje (Podgląd edycji / Edycja metadanych / Wznów / Anuluj, zależne od statusu) w 2. kolumnie zajmującej 70% (przyciski poziomo, zawijane). „Adres odbiorcy" przeniesiony na dół — tuż **nad „Komunikat błędu"** (oba pełna szerokość). Usunięty `detail-span2`. GUI `admin-deliveries.spec` 14, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": akcje w szczegółach + fix znikającej tabeli**. (BUG) Błąd akcji Wznów/Anuluj ustawiał `error()`, który w szablonie był w gałęzi `@else if` → **cała tabela znikała**. Fix: osobny sygnał `actionError` (baner NAD tabelą z przyciskiem ×, `dismissActionError()`), `error()` zostaje tylko dla błędu ŁADOWANIA. (UI) Usunięta kolumna akcji z wiersza; **wszystkie akcje przeniesione do panelu szczegółów** (3. kolumna „Akcje", pionowy stos, zależne od statusu): Podgląd edycji + **Edycja metadanych** (zmieniona nazwa z „Edytuj") + Wznów + Anuluj. „Adres odbiorcy" rozciągnięty na 2 kolumny (`detail-span2`). Tabela 6 kolumn (colspan 6). Komunikaty błędów akcji biorą `err.error.error` z backendu. GUI `admin-deliveries.spec` **14** (+1 regresja: actionError≠error). `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": przebudowa panelu szczegółów (3 kolumny)**. `delivery-details` grid 2→**3 kolumny**: rząd „Master ID | Version ID | Status", rząd „Utworzono | Ostatnia próba | Następna próba". **Usunięto „Deadline"** z panelu. „Podgląd edycji" **przeniesione z akcji wiersza do panelu szczegółów** (wiersz `detail-actions`, widoczne tylko po rozwinięciu) — w `col-action` zostają Edytuj/Wznów/Anuluj. GUI `admin-deliveries.spec` 13, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": usunięta też kolumna „Ostatnia próba"**. Kolumna `lastAttemptAt` usunięta z tabeli (nagłówek+filtr+komórka); wartość zostaje w panelu szczegółów („Ostatnia próba", już tam była). Tabela 7 kolumn (colspan 7). Wyczyszczony `filterLastAtt` + `setFilter` zwężony do `id|doc|att|created|lock`. GUI `admin-deliveries.spec` 13, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": usunięte kolumny Następna próba/Deadline/Ostatni błąd z tabeli**. Te 3 kolumny (nagłówki + filtry + komórki) usunięte z tabeli; wartości przeniesione do panelu szczegółów (rozwinięcie): dodano „Następna próba" i „Deadline" obok „Komunikat błędu" (lastError już tam był). Tabela ma teraz 8 kolumn (colspan 8). Wyczyszczone sygnały/logika filtrów `filterNextAtt`/`filterDeadline`/`filterError` + zwężony typ `setFilter`. „Podgląd edycji" nadal tylko po rozwinięciu. GUI `admin-deliveries.spec` 13, `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": Podgląd edycji + Następna próba/Deadline/Ostatni błąd tylko po rozwinięciu**. W `admin-deliveries` przycisk „Podgląd edycji" (`openInEditor`) oraz wartości kolumn `nextAttemptAt`/`deadlineAt`/`lastError` renderują się tylko gdy wiersz jest rozwinięty (`@if (isExpanded(deliveryId))`); w stanie zwiniętym te komórki są puste, a przycisk ukryty. Pozostałe akcje (Edytuj/Wznów/Anuluj) i kolumny bez zmian. Filtry tych kolumn działają dalej na danych. `ng build` OK. Wcześniej:

2026-06-11 — **Ekran końcowy: wskazówka, gdy „Zamknij kartę" nie zadziała**. `window.close()` zamyka tylko karty otwarte skryptem (Chrome blokuje resztę), więc przycisk „Zamknij kartę" często nic nie robił. `closeFinishedTab()` po próbie ustawia (po 300 ms) `tabCloseBlocked` → overlay końcowy chowa przycisk i pokazuje wskazówkę o ręcznym zamknięciu (Ctrl/⌘+W). Timer sprzątany w `ngOnDestroy`. To ograniczenie przeglądarki, nie bug. GUI `document-editor.spec` 62, `ng build` OK. Wcześniej:

2026-06-11 — **„Otwórz w edytorze" przeniesione do wiersza wysyłki**. Usunięto osobną zakładkę/stronę „Otwórz plik w edytorze" (route `/admin/open`, komponent `admin-open-document` skasowany, pozycja sidebaru usunięta). Zamiast tego w `admin-deliveries` każdy wiersz ma przycisk **„Otwórz"** → `openInEditor(item)` nawiguje do `/editor?masterId=&versionId=` na podstawie danych wysyłki (`documentId`→masterId, `sourceVersionId`→versionId; bez versionId → podgląd). Testy: GUI `admin-deliveries.spec` **13** (+2 nawigacja z/bez versionId); usunięto `admin-open-document.spec`. `ng build` OK. Wcześniej:

2026-06-11 — **Wysyłka na returnUrl: multipart/form-data zamiast octet-stream**. `HttpDeliverySender` wysyła plik jako `multipart/form-data` w polu **`file`** (filename `document.docx`, part `Content-Type` = DOCX MIME) zamiast surowego `application/octet-stream` body. Nagłówki `Idempotency-Key`/`X-Content-SHA256` i klasyfikacja wyniku (perm/retryable) bez zmian. Uzasadnienie nazwy/typu: finish-and-send dostarcza zawsze edytowalny DOCX (PDF nie ma wersji edytowalnej). Testy: Infrastructure **+3** `HttpDeliverySenderTests` (multipart+nagłówki, 422=permanent, 503=retryable). **Uwaga kontraktowa:** odbiorca musi czytać plik z pola formularza `file`, nie z surowego body. Wcześniej:

2026-06-11 — **Observability: wyjątki w GCP Logs Explorer jako ERROR, nie INFO**. Przyczyna: domyślny formatter konsoli (MS logging w internal API; Serilog `WriteTo.Console()` w external API) pisze zwykły tekst na stdout → Cloud Logging taguje wszystko jako INFO (brak pola `severity`). Fix: oba hosty emitują **JSON z polem `severity`** (mapowanie LogLevel→LogSeverity GCP), wyjątek dołączony do `message` (Error Reporting). Internal API: nowy `GcpJsonConsoleFormatter` + `builder.AddGcpStructuredLogging()`. External API: `GcpJsonSerilogFormatter : ITextFormatter` w `WriteTo.Console(...)`. Włączane poza Development lub `Logging:UseGcpFormat=true`; lokalnie zostaje czytelny tekst. Zero nowych zależności. Testy: `Api.UnitTests` **43** (+8 `GcpJsonConsoleFormatterTests`). Oba hosty budują się OK. Follow-up (poza zakresem): `LoggingBehaviour` loguje `{@Request}` (pełny payload, np. base64) — do okrojenia. Patrz ADR-0014. Wcześniej:

2026-06-11 — **Fix: po „Zamknij" w modalu wysyłki użytkownik wracał do edycji**. `window.close()` działa tylko dla kart otwartych skryptem — dla zwykłych kart przeglądarka odmawia, więc po kliknięciu „Zamknij" znikał tylko modal, a użytkownik mógł dalej edytować zakończony dokument. Fix: `closeFinishModalAndExit()` wchodzi w stan końcowy `workFinished` (nowy sygnał), zatrzymuje auto-save (`stopAutoSave` + `autoSaveEnabled=false`) i nadal próbuje zamknąć kartę; gdy karta nie zostaje zamknięta, renderowany jest **blokujący ekran końcowy** (`work-finished-overlay`, z-index 3000 nad toolbarem) „Praca nad dokumentem zakończona" + przycisk „Zamknij kartę" (`closeFinishedTab()` → ponowna próba `window.close()`). Użytkownik nie wraca już do edycji. Testy: GUI `document-editor.spec` **62** (+1). `ng build` OK. Wcześniej:

2026-06-11 — **„Pliki do wysłania": edycja adresu odbiorcy (returnUrl)**. Nowa akcja **Edytuj** w wierszu (dostępna, gdy zadanie nie jest `Sent`/`Sending`) → modal z polem adresu (returnUrl/recipientUrl) + Zapisz/Anuluj. Backend: `DocumentDelivery.UpdateRecipientUrl(url)` (walidacja http(s) + blokada dla Sent/Sending), `UpdateDeliveryRecipientUrlCommand`+handler, endpoint `PUT /deliveries/{id}/recipient-url` (`{ recipientUrl }`). Front: `updateDeliveryRecipientUrl()` w storage service, modal w `admin-deliveries` (sygnały `editingId`/`editUrlValue`/`editError`/`savingEdit`; autoodświeżanie pauzowane gdy modal otwarty). Po zmianie zwykle „Wznów", by ponowić na nowy adres. Testy: Domain `DocumentDelivery` **21** (+5), Application `UpdateDeliveryRecipientUrlCommandHandler` **3**, GUI `admin-deliveries.spec` **11** (+4). `dotnet build` sln OK, `ng build` OK. Bez migracji SQL (zmiana tylko wartości kolumny). Wcześniej:

2026-06-11 — **„Pliki do wysłania": autoodświeżanie 3 s + Anuluj/Wznów**. Lista delivery auto-odświeża się co 3 s (rxjs `interval`, ciche `fetch(silent)` bez spinnera/resetu strony; przełącznik „Autoodświeżanie", `OnDestroy` sprząta sub; pauza w trakcie ładowania/akcji). Akcje per wiersz: **Wznów** (`retryDelivery` → `/deliveries/{id}/retry`) dla `RetryScheduled`/`FailedPermanently`/`DeadLettered`/`Cancelled`; **Anuluj** (nowy `/deliveries/{id}/cancel`) dla `Pending`/`RetryScheduled`. Backend: nowy status **`Cancelled`** (enum `DeliveryStatus` + `IsTerminal`), `DocumentDelivery.Cancel()` (Pending/RetryScheduled→Cancelled, czyści lease) i **rozszerzony `Requeue()`** (dawniej tylko terminalne-nieudane → teraz też RetryScheduled „wyślij teraz" i Cancelled); `CancelDeliveryCommand`+handler (anulowanie zdejmuje dokument ze stanu Sending → `MarkSaved`); endpoint `POST …/cancel`. **Migracja SQL `008_add_delivery_cancelled_status.sql`** rozszerza CHECK constraint o `Cancelled` (uruchomić na środowiskach!). Worker nie claimuje Cancelled (poza due/active). Testy: Domain `DocumentDelivery` **16** (+6), Application `CancelDeliveryCommandHandler` **3**, GUI `admin-deliveries.spec` **7** (nowy). `dotnet build` sln OK, `ng build` OK, GUI **244**. Wcześniej:

2026-06-11 — **Admin: „Otwórz plik w edytorze"**. Nowa pozycja w sidebarze panelu admina (`admin-shell`) + strona `admin-open-document` (route `/admin/open`): formularz `Master ID` (wymagane) + `Version ID` (opcjonalne) + przycisk „Otwórz". Nawiguje do `/editor?masterId=&versionId=` (z `versionId` → edycja wskazanej wersji, bez → podgląd wersji bazowej) — te same query params, których edytor już używa. Stateless, sygnały + `canOpen` (blokada gdy brak masterId). Testy: GUI `admin-open-document.spec` **4** (nawigacja z/bez versionId, walidacja). `ng build` OK. Wcześniej:

2026-06-11 — **5 zgłoszeń: dialog hasła, .doc, GUI „wysyłanie", EMF→PNG, VersionId w mailu**. (P1) Dialog hasła nie zamyka się już klikiem w tło ani Escape — tylko `Anuluj`/`Otwórz`; `cancelPasswordDialog()` nie zostawia pustego dokumentu: bez `returnUrl` → `router.navigate(['/'])`, z `returnUrl` → `tryCloseBrowserTab()` (przepływ powrotu jak po „Zakończ"). (P3) Modal „Trwa wysyłanie pliku…" używa teraz tego samego spinnera co ekran ładowania (`.loading-spinner`, klasa `.finish-dialog-spinner`) — spójny wygląd z „Przetwarzanie…". (P5) Mail „Zgłoś" podstawia realny **Version ID** z `documentVersionId()` (było `documentMetadata().version` = zwykle `—`); fallback `—` tylko gdy wersji faktycznie brak; `Master ID` bez zmian. (P4) **EMF/WMF są teraz rasteryzowane do PNG** w przeglądarce: nowy DIB-extractor (rekordy `EMR_STRETCHDIBITS`/`SETDIBITSTODEVICE`/`BITBLT`/`STRETCHBLT`/`ALPHABLEND` + skan `BITMAPINFOHEADER`) → BMP → **SkiaSharp** (`SKBitmap.Decode`→PNG; pure-managed, Linux/GCP-safe). Placeholder „EMF — podgląd w Word" zostaje TYLKO dla czysto wektorowego metafile bez osadzonego rastra. Eksport: `data-original-src` niesie oryginalny EMF/WMF dla KAŻDEGO metafile (nie tylko placeholdera) → writer zapisuje wektorowy oryginał, PNG to wyłącznie podgląd. (P2) **Binarny .doc jest konwertowany do .docx**: nowy `LegacyDocBinaryConverter` parsuje FIB + piece table (CLX/PlcPcd, compressed CP1252/Latin1 i 16-bit Unicode) i buduje DOCX (tekst + akapity) przez OpenXML; wpięty w `DocumentInputNormalizer` (gałąź `WordDocument`), z fallbackiem na dotychczasowe kontrolowane odrzucenie przy niespójnej strukturze. Testy: Infrastructure **155** (+1 EMF→PNG, +4 .doc), Api `DocumentController` 13, GUI `document-editor.spec` **61** (+4). `dotnet build` sln OK, `ng build` OK. Wcześniej:
2026-06-10 — **Pełny wzorzec Qutas/D2WebCore dla Entra ID (ADR-0012)**. Backend: goły `JwtBearer` → **`Microsoft.Identity.Web` 3.12.0** (`AddMicrosoftIdentityWebApi`; pin JwtBearer usunięty — Identity.Web per-TFM). **Mapowanie grup→role**: `RolesOptions` (sekcja `Roles`) + `ClaimsTransformer` (claim `groups`→`APP_*`; App Roles zachowane, hybryda). **Graph v5 (5.103.0)**: `GraphUserService` app-only + `GET /api/identity/users` (`IdentityController`, admin); bez sekretu → `DisabledGraphUserService`. **GCP Secret Manager** (`EntraSecretLoader` → `AzureAd:ClientSecret`, guard+try/catch). **Keycloak dual-auth**: policy scheme `EntraOrKeycloak` wybierający po issuerze (gdy `Keycloak:Enabled`). Frontend: **runtime `config.json`** (`public/assets/configs/`) ładowany w `main.ts`→`RUNTIME_AUTH_CONFIG` (fallback `environment.auth`); fabryki MSAL/`appAdminGuard`/`CurrentUserService` z runtime-configu. Wszystkie wartości env = **placeholdery**. Testy: `Api.UnitTests` **41** (+6 `ClaimsTransformerTests`), GUI **236**, oba buildy OK. **Aktywacja per-środowisko** wymaga realnych wartości Entra/GCP/Keycloak + app consent Graph; runtime niezweryfikowany lokalnie. Adaptacja do SPA+API: bez serwerowego OIDC. Zrobione na bieżącym drzewie (merge `feature/azure` wciąż otwarty — `.claude/settings.json` do rozwiązania). Wcześniej:

2026-06-09 — **„Lista plików": statusy po polsku**. `admin-files.statusLabel` przetłumaczony: Saved→Zapisany, Editing→W edycji, Sending→Wysyłanie do odbiorcy, DeliveryFailed→Odbiorca nie odpowiada, Sent→Wysłany (wartości enuma API bez zmian; filtr tekstowy działa na nowych etykietach). `tsc` OK. Wcześniej:

2026-06-09 — **Panel administracji: domyślnie firmowa czcionka**. `admin-shell` `:host` dostał `font-family: var(--corporate-font-family, 'Calibri','Segoe UI',Arial,sans-serif)` — dziedziczy na sidebar i strony routowane (admin-files/admin-deliveries). Dodatkowo globalna reguła w `styles.scss` (`d2-admin-shell input,select,button,textarea { font-family: inherit }`), bo natywne kontrolki formularzy nie dziedziczą fontu. `--corporate-font-family` pochodzi z `assets/fonts/_corporate-font.scss` (gdy firmowy krój nieskonfigurowany → fallback Calibri). `ng build` (AOT) OK, admin-shell spec 3/3. Wcześniej:

2026-06-09 — **Panel „Pliki do wysłania": rozwijane szczegóły wysyłki**. Klik w wiersz rozwija pod nim panel ze szczegółami: **adres odbiorcy (recipientUrl)**, **Master ID** (=documentId), **Version ID** (=sourceVersionId), status, utworzono, ostatnia próba, **komunikat błędu**. Backend: `DeliveryListItemDto` rozszerzony o `SourceVersionId` + `RecipientUrl` (mapowanie w `GetDeliveriesByStatusQueryHandler`). Front: `DeliveryListItem` +`sourceVersionId`/`recipientUrl`; `admin-deliveries` sygnał `expandedId` + `toggleExpand`/`isExpanded` (jeden rozwinięty naraz), klik w `.doc-row` (retry ma `stopPropagation`), caret ▸ obraca się. Testy: Application **205** (+1 mapowanie pól; razem `GetDeliveriesByStatusQueryHandlerTests` 8), GUI AOT build OK. Wcześniej:

2026-06-09 — **Panel „Pliki do wysłania": domyślny filtr = wszystkie statusy**. Panel pokazywał pusto, bo domyślny filtr to był `DeadLettered` (a świeże wysyłki mają Pending/Sending/Sent). Endpoint `GET …/deliveries` wymagał konkretnego statusu. Teraz: parametr `status` **opcjonalny** — pusty/`null`/`"all"` (case-insensitive) ⇒ wszystkie. Backend: nowa `IDocumentDeliveryRepository.GetAllAsync` (+impl, `OrderByDescending(CreatedAt)`), `GetDeliveriesByStatusQuery.Status` → `string?` z gałęzią all/konkret/nieznany, kontroler default `"DeadLettered"`→`""`. Front: `getDeliveriesByStatus`→**`getDeliveries(status?: DeliveryStatus|null)`** (pomija param gdy brak), `admin-deliveries` sentinel **`'all'`** jako domyślny + opcja „Wszystkie" w dropdownie. Testy: Application **204** (+7 nowy `GetDeliveriesByStatusQueryHandlerTests`), GUI AOT build OK, `tsc` OK. Uwaga: nadal nie zobaczysz nic, jeśli tabela `document_deliveries` jest pusta (nikt nie zakończył wysyłki). Wcześniej:

2026-06-09 — **UX „Zakończ": modal wysyłki + odliczanie + zamknięcie karty**. Po kliknięciu „Zakończ" (po zapisie + `finishAndSend`) pokazujemy modal „Trwa wysyłanie pliku do Twojej aplikacji" z przyciskiem „Zamknij (30…0)". Dojście do 0 **lub** klik „Zamknij" → zamknięcie modala + best-effort `window.close()` (w try/catch — gdy przeglądarka odmówi, neutralny stan, bez crashu). Nie czekamy na pełne dostarczenie — robi to worker (retry/backoff do 24h, `finish` idempotentny). Błąd natychmiastowego zakolejkowania → toast „Nie udało się natychmiast wysłać pliku. Ponowimy próbę wysłania w tle." (modal zostaje, brak blokady). Guard `isFinishing` + `[disabled]` na przycisku chronią przed wielokrotnym wywołaniem. **Usunięto** blokujący `pollDeliveryStatus` (z kontradyktoryjnymi toastami). Backend bez zmian. Wydzielony `onFinishCountdownTick` (testowalny bez fake-timerów). `finishSendSub` anulowany w `ngOnDestroy` (brak zapisów po teardownie). GUI **229** (+8, `document-editor.spec` 57). `tsc` OK. Wcześniej:

2026-06-09 — **Security: podbicie podatnej zależności (PRISMA)**. `System.Security.Cryptography.Xml` wchodził tranzytywnie (NPOI/OpenXml) w **8.0.2** — flagowany przez PRISMA. Dodany **jawny** `PackageReference` **10.0.6** w `D2ViewerEditor.Infrastructure.csproj` (wyższa bezpośrednia wygrywa nad tranzytywną); propaguje się do obu hostów API przez `ProjectReference`. Zweryfikowane: `dotnet list package --include-transitive` = 10.0.6 we wszystkich 3 projektach; `dotnet build -c Release` Infrastructure + oba API = OK, 0 błędów, brak NU1605. net8.0 konsumuje 10.0.x bez problemu (precedens: `Microsoft.Extensions.Configuration.Abstractions 10.0.4`). Zero zmian kodu. Wcześniej:

2026-06-09 — **Dwa bugi naprawione**. (1) **Dekrypcja hasłem**: NPOI 2.8.0 nie ma implementacji Agile (brak `AgileEncryptionInfoBuilder` we wszystkich TFM) → każdy plik dostawał „WrongPassword". Nowy **`OoxmlAgileDecryptor`** — własna, czysto zarządzana dekrypcja MS-OFFCRYPTO Agile (SHA512 spinCount + AES-256-CBC); NPOI tylko czyta kontener CFB. Round-trip encrypt→decrypt przetestowany (poprawne/złe/brak hasła). (2) **Font-size input gubił selekcję (FMT-004)**: `saveSelection` zapisywał zwinięte selekcje na blur/selectionchange → klik w input nadpisywał zaznaczenie pustą karetką. Fix: strażnik `editorHasFocus()` — zapis tylko gdy edytor ma fokus. Backend Infrastructure **150** (+2), GUI **213** (+1). Wcześniej:

2026-06-08 — **FMT-005: zmiana czcionki nie wpływała na nowo wpisywany tekst**. `setFontFamily` wołał `editor.focus()` **przed** odtworzeniem selekcji → wybór z natywnego `<select>` czyści selekcję contenteditable, `focus()` przywraca karetkę na początek dokumentu (`rangeCount>0`, więc strażnik `===0` nie odpalał restore), ZWS-span z `font-family` lądował w złym miejscu, a tekst pisany w realnej pozycji dziedziczył domyślną/firmową czcionkę. Fix: odtworzenie selekcji **ZANIM** dotkniemy fokusu (lustro `setFontSize`); aktualizacja istniejącego ZWS-spana zamiast zagnieżdżania; zapis karetki + `updateFormattingState`. GUI `wysiwyg-editor.toolbar-actions.spec.ts` **7** (+1). `tsc` OK. Wcześniej:

2026-06-08 — **EMF/eksport + import font-size + import page-break + selekcja**. (IMG-009) writer pisał placeholder SVG jako goły `a:blip` = uszkodzony DOCX; teraz reader niesie oryginał w `data-original-src`, writer zapisuje prawdziwy **EMF part** + twardy guard „nigdy gołego SVG blip". Przy okazji OpenXmlValidator wykrył i naprawiono **2 pre-existing bugi kolejności w `styles.xml`** (heading rPr/pPr) psujące każdy zapis. (IMP-005) „14pt→10pt": reader OK, ale front `_flattenTopBlocks` rozwijał `.document-content` z defaultem → `_captureDocumentDefaults` + bind font-size/family na stronie. (IMP-008) „3 strony→1": reader ignorował `w:pageBreakBefore` → teraz emit marker page-break. (FMT-004) `savedSelection` tylko na blur (za późno) → zapis na `onSelectionChange`. Backend **144**, GUI **211**. Wcześniej:

2026-06-08 — **Edytor: 4 realne naprawy live-bugów**. (PAG-002) `_schedulePaginate` debounce 600→**250 ms** — strona przestaje rosnąć poza A4 przed reflow (Enter). (PAG-001) nowy `_tryMergeAcrossPageBackwards` — Backspace na początku strony z AUTO-paginacji scala treść z poprzednią stroną / usuwa pustą stronę (wcześniej tylko manualny page-break był obsłużony). (UI-006) `onContextMenu` clamp `Math.max(8,…)` — menu nie wjeżdża nad toolbar na niskim oknie. (PDF-010) `pdf-viewer.ts` loguje konkretny błąd + rozróżnia awarię workera od uszkodzonego pliku (koniec maskowania). GUI **209** (+7). tsc OK. Wcześniej:

2026-06-08 — **Akapit „Ustaw jako domyślne" — naprawa (Qutas-PAR-007)**. Przycisk wołał `resetParagraphDefaults()` (reset do wartości bazowych) zamiast zapisać bieżące ustawienia → repointowany na nowe `setParagraphAsDefault()`: snapshot ustawień do `_paragraphDefaults` (default per-sesja) + `applyParagraphSettings()` na bieżącym akapicie (nowe akapity dziedziczą przez contenteditable). `readCurrentParagraphSettings()` przy braku selekcji seeduje formularz z defaultu. Bez localStorage/API. GUI **202** (+3). Diagnostyka pozostałych 10 zgłoszeń edytora: już wdrożone w sesjach 2026-06-03/04 (zweryfikowane w drzewie). Wcześniej:

2026-06-04 — **Health-check API: pierwszy strzał natychmiast + dedup**. `ConnectionStatusService.checkApi()` → publiczne **`checkNow()`** ze strażnikiem in-flight (`_checkInFlight`) — brak duplikatów przy nakładających się wyzwalaczach (start + `online` + interwał). `app.config.ts` `provideAppInitializer` woła `checkNow()` jawnie (nie polega na side-effekcie konstruktora) → pierwszy `/health` zaraz po starcie, przed renderem, non-blocking; guard scala z checkiem konstruktora w 1 żądanie. Polling 30 s/banner bez zmian. GUI **199** (+2). Wcześniej:

2026-06-04 — **Konwerter grafik VML/EMF/WMF → web** (ref: `.ai/GRAPHICS_CONVERSION.md`). Reader emitował EMF/WMF jako `data:image/x-emf` (nierenderowalne) → złamany obraz. Nowy `IGraphicConversionService` (Domain) + `GraphicConversionService` (Infrastructure) — **pure-managed, bez LibreOffice/GDI/System.Drawing** (Linux/GCP-safe): detekcja magic-bytes, wymiary z nagłówków (EMF rclFrame/WMF placeable), ekstrakcja osadzonego rastra, **placeholder SVG** + **pass-through oryginału** (Word renderuje prawdziwą grafikę), sanitizer SVG (XXE/script blok), VML rect/oval/line→SVG. Hook `DocxToHtmlConverter.WebGraphicForLegacy` (a:blip + v:imagedata). Testy: Infrastructure **137** (+21 unit/security/integration). Benchmark `GraphicConversionBenchmarks`. Decyzja: zero nowych zależności graficznych; rasteryzacja EMF przez sidecar = roadmapa. Wcześniej:

2026-06-04 — **Edytor: 8 błędów naprawionych/przeanalizowanych** (ref: `.ai/EDITOR_KEYBOARD.md`). (1) ENTER cofał kursor — kotwica karetki przy repaginacji zmieniona z globalnego offsetu tekstowego na **`{block,offset}`** (pusty akapit ≠ poprzednia linia). (2) font-size input ukrywał tekst — `setFontSize` odrzuca `0`/NaN/zakres. (3) font-family wracała do domyślnej — **root cause backend**: `innerHTML` daje `&quot;`, writer regex ucinał nazwę na `;` w encji → HTML-decode stylu przed parsowaniem. (4) „Wklej bez formatowania" — `insertText` odtwarza selekcję/fokus (klik menu gubił karetkę). (5) interlinia — mapowanie OOXML→CSS **poprawne** (pinned testami); różnice z Word to nieusuwalne metryki fontu + kolaps marginesów. (6) link — `insertLink` odtwarza selekcję + normalizuje URL. (7) `.doc` — **Wariant B: jawne odrzucenie** z instrukcją konwersji (brak konwertera w pipeline). (8) Backspace usuwa manualny page-break na początku strony (`_tryDeletePageBreakBackwards`); strzałki między stronami złagodzone. Testy: backend Infrastructure **116** (+9), Api **34** (+1), App 193, Domain 57; GUI **197** (+12). Import/eksport/autozapis/undo/tabele/obrazy nietknięte. Wcześniej:

2026-06-04 — **Layout: banner środowiska spychał dashboard w dół**. Shell (`d2-root` flex-column `height:100vh`) daje stronie obszar `100vh − banner` (`d2-root > d2-<page>{flex:1 1 0;min-height:0;overflow:hidden}`). Edytor robił to dobrze (`:host{height:100%}`). Bug: `dashboard.scss .dashboard-wrapper` i `pdf-maintenance .maintenance-wrapper` miały **`min-height:100vh`** → wymuszały pełny viewport wewnątrz mniejszego hosta → treść zjeżdżała o wysokość bannera, stopka ucinana. Fix: `100vh`→**`100%`** (banner-agnostyczny idiom `100%-of-host`, jak edytor) — bez magicznych marginesów/zmiennych/`!important`/global CSS. `app.scss .app-container` = martwy CSS, bez zmian. Testy: nowy `pages/layout-shell.spec.ts` (2: wrapper CSS nie zawiera `100vh`, ma `min-height:100%`), GUI **185** (+2). Edytor i bannery nietknięte. Wcześniej:

2026-06-04 — **3 poprawki renderowania (font fallback / nawigacja kursora / numer strony)**. (P1) Reader emitował font jako `,'X',sans-serif` → brakujący Times New Roman renderował się bezszeryfowo (jak Calibri). Fix: `FontFamilyCss`/`GenericFontFallback` dobiera generyk (serif/monospace/sans-serif) wg nazwy kroju; bez hardcode/plików czcionek. (P2) Strony to osobne `contenteditable`, więc ArrowDown/ArrowUp nie przechodził między nimi. Fix: `_tryMoveCaretAcrossPages` + `_isCaretOnEdgeLine` + `_placeCaretAtEditorEdge` w `handleKeyboard` (tylko zwinięta karetka, bez modyfikatorów — Shift/zaznaczenia nietknięte). (P3) Pole PAGE w stopce ignorowało font-size runów stopki. Fix: reader `FieldSpan` niesie rPr pola; writer `BuildFieldRun` → poprawny `w:fldSimple` z wewnętrznym runem (rPr), `FlushPending` uznaje paragraf z `SimpleField`; frontend `_pageNumberHtml`/`_inlineFieldFontSize` + CSS `font-size:inherit`. Testy: backend Infrastructure **107** (+4), Application **193**, Domain **57**; GUI **183** (+7). Api.UnitTests pominięte (lock DLL od działającej instancji API — nie dotyczy Infrastructure). Ograniczenia: edge-line detection layout-zależne (jsdom nie odtwarza → stub w testach); fallback nie podstawia brakującego kroju (świadomie). Wcześniej:

2026-06-03 — **Font Times New Roman + page break display (druga iteracja)**. (Font) Reader ignorował font **domyślnego stylu akapitu `Normalny` (`w:default="1"`, ascii=Times New Roman)**, który nadpisuje docDefaults (asciiTheme→Cambria) → edytor pokazywał Calibri. Fix: `ApplyDefaultParagraphStyleFont` ustawia font kontenera z default-style. Realny plik: kontener `font-family:'Times New Roman';font-size:10.5pt`. (Page break) Round-trip był OK (R-15), ale **display** wciąż gubił podział: `_splitHtmlIntoPages` **konsumował** marker → `_repaginateNow` scalał. Fix: split **zachowuje** marker → repaginate honoruje (`_isPageBreakBlock`), zapis go nie gubi; usunięty martwy `_joinPagesWithBreaks`. **386 backend** (+4 `DefaultStyleFontTests`), **176 GUI** (+1). Wymaga reloadu dokumentu w edytorze. Ograniczenia: font niedostępny w przeglądarce → fallback (nazwa zachowana); autosave bezstanowy R-20. Wcześniej:

2026-06-03 — **Page break „od nowej strony" w edytorze (display)**: manualny break nie zaczynał nowej strony w widoku (np. „PROTOKÓŁ…" pod podpisami). Reader emituje break-only akapit jako **top-level** `<div class="page-break">` (`IsPageBreakOnlyParagraph`) zamiast zagnieżdżony (psuł `_splitHtmlIntoPages` + ignorowany przez height-paginację). Frontend `_repaginateNow` **wymusza nową stronę** na bloku page-break (`_isPageBreakBlock`), marker zostaje w treści (przeżywa zapis). Backend **378 pass** (+1), GUI **171** (+1). Wymaga ponownego wczytania dokumentu w edytorze. Wcześniej:

2026-06-03 — **R-15/R-16/R-17**: (R-15 ZAMKNIĘTE) writer konwertuje page-break (zagnieżdżony/blok/CSS) → `w:br type=page`, round-trip zachowany (AFTER=1=GOOD). (R-16 CZĘŚCIOWE) `ConvertPreservingPackage` — pass-through `styles.xml`/`theme`/`fontTable` z oryginału (AFTER: **164 style + Cambria** vs BAD 16+Calibri); wpięte w `DownloadEditedDocument` (ładuje v1, fallback gdy brak). (R-17 ZAMKNIĘTE) scalanie fragmentów split-table w `getContent` (`data-split-table-id`+`_mergeSplitTables`, colgroup zachowany; niezależne tabele bez zmian). Testy: backend **377 pass** (+10 Infra, +1 App), GUI **170** (+2). `zapisany_AFTER.docx` zregenerowany pass-through. **Pozostaje: R-19** (numbering nieprzenoszony), **R-20** (pass-through tylko Download, nie autosave — autosave wciąż stratny), **R-21** (`styles2.xml` artefakt SDK). Checklista weryfikacji Word: `DOCX_CONVERSION.md` §6b. Wcześniej:

2026-06-03 — **Round-trip fidelity: naprawa 4→7 stron + powiększonych tabel** (analiza orginał_GOOD vs zapisany_BAD). 4 fixy źródłowe: (1) reader domyślny padding komórek `4px`→**0** góra/dół (Word default) — tabele nie rosną w pionie; (2) writer `tblW` domyślnie **auto** zamiast `pct 5000` — tabele nie rozciągane do 100%; (3) writer tabelowy `tblCellMar` 40/80→**0/108**; (4) writer `AddPageSettings` bez inflacji marginesów + **rekonstrukcja header/footer distance** = `clamp(margin−band,0,720)`. Frontend: `getContent()` scala strony **bez** `<div class="page-break">` (nie materializuje auto-paginacji wg wysokości → koniec nadmiarowych twardych breaków). **Weryfikacja na realnym pliku** (reader→writer→AFTER): top 567/bottom 850 (vs BAD 1281/1231), header 6/footer 339 (vs 720/720), tblW auto (vs pct 5000), tcMar góra+dół=0 (vs 8160 tw), twarde breaki 0 (vs 3) — **AFTER ≈ GOOD**. Testy: backend **366 pass** (+4 `RoundTripLayoutFidelityTests`, +regen 2 golden), GUI **168** (+2 `getContent`). Ograniczenia: R-15 (1 oryg. break nieodtworzony), R-16 (model stratny: styles/numbering/theme/font), R-17 (split-table=2 tabele), R-18 (zapiekane trHeight). Wcześniej:

2026-06-03 — Dokumentacja: nowy **`.ai/DOCX_CONVERSION.md`** — zweryfikowany w kodzie reference konwersji DOCX↔HTML (pipeline + 3 diagramy Mermaid, model `DocumentContent`, macierz statusów Implemented/Partially/Planned z kierunkiem R/W i ścieżkami, roadmapa R-10 / computed-style / domknięcia 5,6,7, harness testów). `INDEX.md` + cross-linki z `FIDELITY_REPORT`/`FEATURES`. Zero zmian kodu (testy bez zmian: backend 358, GUI 166). Kluczowe ustalenia: multi-section = tylko 1 sekcja (R-10); computed-style częściowy (brak centralnego resolvera); rotacja obrazów brak; VML tylko podstawowy; `tcMar` jest, `tblCellSpacing` brak; `w:tabs` na zapisie tylko w stylach Header/Footer. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 8 (audyt izolacji CSS — bez zmian kodu)**: ustalono, że treść jest już izolowana (inline > klasy przez specyficzność). Faktyczne `!important` w `wysiwyg-editor.scss` = **6** (nie 65; tylko 1 dotyczy treści — świadoma Calibri Light→Calibri w nagłówkach); reszta + `styles.scss`/`document-editor.scss` to UI (podświetlenia/paddingi), nie nadpisują treści. `FIDELITY_REPORT` skorygowany. Brak zmian kodu → testy bez zmian (backend 358, GUI 166). **Cały plan 0–9 pokryty** (3,5,6,7 jako wartościowe slice'y; pozostaje multi-section R-10 i pełny computed-style — udokumentowane). Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 7 (tab-stopy nagłówka/stopki)**: akapit z tab-stopem Center/Right → `display:flex`; run-tylko-tab → rosnący spacer `flex:1 1 0` (bezpośrednie dziecko flexa) → układ lewo⇥środek⇥prawo rozkłada się na szerokości. Znak taba zachowany (round-trip OK), mieszane runy bez zmian. **80/80 Infrastructure** (+golden `TabStopLeftCenterRight`). Ograniczenia: środek nie zawsze idealnie centrowany; writer nie emituje `w:tabs` (po zapisie układ wraca do tabów, bez regresji); multi-section (R-10) wciąż otwarte. Kolejność: 0✅1✅2✅3◐4✅5◐6◐7◐9✅ → **8 (izolacja CSS front)** → domknięcia. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 9 (raport zgodności)**: nowy `.ai/FIDELITY_REPORT.md` — macierz wierności (wysoka/częściowa/niemożliwa + przyczyny techniczne), pokrycie testami (backend **358 pass**, GUI **166 pass**), rekomendowane kolejne kroki (priorytet: Etap 7 multi-section/tab-stopy, Etap 3 cd. computed-style, Etap 8 izolacja CSS). Indeks `.ai/INDEX.md` rozszerzony. Dostarczone w sesji: Etapy 0,1,2,3◐,4✅,5◐,6◐,9. Pozostaje: 7 (header/footer multi-section/tabs), 8 (izolacja CSS front), domknięcia 3/5/6. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 6 (obrazy — alt text)**: round-trip tekstu alternatywnego `<img alt>` ↔ `wp:docPr/@descr` (reader: descr/title→alt tylko gdy niepuste; writer: alt→descr w inline+anchor, `DeEntitize` p/podwójnemu escape). **79/79 Infrastructure** (+3 `ImageAltTextRoundTripTests`), golden niezmienione. Obrazy wcześniej: floating/border/crop/EMU round-trip. Pozostaje: rotacja, skalowanie crop, VML. Kolejność: 0✅1✅2✅3◐4✅5◐6◐ → **7 (header/footer)** → 8 (izolacja CSS) → 9 (raport). Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 4 full-stack ZAMKNIĘTY (rozmiar strony + orientacja)**: pełny round-trip DOCX→reader→`pageSize`→front→zapis→writer `w:pgSz`. Backend: `PageSize` przez 3 komendy/handlery/kontrolery (Save/Sign/Download). Front: `document.model.ts` (`PageSize`), sygnał `documentPageSize` (load + `buildSaveRequest`), 2 testy Vitest. Backend **358 pass**, GUI **166 pass**, build OK. R-14 zamknięte. Kolejność: 0✅1✅2✅3◐4✅5◐ → **6 (obrazy)** → 7 (header/footer) → 8 (izolacja CSS) → 9 (raport). Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 4 backend (rozmiar strony + orientacja)**: model `PageSize` (cm + portrait/landscape) w `DocumentContent`/`SaveDocumentRequest`/`SignDocumentRequest`; reader `ExtractPageSize` z modelu `PageSettings`; writer `Convert(... PageSize? pageSize)` emituje `w:pgSz`(+`Orient`) z fallbackiem A4 (bez regresji). Cała solucja **358 pass / 0 fail / 6 skip** (+3 `PageSizeRoundTripTests`). Interfejs `IHtmlToDocxConverter` rozszerzony (opcjonalny param → mocki Moq zaktualizowane). **Pozostaje full-stack:** przewlec `PageSize` przez komendy/DTO + front (model/editor/ruler/scss/Vitest), by zapis round-tripował rozmiar (R-14). Kolejność: 0✅1✅2✅3◐4◐(backend) 5◐ → front Etapu 4 → 6 → 7 → 8 → 9. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 3 (style znakowe)**: `ConvertRunToHtml` rozwiązuje `w:rStyle` — run dostaje CSS nazwanego stylu znakowego (z dziedziczeniem `basedOn`) pod formatowaniem bezpośrednim (direct wygrywa). Naprawia runy formatowane wyłącznie stylem znakowym (Hyperlink/Strong/własny), które wcześniej traciły formatowanie. **73/73 Infrastructure** (+golden `CharacterStyleRun`); istniejące snapshoty niezmienione. Pozostaje w Etapie 3: pełny computed-style (docDefaults folding, numbering/linked styles, theme). Kolejność: 0✅1✅2✅3◐ → **4 (page size full-stack)** → 5◐ → 6 → 7 → 8 → 9. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 2 (model pośredni, read-side, strangler)**: nowy `Infrastructure/DocxModel/` — `PageSettings` (geometria strony/sekcji w surowych twipsach + page size + orientacja) + `SectionPropertiesReader` (czysty parser `sectPr`→model). `DocxToHtmlConverter` liczy margines i wysokość pasma nag/stopki z modelu (`ComputeBandHeightCm`), zachowanie **1:1** (golden-snapshoty niezmienione). Page size/orientacja już parsowane (fundament Etap 4). **72/72 Infrastructure** (+5 testów modelu). Kolejność realizacji: 0✅ 1✅ 2✅ → **3 (style resolver)** → 4 (page size full-stack) → 5(tabele częściowo✅) → 6 → 7 → 8 → 9. ADR-0009. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 5 (tabele)**: `ConvertTableToHtml` emituje `<colgroup>` z `tblGrid` + `table-layout:fixed` (gdy `tblLayout=fixed`/jawna szerokość; fallback szerokości = suma kolumn grid) → przeglądarka honoruje geometrię kolumn Worda. Naprawiony bug `rowspan` przy `vMerge` sąsiadującym z `gridSpan` (dopasowanie wg pozycji kolumny w gridzie, nie indeksu) + obsługa jawnego `val="continue"`. Writer ignoruje `<colgroup>` (czyta `.//tr`, odtwarza grid) → round-trip bez regresji. **67/67 Infrastructure** (+golden `MergedCellsTable`). Pozostaje: cellMargins/shading/cellSpacing, fixed na zapisie. Wcześniej:

2026-06-03 — Refaktor DOCX→HTML **Etap 0+1** (fundament wierności z Wordem; plan: `~/.claude/plans/fancy-spinning-wirth.md`). **Etap 1**: nowy `Infrastructure/Conversion/OoxmlUnits.cs` — jedno źródło prawdy dla jednostek OOXML (twips/EMU/half-points/cm/px, nazwane stałe, DPI=96). Oba konwertery (`DocxToHtmlConverter` 2950 l., `HtmlToDocxConverter` 2525 l.) przekierowane na `OoxmlUnits`; usunięte zduplikowane helpery i magiczne stałe (`567`/`1440`/`914400`/`9525`/`/20`/`*0.75`/`/2`) — reguła 14 spełniona. Zachowanie liczbowe niezmienione (lokalny rounding zostaje); cm liczone dokładnym `1440/2.54` zamiast przybliżenia `567`. **Etap 0**: harness regresji `Infrastructure.UnitTests/Golden/` — `GoldenDocuments` (5 deterministycznych DOCX in-memory), `HtmlSnapshot` (normalizacja base64/`data-image-id`/daty, approve-on-missing → `__snapshots__/*.approved.html`), `GoldenSnapshotTests` (snapshoty + asercje punktowe jednostek). **66/66 Infrastructure pass** (54+7+5), build solucji OK, snapshoty stabilne 2×. Kontrakt `DocumentContent`/API bez zmian, front nietknięty. **Następny: Etap 2** (jawny model pośredni read-side, strangler) — duża zmiana, wymaga decyzji. ADR-0008. Wcześniej:

2026-05-29 — Word-like obraz: **7 trybów zawijania + obramowanie + przycinanie** (round-trip `a:ln` + `a:srcRect`). Panel: 7 trybów wg menu Worda (5 działa: Równo z tekstem / Ramka / Góra i dół / Za tekstem / Przed tekstem; 2 disabled: Przylegle / Na wskroś — wymagają reflow tekstu, niedostępne natywnie w HTML/CSS). Border section: checkbox+color+grubość 1–20 px+styl ciągła/przerywana/kropkowana. Crop section: 4 inputs % (0–95) + Resetuj. `wysiwyg-editor` extended: `setSelectedImagePositionMode` obsługuje `square` (`float: left`) i `topBottom` (`display: block; clear: both`), nowe `setSelectedImageBorder` (inline CSS border + persist `data-border-*`), `setSelectedImageCrop` (`clip-path: inset()` + persist `data-crop-*`), `resetSelectedImageCrop`. `wrapExistingImages` przywraca po loadzie square/topBottom/border/crop z data-attrs. Backend writer (`HtmlToDocxConverter`): `data-border-*` → `a:ln`+`a:solidFill`+`a:prstDash` (`solid`/`dashed`/`dotted` mapping), `data-crop-*` → `a:srcRect` (1/1000 %). Reader (`DocxToHtmlConverter`): czyta `a:ln`/`a:srcRect` → `data-border-*`/`data-crop-*`. Tests: nowy `ImageBorderCropRoundTripTests` **7/7 pass** (border 3 style + bad-color fail-safe + crop 2 cases + plain-image guard) + 4/4 floating round-trip + 4 nowe testy panelu (7 modes z disabled, 5 emisji, border toggle, crop clamp, resetCrop). GUI **164/164**, Backend **329/329** (Infrastructure 50 +11 nowych). Wcześniej:

2026-05-29 — Word-like floating: **Przed/Za tekstem** + drag w obrębie strony + **round-trip `wp:anchor`**. Panel boczny: 3 przyciski trybu z aktywnym podświetleniem (`W tekście` / `Przed` / `Za`); `ImageSelectionState.positionMode` + `@Output() positionModeChange`. Editor: `setSelectedImagePositionMode()` (przy floating: `position: absolute`, `z-index` 10/−1, persist `data-x/y-px/-emu`); drag-aware mousedown (floating → update left/top zamiast DOM reinsert); `wrapExistingImages` przywraca floating po imporcie. CSS: `.editor-image-wrapper[data-pos-mode="front"]` z=10, `[data-pos-mode="behind"]` z=−1; edytory dostają `position: relative; z-index: 0` (kontekst stacking). **Backend**: `HtmlToDocxConverter` emituje `wp:anchor` (positionH/V relativeFrom=page + offsety z `data-x/y-emu` + `wp:wrapNone` + `BehindDoc`) gdy `data-pos-mode` set; `DocxToHtmlConverter` czyta `wp:anchor` → `data-pos-mode`/`data-x-emu`/`data-y-emu` na `<img>`. Tests: **4 nowe `ImageFloatingRoundTripTests` pass** (front/behind round-trip pozycji+trybu, inline-guard, size+pozycja); GUI **162/162 pass** (+2 testy panelu). **Pozostaje**: wrap modes square/tight/through (text reflow — niedostępne natywnie w HTML), drag między stronami/sekcjami. Wcześniej:

2026-05-29 — Word-like pozycjonowanie obrazów (MVP): czwarty docked panel **`d2-image-properties-panel`** (Rozmiar/Wyrównanie/Opływanie info-only/Usuń) w slocie Wyszukiwanie/Tabele/Nagłówek+Stopka. Stateless — wszystkie akcje przez nowe public methods na `wysiwyg-editor`: `setSelectedImageWidth/Height(px, lockAspect)`, `setSelectedImageAlignment(L/C/R/null)`, `resetSelectedImageAspect()`, `removeSelectedImage()`. Nowy `@Output() imageSelectionChange` emitowany przy select/deselect/resize-end/drop-end. Sygnał `selectedImage` + `imageLockAspect` (default true) w `document-editor`; `showImagePanel = selectedImage()!=null && !showFindReplace() && !showTablePanel()`; **panel obrazu ma pierwszeństwo nad HF panelem** (rozsądny UX: formatowanie logo w nagłówku w trakcie edycji). Round-trip rozmiaru przez istniejące `data-*-emu` na `<img>`; alignment przez `text-align` na akapicie. Drag/resize mechaniki bez zmian — tylko emit snapshot po każdej operacji. Tests: **160/160 pass** (+9 panel, +1 koordynacja). **Ograniczenie MVP**: floating wp:anchor (square/tight/behind/in-front wrap) odłożone — panel pokazuje sekcję info-only „W tekście (inline). Floating w przygotowaniu." Wcześniej:

2026-05-29 — Reguła `userDownload` (kontrola pobierania edytowanego pliku). Opcjonalne pole w `documents.metadata` JSON: default **false**; brak/null/non-true ⇒ blokada; tylko jawne `true` zezwala. **Local upload** (`UploadDocumentCommandHandler`) **automatycznie** ustawia `userDownload=true` (anti-tamper — system, nie klient). **External ingest** (`POST /api/v1/document`) dostaje opcjonalne `UserDownload: bool?`. Nowy gated endpoint `POST /api/documentstorage/{masterId}/user-download` (`DownloadEditedDocumentCommand` + handler) → 403 gdy off (sentinel `USER_DOWNLOAD_FORBIDDEN:`). Frontend: sygnał `userDownload` + `canUserDownload` computed; pozycja menu „Pobierz dokument" gated; `downloadDocument()` POST na nowy endpoint z obsługą 403. Wspólny `ExternalDocumentMetadata` (parser+serializer) zastąpił 4 lokalne kopie. DTO metadanych/statusu eksponują flagę. **325/325 backend pass, 150/150 GUI pass**. **Ograniczenie**: `GET .../download` i `GET .../versions/{vid}/download` (load do edytora) celowo nie gated — zwracają ostatnio zapisaną wersję, nie aktualny stan edytora; do follow-up. Wcześniej:

2026-05-29 — External API: 3 nowe endpointy (callback URL / unlock / status). Wszystkie na **`masterGuid`** (returnUrl w master metadata; brak osobnego user-locka — `Editing` to lock; status = atrybut master). Implementacja: domena (`Document.MarkSaved()` + `Document.UpdateMetadata()`); 2 commands + 1 query w `Application/Features/Documents/`; 3 akcje w `D2ServicesViewerEditor.Api/Controllers/DocumentController` (`PUT .../callback-url`, `POST .../unlock`, `GET .../status`). Walidacja URL współdzielona z `DocumentDelivery.IsValidRecipientUrl`. `callbackUrl` nigdy nie surfacowany w response (`hasCallbackUrl: bool`) ani w logach (token-safe). Idempotencja: callback-url (zapis tego samego URL), unlock (Saved → `Changed=false`). Stany wysyłki blokują obie zmiany (409). Testy: +25 (Application UnitTests); pełna solucja **305/305 pass**. `.ai/API_CONTRACTS` rozszerzony o tabelę endpointów + uzasadnienie decyzji identyfikatora. Wcześniej:

2026-05-29 — Menu „Pomoc" + status autozapisu w stopce: (a) nowa zakładka menu „Pomoc" po „Widok" z jedną pozycją „Zgłoś" delegującą do istniejącego `openReportEmail()` (bez duplikacji); sygnał `showHelpMenu` + `toggleHelpMenu()` + `closeAllMenus` rozszerzony; **stary przycisk „Zgłoś" usunięty** z `.header-right` (toolbar czystszy). (b) **Data/czas autozapisu przeniesione z toolbara do stopki** — `.autosave-status` (min-width:70px) usunięty; przy switchu zostaje tylko switch + label „Autozapis"; w `.status-right` nowy `.status-autosave` („Autozapis: HH:MM:SS" / „zapisywanie…" / „błąd zapisu" / „włączony"; ukryty gdy off). Bez zmian w logice (te same sygnały `autoSaveEnabled`/`autoSaveStatus`/`lastAutoSaveAt`). Testy: +4 w `document-editor.spec.ts`, **146/146 pass**. Wcześniej:

2026-05-29 — Porządek panelu nagłówka/stopki: (a) **usunięty primary CTA „Zamknij nagłówek i stopkę"** z `d2-header-footer-panel` (redundantny z X i ESC) — X jako jedyne zamknięcie z panelu (spójne z `Wyszukiwanie`/tabela); (b) **NG8107**: 9× `editor?.` → `editor.` w bindingach panelu w `document-editor.html` (`editor` jest `!:`, panel renderuje się dopiero po `editingSection() !== 'body'` emitowanym przez edytor — operand nigdy nie null). Tests **142/142**, `ng serve` czysty. Wcześniej:

2026-05-29 — 4 UX poprawki: (1) **autozapis stabilny** — `.autosave-status` rezerwuje przestrzeń (min-width:70px) niezależnie od stanu toggle, więc Zgłoś/Zapisz/Zakończ nie skaczą; (2) **prowadnice marginesów domyślnie ukryte** (`showMarginGuides=false` w obu komponentach); (3) **obraz w trybie edycji nagłówka NIE jest reskalowany** — `wrapExistingImages` nie usuwa już inline width/height; CSS `.editor-image-wrapper img` bez `width:100%`; obraz wygląda tak samo w preview i edit; (4) **pływający pasek nagłówka/stopki USUNIĘTY** — nowy komponent `d2-header-footer-panel` (dock po lewej, ten sam slot co Wyszukiwanie / Tabela), stateless, wszystkie akcje delegowane do `wysiwyg-editor` (Inna pierwsza strona / Wstaw obraz / Numery stron / Format / Usuń / **Primary „Zamknij nagłówek i stopkę"**). Koordynacja single-mode: `showHeaderFooterPanel = computed(() => editingSection()!='body' && !showFindReplace() && !showTablePanel())` — Find/Tables mają pierwszeństwo. Testy `header-footer-panel.spec` (6) + `document-editor.spec` +5 koordynacja. `npm test` **142/142 pass**. **Manual verify**: top-margin drag w trybie edycji nagłówka (overlay usunięty, ale interakcja z `d2-ruler` wymaga weryfikacji w przeglądarce). Wcześniej:

2026-05-28 — Faza 2: audyt trybu edycji nagłówka/stopki. Naprawione realne gapy: (a) **routing wariantu** — `startEditingHeader/Footer` + `onHeaderInput/Footer` + `onHeaderBlur/Footer` zapisują/ładują wariant aktywny na stronie 0 (`firstPageHtml` gdy `differentFirstPage`, inaczej `_headerHtml`); wcześniej edycja first-page niewidocznie nadpisywała default (łamanie rule 10); (b) `_computeHeaderContent/Footer` w trybie odd/even używa kanonicznego `_headerHtml/_footerHtml` dla odd (= default w OOXML); (c) **ESC** w `document-editor.ts` zamyka tryb edycji nag/stopki (priorytet nad bocznymi panelami); (d) przycisk **„Zamknij nagłówek i stopkę"** w obu toolbarach (spec funkcjonalny #3). Nowy `wysiwyg-editor.spec.ts` (7) + `document-editor.spec.ts` (+4 ESC). `npm test`: **131/131 pass**. Ograniczenie: edycja even-page nieosiągalna z UI (template renderuje contenteditable tylko dla strony 0; even round-tripuje z importu). Wcześniej:

2026-05-28 — Faza 1b: pomiary wierności importu nag/stopki na `stupki.docx`. Nowy `DocxToHtmlConverterFidelityTests.cs` (9): geometria (Height ≈ 1.25 cm, Margins ≈ 2.5 cm), font-family z `docDefaults`, image EMU 133×34 px + aspect (4:1 stays 4:1) + `data-width-emu`/`data-height-emu` do round-tripu, L/C/R alignment. Realny plik nie odsłonił nowych gapów na T1–T12. Pełny `Infrastructure.UnitTests` **43/43 pass**. R-11 pozostaje (brak sygnału z tego pliku). Wcześniej:

2026-05-28 — Round-trip first/even header/footer (zapis): `HtmlToDocxConverter` zrefaktorowany — `WriteHeaderPart`/`WriteFooterPart(html, type)` tworzą osobne party Default/First/Even + `HeaderReference`/`FooterReference` z `Type`; `EnsureTitlePage` (do sekcji) i `EnsureEvenAndOddHeaders` (do `settings.xml`). 6 testów round-trip w `HeaderFooterRoundTripTests` (write → read potwierdza pola `DifferentFirstPage`/`FirstPageHtml`/`DifferentOddEven`/`EvenHtml`). Pełny `Infrastructure.UnitTests` **34/34 pass**. Edycja wariantów first/even jest teraz trwała (reguła 10). Wcześniej:

2026-05-28 — Import nagłówka/stopki (Faza 1, fidelity): `DocxToHtmlConverter.ExtractHeader/ExtractFooter` wybierają part **default** wg `sectPr`/`HeaderReference`/`FooterReference` (nie `FirstOrDefault()`, który mógł zwrócić pusty even/first), **first-page** przez `titlePg` → `DifferentFirstPage`/`FirstPageHtml`, oraz **even/odd** przez `settings/evenAndOddHeaders` → `DifferentOddEven`/`EvenHtml` (model domeny rozszerzony o te 2 pola). Front (`document-editor.ts`) spread'uje cały obiekt header/footer na obu ścieżkach load. `.ai/FEATURES.md` uzupełnione (sekcja nagłówek/stopka). Testy backendu **13/13** (`DocxToHtmlConverterSectionReferenceTests` 8 + Header/Footer 5), `tsc --noEmit` OK. R-10 częściowo zamknięte (**pozostaje: round-trip first/even na zapisie + multi-section**). Diagnoza na `stupki.docx`. **Uwaga:** w drzewie roboczym są też 4 niezacommitowane poprawki GUI (env init via `provideAppInitializer`, switch „Autozapis", widoczność „Zakończ" wg `returnUrl`, niższe nagłówki/stopki dialogów) — patrz niżej. Wcześniej:

2026-05-27 — 4 poprawki GUI: (1) admin nazewnictwo `Wysyłki`→`Pliki do wysłania` (`admin-shell`/`admin-deliveries`); (2) dok boczny `syncTablePanel` — ponowne zaznaczenie tabeli przy otwartym wyszukiwaniu przełącza dok na formatowanie tabeli (zamyka search; jeden aktywny tryb; nadpisuje ADR-0006); (3) menu „Wstaw" — QR/Linia pozioma/Podział strony owinięte `@if (false)` (ukryte, logika zostaje); (4) toolbar — przycisk „Akapit" (`editor-toolbar` `@Output openParagraph` → istniejący `openParagraphDialog()`, ta sama ikona co menu „Narzędzia", bez duplikacji). Testy 101/101 (nowe: `admin-shell.spec` 3, toolbar +1, document-editor +5). Build OK. Wcześniej:

2026-05-27 — Globalne bannery górne: nowy kontener `d2-global-banners` w `App` (flow flex, kolejność: środowisko → offline/API), banner środowiska `d2-environment-banner` (źródło: `BuildInfoService.environment()`; mapowanie w `core/utils/environment-banner.util.ts` — Local niebieski / DEV zielony / UAT,TST jasnożółty / PRE purpurowy / PROD,PRD ukryty / nieznane fallback). Pasek braku API (`offline-banner.scss`): usunięty arbitralny `z-index:10000`, w flow → nie przykrywa edytora; logika `ConnectionStatusService` bez zmian. **Uwaga:** brak `fileReplacements` w `angular.json` → `environment.ts` zawsze `PRD`, dlatego env bierzemy z health-check API. Naprawiony stary scaffold `app.spec.ts`. Testy: 92/92 (nowe: util 10, env-banner 10, global-banners 4). Build OK. Wcześniej:

2026-05-27 — ESC zamyka aktywny boczny panel (Wyszukiwanie / właściwości+stylizacja tabeli) tą samą logiką co × — `document-editor.ts` `@HostListener('document:keydown.escape')` → `closeActiveSidePanel()` (deleguje do `closeFindReplace`/`closeTablePanel`); guardy: `event.defaultPrevented` (deselekcja obrazu w edytorze ma pierwszeństwo), otwarty dialog/menu kontekstowe, brak otwartego panelu → no-op; focus wraca do edytora tylko gdy był w panelu. Przycisk „Cofnij" (undo) zweryfikowany — działa, ten sam `undo()` co Ctrl+Z; operacje tabeli wchodzą do historii przez `notifyEditorChange()`→`input`→`onContentChange`→`saveToUndoStack`. Nowe testy: `editor-toolbar.spec.ts` (6) + `document-editor.spec.ts` (7), 13/13 OK. Build OK. Wcześniej:

2026-05-27 — Fix nagłówka/stopki DOCX→HTML: kontener `.header-footer-content` z domyślnym rozmiarem/krojem z `docDefaults` (backend `DocxToHtmlConverter`) + fallback CSS frontu (11pt, `#000`) — naprawia „za duży tekst" i zgubiony kolor. Testy backendu 5/5. Otwarte: R-10 (first-page/even-odd/wiele sekcji — bierzemy tylko pierwszy header part), R-11 (round-trip rozmiaru). Wcześniej:

2026-05-27 — Bugfix cyklu życia aktywnej tabeli: `detectTableContext` nie czyści tabeli przy selekcji poza edytorem (klik w panel boczny ≠ opuszczenie tabeli; model „last known context" w `core/utils/table-context.util.ts`), podpięto `selectionChange` edytora (klik akapitu poza tabelą czyści kontekst + wizualne zaznaczenie komórek). Build OK; `ng test` 55 passed. Wcześniej:

2026-05-27 — Auto-zakres obramowań: usunięto ręczny wybór celu; zakres wnioskowany z zaznaczenia (`classifyBorderTarget`: komórka/wiersz/kolumna/tabela/zakres), panel pokazuje podpis „Zastosowanie: …". Domyślnie (sama karetka) → aktywna komórka. Build OK; `ng test` 49 passed. Wcześniej:

2026-05-27 — Szczegółowy edytor obramowań tabeli (zakładka „Obramowania" w `d2-table-properties-panel`): rodzaj linii (ciągła/przerywana/kropkowana/podwójna/brak), grubość, kolor (paleta+picker+reset), 10 ikon miejsca (wszystkie/brak/zewn./wewn./poziome/pionowe/góra/dół/lewa/prawa) oraz **cel: cała tabela / komórka / zaznaczenie**. Galeria presetów usunięta. Logika = `core/utils/table-style.util.ts` (`applyBorderToCells` względem prostokąta zbioru komórek); styl inline (ADR-0007). Build OK; `ng test` 42 passed. Wcześniej:

2026-05-27 — Stylizacja tabel w panelu `d2-table-properties-panel`: zakładki „Układ"/„Style", gotowe style (`TABLE_STYLE_PRESETS`), obramowania (kolor/grubość/zakres), wiersz nagłówka, wiersze naprzemienne, pierwsza/ostatnia kolumna, reset. Logika = czyste funkcje `core/utils/table-style.util.ts`; styl jako inline na `<table>/<tr>/<td>` (przeżywa zapis HTML→DOCX — ADR-0007). Bez zmian backendu. Build OK; `ng test` 41 passed. Wcześniej:

2026-05-27 — Konfiguracja tabeli przeniesiona z poziomego paska `.table-toolbar` do bocznego panelu `d2-table-properties-panel` (dokowany po lewej, spójny z panelem „Wyszukiwanie"; auto-otwierany po kliknięciu tabeli, wyszukiwanie ma pierwszeństwo w doku — ADR-0006). Naprawiono uruchamianie testów GUI (`angular.json` cel `test` + `src/test-setup.ts` z Zone.js). Build OK; `ng test` panel 6/6 (2 stare scaffoldowe `app.spec.ts` padają niezależnie). Wcześniej:

2026-05-27 — funkcja „Wklej tylko tekst" w edytorze WYSIWYG: util `core/utils/paste-text.util.ts` (htmlToText/normalizeWhitespace/resolvePlainText + 17 testów) i skrót `Ctrl/Cmd+Shift+V` w `wysiwyg-editor.ts`. Build + vitest OK. Otwarte: słaby regexowy `sanitizeHtml` (R-09, rekomendacja DOMPurify), brak przycisku toolbara/menu. Wcześniej:

2026-05-25 — panel admina `/admin/deliveries` (lista wysyłek + filtry + retry + `locked_by`, statusy PL) + uruchomienie migracji `infra/sql/001..007` na lokalnym PostgreSQL (Podman). Wcześniej: funkcja „Zakończ i wyślij" (kolejka `document_deliveries` + worker) + audyt/synchronizacja `.ai/` z kodem.

## Aktualny cel prac

Domknięcie przepływu Krok 1–4 (ingest → podgląd → edycja → zakończ i wyślij). Krok 4 zaimplementowany; Krok 2 (podgląd v1) wciąż w toku.

## Aktualny etap

- Branch: `feature/baz-astral-obciaga-mb`
- Główne obszary kodu:
  - `D2ApiViewerEditor/` (internal API + Application/Domain/Infrastructure)
  - `D2ServicesViewerEditor/` (external ingest API)
  - `D2GuiViewerEditor/` (Angular)
  - `infra/sql/` (schemat)

## Co zostało zrobione (ta i poprzednie sesje)

- Ingest zewnętrzny: `IngestExternalDocumentCommand` + endpoint `POST /api/v1/document` (DOCX → v1+v2, PDF → v1).
- Płaski zapis: `Document.UpdateVersion` + `UpdateDocumentVersionCommand` + `PUT /api/documentstorage/{masterId}/versions/{versionId}` (nadpisanie GCS pod tym samym versionId).
- `DocumentVersion.ModifiedAt` + skrypt `infra/sql/005_add_version_modified_at.sql` + mapowanie EF.
- Endpoint metadanych: `GetDocumentMetadataQuery` + `GET .../{masterId}/metadata`.
- GUI: mechanizm auto-save (timer, `environment.autoSave { enabled, intervalSeconds: 30 }`), switch „AutoSave", sygnały `documentVersionId/autoSaveEnabled/autoSaveStatus/lastAutoSaveAt`.
- GUI: ujednolicony „Zapisz" (API, PUT/POST) + „Pobierz dokument" (dawne „Zapisz" = download lokalny). Usunięto `saveDocumentAs()`.
- Dokumentacja `.ai/` przepisana wg wzorca z `template/`.
- „Zakończ i wyślij": `FinishAndSendDocumentCommand` + tabela `document_deliveries` (SQL 007) + worker `DocumentDeliveryWorker` (claim `FOR UPDATE SKIP LOCKED`, retry/backoff do 24h, snapshot GCS) + endpointy `finish`/`deliveries`/`retry` + GUI `finishDocument()` z pollingiem. Patrz `DECISIONS.md` ADR-0005.
- `documents.status` (`DocumentStatus`) + skrypt SQL 006 (dodane przed tą sesją, teraz udokumentowane).
- Panel admina `/admin/deliveries` (`admin-deliveries`): lista zadań wysyłki (filtr per kolumna + dropdown statusu + paginacja, spójny z `/admin/files`), `locked_by`/`locked_until`, przycisk „Ponów" (`POST deliveries/{id}/retry`), statusy tłumaczone na PL. `DeliveryListItemDto` rozszerzony o `LockedUntil`/`LockedBy`.
- Migracje `infra/sql/001..007` wykonane na lokalnym PostgreSQL (Podman `d2viewereditor_postgres`); `007` utworzył `document_deliveries`.
- Kontrola dostępu do dokumentu (`allowedCorporateKeys`): `DocumentAccessPolicy` (Domain) + `IDocumentAccessGuard` (App). Egzekwowanie → 403 w handlerach treści/metadanych. GUI: `documentAccessGuard` + `DocumentAccessDeniedComponent` (`/access-denied`). Brak migracji DB (klucze w `metadata`).
- **Microsoft Entra ID (docelowo, ADR-0011):** backend JwtBearer + policy `RequireAppOperator`/`RequireAppAdmin`, `[Authorize]` na `BaseApiController` (cała aplikacja za logowaniem), endpointy admina → `RequireAppAdmin`, `ClaimsCurrentUserProvider` (CorporateKey z claimu access tokena), `Administrator` omija `allowedCorporateKeys`. Front: MSAL (`MsalGuard`/`appAdminGuard`/`MsalInterceptor`), sekcja `auth` w `environment`. **Wymaga `dotnet restore` (JwtBearer) + `npm install` (MSAL) + konfiguracji Entra (placeholdery w `appsettings`/`environment`).** Patrz `SECURITY.md`, `DECISIONS.md` ADR-0011.

## Jak uruchomić projekt lokalnie

Patrz `DEVOPS_DEPLOYMENT.md`. Skrót:

```bash
cd D2ApiViewerEditor && dotnet run --project D2ViewerEditor.Api      # 5190
cd D2ServicesViewerEditor && dotnet run --project D2ServicesViewerEditor.Api  # 15112 (swagger: /swagger)
cd D2GuiViewerEditor && npm install && npm start                     # 4200
# Wymaga: PostgreSQL (skrypty infra/sql/ 001..005) + GCS/fake-gcs-server (bucket d2viewereditor-documents)
```

## Jak zweryfikować stan

```bash
dotnet build D2ApiViewerEditor/D2ViewerEditor.sln   # ostatnio: OK (0 błędów)
cd D2GuiViewerEditor && npm run build               # ostatnio: OK
```

## Znane problemy / niedokończone

| Problem | Wpływ | Status |
|---|---|---|
| Tryb podglądu (Krok 2) ładuje aktywną wersję (po DOCX = v2), nie v1 oryginał | Medium | Open — przełączyć GUI na `/download` |
| `finishDocument()` — zaimplementowane: async wysyłka na returnUrl (kolejka `document_deliveries` + worker) | — | Zamknięte |
| `SaveDocumentVersionCommandHandlerTests` oczekuje `UpdateAsync` (płaski zapis go nie woła) | Low | Zamknięte — test asercją `DidNotReceive().UpdateAsync` + `Received(1).SaveChangesAsync` |
| Budżet SCSS `document-editor.scss` przekraczał limit | Low | Zamknięte — `angular.json` `anyComponentStyle` podniesiony (error 48 kB / warn 24 kB) |
| Ręczny „Zapisz" przy włączonym auto-save jest częściowo redundantny | Low | Akceptowalne (wymuszenie zapisu) |
| Port External API potwierdzony: 15112 (`appsettings.json` → `Urls`) | — | Zamknięte |

## Ostatni bezpieczny punkt

Build backendu i GUI przechodzą po zmianach auto-save / „Zapisz/Pobierz".
