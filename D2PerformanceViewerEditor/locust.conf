# Domyślna konfiguracja Locusta — nadpisywana przez CLI / .env
# Użycie: locust -f locustfiles/all_endpoints_locust.py (odczyta ten plik automatycznie)

locustfile = locustfiles/all_endpoints_locust.py
host = http://localhost:5190
users = 10
spawn-rate = 2
run-time = 60s
headless = false
